- **Vai trò:** bộ nhớ ngắn hạn agent; keyword; bullet; không log dài.
- **Repo:** lab CV inference; core portable Windows/Linux; Modal wrapper tách core; không hard-code OS/path/user/GPU; root `docs/` chỉ đọc/sửa khi user yêu cầu.
- **Lệnh:** Windows dùng `py`; Linux server dùng `.venv/bin/python`/pip.
- **Tracking:** mọi thay đổi append `CHANGELOG.md`, timestamp GMT+7, command, file trực tiếp/gián tiếp.
- **Scope:** `tasks/smoke_fire_detection/`; dataset `datasets/smoke_fire_detection/`; artifact `artifacts/smoke_fire_detection/`.
- **Mục tiêu:** early smoke/fire detection video; ưu tiên event-level TTD, false alarm/hour; phụ AUROC, mAP, latency, cost.
- **Scope correction:** FIgLib cadence 60s + Modal/GPU hiện tại chỉ benchmark scenario/hạ tầng thí nghiệm; không phải constraint vĩnh viễn; RQ1 fixed-camera chỉ track hiện tại.
- **Near–far:** phổ kích thước biểu kiến, không hai class; A/B/C không loại trừ; thêm scale-MoE, spatial coarse-to-fine, dynamic model, temporal multi-rate, camera-conditioned, edge-cloud.
- **Architecture gate:** baseline đúng domain + error slice trước; tiny-visible ưu tiên data-scale control → P2/stride4 → P2↔P5 compute-match → adaptive ROI; kernel/neck/distill sau.
- **Dataset:** Pyro-SDIS parquet, 33,636 row, 5,499 empty annotation, source class `1` remap output class `0`; FIgLib 40,362 frame hợp lệ, 511 folder / 510 event có frame, 143 camera, weak label theo ignition offset.
- **FIgLib split (2026-07-14):** camera-disjoint, seed `20260707`, dev 359 seq/108 cam, final 152 seq/35 cam; artifacts `figlib_index.jsonl`/`figlib_audit.json`/`figlib_split_manifest.json`; không hash lock.
- **No-hash rule:** tuyệt đối không tự thêm hash/checksum/fingerprint cho dataset/split/checkpoint/artifact/file-list; chỉ khi user yêu cầu rõ trong lượt hiện tại; integrity/reproducibility/audit chung chung không đủ.
- **Doc layering:** research_plan static, số chốt inline; agent_context/CHANGELOG volatile, file static không referred sang; không thêm process layer (hash/ledger/manifest phụ) khi chưa có failure mode.
- **Protocol:** score max smoke/fire confidence; offset `<0` false alarm; TTD = alarm offset; FA/hour theo negative-window hours; bootstrap event tối thiểu 1,000; operating points 1/camera/day và 1/camera/week.
- **Code:** `dataset.py` Pyro converter + FIgLib index; `train.py` YOLO26x + exact resume; `eval.py` accuracy/cache; `temporal_eval.py` G0/temporal/compare CPU; `modal_app.py` Modal wrappers; `train_rfdetr.py` RF-DETR-L 13f (guard protocol + shim + resume verify).
- **Server ai2 (DGX Spark GB10):** aarch64, CUDA 13.0, unified memory `121.69GiB` = 1 pool RAM+VRAM; đo footprint = delta `free -h` + `nvidia-smi` per-process (per-process vẫn hoạt động, tổng board thì không); `KillUserProcesses=false` (D-Bus verified) → tmux sống qua logout, linger không cần.
- **13f RF-DETR-L (user chốt 2026-07-14):** pin `rfdetr @ git+e877b758c...` trong `.venv` (kéo lightning 2.6.5, transformers 5.13.1, numpy hạ 2.4.6 — ultralytics vẫn OK); protocol khóa 1280/20ep/b4×acc4/seed 20260707/patience 5/ckpt-interval 1, chọn `val/mAP_50_95`; probe GB10: ~18GiB footprint < 25GB budget, ~1h/epoch → ~20h full; kill-resume PASS (epoch/step/EMA/best tiếp nối, không bit-exact); shim `pyro-sdis-yolo-rfdetr/` symlink layout Roboflow, script tự tạo; XL/2XL bị PML 1.0 loại; chi tiết inline research_plan 13f.
- **Removed:** D-Fire, hard-negative, tiling, motion/temporal probes phụ, smoke/resume-gate, CPU preflight, staging cache, checkpoint manifest/hash/snapshot.
- **Training policy mới cho fresh/retrain:** đủ `20` epoch, không early stop; seed `20260707`; không custom augmentation override; mỗi framework giữ native trainer recipe — xem như phần model ecosystem, không ép đồng nhất nội bộ.
- **Fairness theo dataset:** Pyro YOLO26x↔RF-DETR cùng split `train/val`, `1280`, 20ep, seed, không user augmentation override; D-Fire YOLO26x↔RF-DETR cùng split cố định `train/valid/test`, `640`, 20ep, seed, không user augmentation override.
- **Checkpoint contract:** `last.pt`/ `best.pt` dùng inference; `last_resume.pt` full model + EMA + optimizer + scaler + scheduler + train args + updates; atomic save; verify state trước resume; commit Volume mỗi epoch.
- **Modal target:** App `smoke-fire-detection-yolo`; Volume `smoke-fire-step13-volume`; run `yolo26x_pyro_sdis_budget9`; target L4, batch 4, imgsz 1280; checkpoint `/workspace/artifacts/smoke_fire_detection/runs/yolo26x_pyro_sdis_budget9/weights/last_resume.pt`.
- **Last verified:** 2026-07-14 07:57 +0700; container `ta-01KXERQ6TWZND3DTZS7A4W5N8R`; epoch `4/20` đang chạy; checkpoint epoch 3 đủ full-state; không poll tiếp.
- **Next:** sau train dùng `best.pt` chạy Pyro val; cache FIgLib; G0; temporal; chọn candidate; final camera-held-out theo research plan.
- **Training đêm 2026-07-14 (server ai2, KHÔNG giám sát, KillUserProcesses=false → tmux sống qua logout):**
  - CHỈ còn `train_rfdetr_pyrosdis` chạy local — RF-DETR-L/Pyro-SDIS (13f khóa). Output `artifacts/smoke_fire_detection/runs/rfdetr_large_pyro_sdis_gb10`. Resume: `checkpoint_<N>.ckpt` (Lightning, `--resume <path>` của `train_rfdetr.py`).
  - 2 job companion D-Fire (`train_dfire_yolo26x.py` YOLO26x/D-Fire relabeled, `train_dfire_rfdetr.py` RF-DETR-L/D-Fire relabeled) đã **KILL theo quyết định user 2026-07-14 20:00** (đo `nvidia-smi pmon` thấy 3 job chung GPU chia SM% không đều, RF-DETR/Pyro-SDIS — track chính — chỉ được ~25-29%, ~12.8h/epoch; solo lên ~89% SM). Script vẫn giữ trong repo, KHÔNG xóa — user tự chạy trên Kaggle/Colab (repo GitHub `KhanhAIDS/cv-inference-lab` public, clone thẳng không cần token).
  - **2026-07-15 dọn dẹp:** xóa local split `dfire_relabeled_split/` + shim `dfire-relabeled-rfdetr/` — cả hai là output thuần của `build_split()`/`ensure_shim_dataset()` (seed `20260707` + data trên đĩa), script tự tái tạo mỗi lần chạy, không cần transfer. Xóa luôn 2 run dir dở dang không checkpoint (`dfire_yolo26x_relabeled`, `rfdetr_large_dfire_relabeled_gb10`). Thứ duy nhất cần chuyển thủ công sang Kaggle/Colab: dataset D-Fire đã relabel (`datasets/smoke_fire_detection/D-Fire/`, 3.0GB, ngoài git — upload Kaggle Dataset/Drive); lúc hết session tải về `weights/last_resume.pt` (YOLO26x) / `checkpoint_<N>.ckpt` mới nhất (RF-DETR) để resume tiếp.
  - Root `yolo26n.pt` (checkpoint COCO bị Ultralytics tự tải cwd, lọt git qua commit `b9e9d4c`) đã xóa; `.gitignore` thêm `/*.pt` chặn tái diễn.
  - Check nhanh: `tmux ls`; `tail -f /tmp/train_rfdetr_pyrosdis.log`; `nvidia-smi`.
  - **D-Fire notebooks (2026-07-15):** pre-split seed 20260707: train 15,500 / valid 1,721 / test 4,306; root D-Fire/{train,valid,test}; 640/20ep/effective batch32/cosine/no early stop/no custom augmentation override. `kaggle_train_dfire_rfdetr.ipynb`: Kaggle T4×2. `kaggle_train_dfire_yolo26x.ipynb`: Google Colab, dataset giải nén sẵn trong Drive, train/valid/test audit; optimization dùng train, model selection dùng valid, final eval dùng test; checkpoint/output Drive; single GPU.
- **Modal budget9 dừng tự nhiên epoch 18/20 (2026-07-15 11:00):** ephemeral `modal run` không `--detach` → teardown sau khi hàm trả (không phải cancel). Checkpoint tải về `artifacts/smoke_fire_detection/runs/yolo26x_pyro_sdis_budget9/weights/last_resume.pt` (epoch zero-based 17, val mAP50-95 0.492, full state verify OK). User chốt: train tiếp epoch 19-20 trên **Kaggle** (không GB10 — tránh chia GPU).
- **Kaggle resume notebook (2026-07-15):** `kaggle_resume_pyro_sdis_yolo26x.ipynb` — THUẦN, không git clone (user cấm lặp kiểu D-Fire), rebind path (`data`/`project`/`name`/`save_dir`) qua subclass `DetectionTrainer` viết inline (ultralytics `check_resume` chỉ override allowlist cố định, không gồm project/name/save_dir — verify trong `ultralytics/engine/trainer.py:906`). Input cần 2 Kaggle Dataset: (1) `pyro-sdis-yolo.zip` (zip store-only `-0`, KHÔNG dùng tar vì Kaggle chỉ tự giải nén zip) — chưa upload, user tự làm; (2) `last_resume.pt` — chưa upload, user tự làm. Không override hyperparam nào (seed/imgsz/batch/patience/augmentation tự phục hồi từ checkpoint).
- **Next:** user upload 2 Kaggle Dataset (dataset zip + checkpoint) → tạo notebook mới trên Kaggle, dán/copy nội dung `kaggle_resume_pyro_sdis_yolo26x.ipynb`, chỉnh `DATASET_SLUG`/`CHECKPOINT_SLUG` khớp tên Dataset thật → chạy hết cell → tải `last_resume.pt`/`best.pt` mới về sau khi xong epoch 20.
- **Fix 2026-07-15 13:23:** cell-3 detect dataset root cũ (2 path fixed-depth đoán trước) fail thật trên Kaggle dù UI show đúng cây nesting — đổi sang `rglob('dataset.yaml')` tìm đệ quy, in `dataset root:` để debug. Chưa verify lại đã chạy được chưa. GPU T4x2: giữ single-GPU (`device` không override) theo đúng yêu cầu giữ nguyên config — đổi 2-GPU kỹ thuật được nhưng batch=4 chia đôi lợi ích thấp, không tự ý đổi. "Save & Run All (Commit)" nói lần trước có thể sai/lỗi thời — đã rút lại, dùng Persistence dropdown thật (No persistence/Variables and Files/Variables only/Files only).
- **Kaggle thực tế (2026-07-15 13:56, từ run thật của user):** Input path layout MỚI `/kaggle/input/datasets/<username>/<slug>/...` (không phải `/kaggle/input/<slug>`). File live user tự quản: `continue-training.ipynb` (root repo, untracked, hardcode username — user upload/sửa trực tiếp, KHÔNG xóa); bản canonical `tasks/.../kaggle_resume_pyro_sdis_yolo26x.ipynb` đã sync fix + autodetect INPUT_ROOT không hardcode username. Dataset + checkpoint staging PASS trên Kaggle (resume from epoch 18 → 20 verify OK). Blocker đã xử lý: P100 sm_60 bị torch 2.10+cu128 Kaggle loại (min sm_70) → PHẢI chọn T4 x2, đã thêm assert capability >= (7,0) fail-fast cả 2 notebook. DDP 2×T4 không khả thi: trainer class inline notebook không import được trong subprocess DDP của ultralytics.
- **Run Kaggle T4 đang chạy (2026-07-15 14:15):** OOM batch=4 → ultralytics TỰ retry batch=2 (không phải chủ động đổi) — **DEVIATION so config khóa batch=4, phải note khi báo kết quả**. Impact đã phân tích: optimizer step/EMA update mỗi epoch không đổi (accumulate bù nbs=64), LR schedule tái tạo exact (verify khớp results.csv: ep19=0.000218, ep20=0.000119), chỉ khác BN stats (2 ảnh/forward) + grouping → không bit-exact. ~3h24/epoch, tổng ~7-7.5h; checkpoint chỉ ghi cuối epoch → chết giữa epoch mất ~3.4h, giữ tab sống. Đã vá cả 2 notebook: copy checkpoint từ Input chỉ khi target chưa tồn tại (chống rerun ghi đè epoch 19 về 18). Notebook không cần restore scheduler_state (khác train.py): ultralytics tự set last_epoch khi resume, base_lr deterministic — đã verify. Sau khi xong: tải `last_resume.pt`/`best.pt`/`results.csv` từ `/kaggle/working/runs/yolo26x_pyro_sdis_budget9` về artifacts local.
- **Report folder (2026-07-15 15:08, chuẩn bị demo/báo cáo lần 1):** `tasks/smoke_fire_detection/report/` — `README.md` (timeline + bằng chứng + tiling failure + giới hạn), `slide_script.md` (15 slide cho agent khác dựng), `demo_inference.py` (load checkpoint hiện có kể cả dở dang, chạy demo trên Pyro-SDIS val/D-Fire test/FIgLib sample, output `demo_results/` — đã chạy thật, có bằng chứng ảnh + `summary.json`). Script tự fallback CPU khi GPU server dùng chung OOM (đã gặp thật, đã fix, đã verify). Không phục hồi code tiling đã xóa (commit `0916eec`) — bằng chứng tiling dùng số liệu inline sẵn có trong `research_plan.md`.
- **Checkpoint hiện tại dùng cho demo (verify trực tiếp từ metrics.csv/results.csv, 2026-07-15):** RF-DETR-L/Pyro-SDIS 7/20 epoch xong, val mAP50-95=0.4449 (epoch 6)/best EMA 0.4648 (epoch 4); YOLO26x/Pyro-SDIS 18/20 epoch (0-based 17), val mAP50-95=0.49177. Cả hai CHƯA final, số sẽ đổi.
- **D-Fire local final 2026-07-16:** source đúng `datasets/smoke_fire_detection/D-Fire`; raw đủ 17,221 train + 4,306 test, 21,527 cặp. Tách stratified `train=15,500/valid=1,721/test=4,306`, seed 20260707. Sửa tại local: 383 clip cạnh/trục độc lập + drop 18 zero-area; hậu audit 26,584 box, 0 lỗi. `data.yaml`; ZIP upload `datasets/smoke_fire_detection/D-Fire-clean.zip`, 43,055 file, audit archive PASS. Notebook không sanitize/audit/UI: YOLO Colab 1×T4 b4/nbs32; RF-DETR Kaggle 2×T4 b4×2×acc4; effective batch 32. Viewer local `review_dfire_label_fixes.py`: report 522 sample = 344 label lỗi ∪ 192 sample mtime 2026 trước sửa; 1100×720; không upscale; giữ aspect; bbox cùng pixel scale.
- **D-Fire viewer CRUD 2026-07-16:** khóa 1100×720; bbox normalized ↔ đúng raster; scale fit 1 số; cyan=smoke, cam=fire; lỗi rõ vượt biên/cắt hoặc zero-area/xóa; thêm kể cả lồng bbox, chọn + resize 4 góc, không move, xóa, đổi class, undo; label lưu nguyên tử thẳng dataset; `reviewed:true` bền trong report + giữ entry + dấu ✓. Viewer riêng `review_dfire_valid_labels.py`: read-only, ảnh+label mtime trước 2026, ≥1 bbox, toàn label hợp lệ.
