- 2026-08-04 16:17:56 GMT+7
  - Audit nội dung mẫu: DFS, Home, Indoor, FASDD V9; 170 ảnh; raw mapping đều `0=fire`, `1=smoke`; không thấy lỗi nhãn hệ thống.
  - Giữ: `near_field_source_audit.json`, `fasdd_cv_v9_audit.json`, `near_field_visual_audit_sample.json`.
  - Xóa: script audit one-shot, 2 artifact audit stale, contact sheet tạm `/tmp/near_field_visual_audit/`.
  - Sửa: `AGENTS.md`, `agent_context.md`, `CHANGELOG.md`.
  - Rule mới: job GPU phải đo tải và tối đa hóa utilization an toàn.
  - Command: đọc context/rule/script/artifact; tạo và xem contact sheet tạm; parse JSON; `git diff --check`; `git status --short`.
- 2026-08-04 16:23 GMT+7
  - Sửa rule GPU theo user: GPU thuê ngoài tối đa utilization; GPU server công ty báo estimate thời gian/chi phí trước job và ưu tiên không ảnh hưởng workload khác.
- 2026-08-04 16:26:27 GMT+7
  - Lineage audit hẹp: Indoor có 643 nhóm tên lặp, 375 nhóm bắc vendor split; split gốc leak qua augmentation. Home không có nhóm tên lặp nên không có bằng chứng split scene-disjoint.
  - Tạo `artifacts/smoke_fire_detection/near_field_lineage_triage.json`.
  - Tạo `artifacts/smoke_fire_detection/indoor_fire_smoke_augmentation_group_split.json`: group-disjoint 3,500 train / 750 valid / 750 test; raw dataset không đổi.
  - Không dùng GPU; command CPU-only bằng `.venv/bin/python`; không giữ script tạm.
  - Thay đổi trực tiếp: `agent_context.md`, `CHANGELOG.md`.
  - Thay đổi gián tiếp: 2 artifact JSON nêu trên.
- 2026-08-04 16:42 GMT+7
  - Tạo tool giữ lại: `tasks/smoke_fire_detection/near_field_review.py`; tạo queue audit nhãn vòng 1, stratified theo source và `fire_only`/`smoke_only`/`both`/`empty`.
  - Queue hợp lệ: 182 ảnh; DFS 40, Home 40, Indoor 30, Annotated 32, FASDD 40. Tất cả `pending`; không coi là benchmark hay split.
  - Xác nhận `near_field_priority_duplicate_audit.json`: 1 exact duplicate DFS validation ↔ FASDD; phải loại một bản khi canonical export để không contaminate evaluation.
  - Không dùng GPU; command: đọc context/status/artifact/layout, `.venv/bin/python near_field_review.py`, `py_compile`, `git diff --check`.
  - Thay đổi trực tiếp: `tasks/smoke_fire_detection/near_field_review.py`, `agent_context.md`, `CHANGELOG.md`.
  - Thay đổi gián tiếp: `artifacts/smoke_fire_detection/near_field_round1_review_queue.json`.
- 2026-08-04 16:50 GMT+7
  - Mở rộng filename-lineage audit (CPU-only) sang DFS và Annotated: DFS 0 nhóm lặp/cross-split; Annotated 2,214 nhóm lặp, 879 nhóm bắc vendor split → leak augmentation xác nhận.
  - Tạo `artifacts/smoke_fire_detection/annotated_fire_smoke_augmentation_group_split.json`: group-disjoint 7,715 train / 1,653 valid / 1,653 test; raw dataset không đổi. Raw class ratio giữa split có thể lệch, nên dùng metric riêng từng class; manifest không chứng minh scene/video-disjoint.
  - Xóa thư mục contact sheet tạm `/tmp/near_field_round1.NYrtUL/`; không giữ preview/cache/hashing.
  - Không dùng GPU; command: filename lineage traversal và kiểm validation bằng `.venv/bin/python`; `git diff --check`; `git status --short`.
  - Thay đổi trực tiếp: `agent_context.md`, `CHANGELOG.md`.
  - Thay đổi gián tiếp: `artifacts/smoke_fire_detection/near_field_lineage_triage.json`, `artifacts/smoke_fire_detection/annotated_fire_smoke_augmentation_group_split.json`.
- 2026-08-04 17:00 GMT+7
  - Tạo `artifacts/smoke_fire_detection/near_field_round1_review.html`: review offline 182 ảnh, overlay bbox; phím `1` đúng, `2` thiếu nhãn, `3` sai/thừa, `4` không chắc; tải JSON quyết định sau review.
  - Không sửa raw dataset/label; không tạo cache/hash; không dùng GPU.
  - Command: `.venv/bin/python` parse queue+label để xuất HTML; kiểm tra payload/size; `git diff --check`.
  - Thay đổi trực tiếp: `agent_context.md`, `CHANGELOG.md`.
  - Thay đổi gián tiếp: `artifacts/smoke_fire_detection/near_field_round1_review.html`.
- 2026-08-04 17:08 GMT+7
  - Sửa `near_field_round1_review.html`: thêm legend đỏ=fire/xanh=smoke; auto-save trạng thái review trong localStorage của browser. JSON quyết định chỉ thành file khi bấm tải.
  - Không dùng GPU; không sửa raw dataset/label.
  - Thay đổi trực tiếp: `agent_context.md`, `CHANGELOG.md`.
  - Thay đổi gián tiếp: `artifacts/smoke_fire_detection/near_field_round1_review.html`.
- 2026-08-04 18:09 GMT+7
  - Nhập và xác thực `to_be_resolved/near_field_round1_review_decisions.json` đối chiếu 1:1 với queue: 182/182 quyết định hợp lệ, không ID trùng/lạ/thiếu, không trạng thái lạ.
  - Kết quả: 155 pass; 17 missing; 9 wrong_or_extra; 1 uncertain. Lỗi thiếu nhãn lặp theo source: Annotated 9/32 missing (6 fire-only, 2 smoke-only, 1 empty), Indoor 5/30 fire-only missing, DFS 2/40 fire-only missing, FASDD 1/40 fire-only missing; Home 0/40. Bbox rộng/hẹp chưa tự sửa vì không phải lỗi ưu tiên.
  - Tạo `artifacts/smoke_fire_detection/near_field_round1_review_outcome.json`: outcome đã validate, chỉ dùng triage, không tự relabel raw dataset.
  - Sửa `tasks/smoke_fire_detection/near_field_review.py`: thêm mode import/validate `--queue --decisions --output`; phát hiện và sửa lỗi newline trong artifact trước khi hoàn tất validation.
  - Không dùng GPU; command CPU-only: `.venv/bin/python -m py_compile`, `near_field_review.py --queue ... --decisions ...`, `.venv/bin/python -m json.tool`, `jq`, `git diff --check`.
  - Thay đổi trực tiếp: `tasks/smoke_fire_detection/near_field_review.py`, `agent_context.md`, `CHANGELOG.md`.
  - Thay đổi gián tiếp: `artifacts/smoke_fire_detection/near_field_round1_review_outcome.json`.
- 2026-08-04 18:18 GMT+7
  - Điều chỉnh cách diễn đạt triage: lỗi missing nằm trong các nhóm nhãn gốc `fire_only`/`smoke_only`/`empty`, không phải mọi ảnh của Annotated/Indoor. Quyết định đúng: không nạp nguyên trạng toàn bộ hai nguồn; chỉ dùng phần curated/đã qua audit ở seed export, chờ model-assisted audit trước khi mở rộng. Không xóa source nào.
  - Kế hoạch GPU user: Modal chỉ chạy profile ngắn trên 1 hoặc 2 T4 để đo compute/VRAM, tối ưu training sau này trên Kaggle 2×T4 và Colab 1×T4; không train nghiêm túc trên Modal. Chờ user login Modal trước khi chạy.
  - Không dùng GPU.
  - Thay đổi trực tiếp: `agent_context.md`, `CHANGELOG.md`.
- 2026-08-05 09:09:43 GMT+7
  - Modal hardware profile ngắn, không phải train/eval chất lượng: YOLO26x, D-Fire copy tạm 256 ảnh, `imgsz=800`, 1 epoch.
  - 1×T4: batch 8, 68.21s; log train ~13.0/14.9GiB VRAM.
  - 2×T4 DDP: batch tổng 16 (8/GPU), 80.64s; log train ~13.1/14.9GiB mỗi GPU. Overhead khởi tạo DDP làm job ngắn chậm hơn 1 GPU; không suy ra throughput epoch dài.
  - Chốt cấu hình bắt đầu: Colab 1×T4 batch 8; Kaggle 2×T4 batch tổng 16. Report JSON chỉ là profile phần cứng.
  - Command: `.venv/bin/python -m modal run --detach -m tasks.smoke_fire_detection.modal_t4_training_profile::profile_one --batch 8 --imgsz 800 --fraction 0.02 --remote-output artifacts/smoke_fire_detection/modal_t4_profile_1gpu_batch8.json`; `.venv/bin/python -m modal run --detach -m tasks.smoke_fire_detection.modal_t4_training_profile::profile_two --batch 16 --imgsz 800 --fraction 0.02 --remote-output artifacts/smoke_fire_detection/modal_t4_profile_2gpu_batch16.json`; `modal app logs`; `modal volume get`; JSON validation.
  - Thay đổi trực tiếp: `agent_context.md`, `CHANGELOG.md`; tạo rồi xóa `tasks/smoke_fire_detection/modal_t4_training_profile.py`.
  - Thay đổi gián tiếp: tạo `artifacts/smoke_fire_detection/modal_t4_profile_1gpu_batch8.json`, `artifacts/smoke_fire_detection/modal_t4_profile_2gpu_batch16.json`; tạo cache tạm trong container và xóa cùng job; xóa bản report trung chuyển trên Modal Volume.
- 2026-08-05 09:45 GMT+7
  - Xác nhận teacher hiện có phù hợp: `rfdetr_large_dfire` 2 lớp, checkpoint local; checkpoint ngoài Pyronear/SmokeyNet chỉ smoke hoặc có leakage-caveat, không dùng làm teacher audit near-field hai lớp.
  - Tạo `tasks/smoke_fire_detection/near_field_model_audit.py`: chạy teacher trên nguồn near-field, chọn bất đồng strong `possible_missing_label`/`possible_wrong_or_extra_label`, xuất JSON review; prediction chỉ là triage, không tự relabel.
  - Không chạy inference GPU: Modal chỉ được user duyệt để profile ngắn; GPU server nội bộ thiếu đơn giá/estimate trước job.
  - Command: đọc `agent_context.md`, registry asset/checkpoint, evaluator; `.venv/bin/python -m py_compile tasks/smoke_fire_detection/near_field_model_audit.py`; `git diff --check`.
  - Thay đổi trực tiếp: `tasks/smoke_fire_detection/near_field_model_audit.py`, `agent_context.md`, `CHANGELOG.md`.
  - Thay đổi gián tiếp: không có.
- 2026-08-05 10:31:16 GMT+7
  - Sửa workflow model-assisted review theo yêu cầu: chấm bbox nhãn gốc; bbox teacher chỉ là gợi ý nét đứt kèm confidence.
  - HTML mới cho phép đồng thời đánh dấu `missing_or_extra`, `wrong_class`, `geometry`; `pass` và `uncertain` tách riêng. Không gộp lỗi hình học bbox với lỗi nhãn.
  - Bổ sung validation JSON: chặn review ID lạ/trùng/thiếu, status sai và issue/status không khớp; không tự sửa raw dataset/label.
  - Command: đọc context/tool/eval/HTML; `.venv/bin/python -m py_compile tasks/smoke_fire_detection/near_field_model_audit.py`; `--help`; kiểm thử HTML in-memory rồi xóa `/tmp/near_field_model_audit_review_test.html`; `git diff --check`.
  - Thay đổi trực tiếp: `tasks/smoke_fire_detection/near_field_model_audit.py`, `agent_context.md`, `CHANGELOG.md`.
  - Thay đổi gián tiếp: không có.
- 2026-08-05 10:53:36 GMT+7
  - Tạo notebook Kaggle tự chứa toàn bộ mã model-assisted audit: `tasks/smoke_fire_detection/near_field_model_audit_kaggle.ipynb`. Notebook chỉ cần Kaggle Input cho 5 source near-field đã giải nén và checkpoint RF-DETR D-Fire; tự kiểm GPU, cài `rfdetr==1.8.3`, chạy tối đa 2 worker theo GPU, theo dõi utilization/VRAM, gộp queue top-100/source và xuất ZIP offline review.
  - Sửa `AGENTS.md`: bắt buộc kiểm kê/dọn trước handoff; `to_be_resolved/` temp-only; notebook cloud tự chứa source code, Input chỉ mang binary data/checkpoint lớn.
  - Sửa `agent_context.md`: lưu layout Kaggle audit và rule cleanup mới.
  - Command: đọc context/rule/source audit/layout/dung lượng; đối chiếu archive với dataset giải nén; `cmp` checkpoint selected; tạo notebook bằng `.venv/bin/python`; `.venv/bin/python -m json.tool` validate notebook.
  - Thay đổi trực tiếp: `AGENTS.md`, `agent_context.md`, `CHANGELOG.md`, `tasks/smoke_fire_detection/near_field_model_audit_kaggle.ipynb`.
  - Thay đổi gián tiếp: không có.
- 2026-08-05 10:56:03 GMT+7
  - Xóa hoàn toàn `to_be_resolved/` (~16.5GB): 8 ZIP dataset đã giải nén/audit, JSON quyết định review đã import/validate, 3 checkpoint Pyro-SDIS thử nghiệm và metric/config của run đã chốt. Checkpoint `checkpoint_best_total.pth` selected đã được đối chiếu byte-for-byte với bản trong `artifacts/` trước khi xóa.
  - Xóa cache `tasks/smoke_fire_detection/__pycache__/`; xóa notebook Pyro fine-tune đã hoàn tất và script one-shot `firesense_cache.py`, `firesense_gate.py`, `smokeynet_cache.py`.
  - Không xóa Pyro-SDIS/PYRONEAR và historical checkpoint/cache vì đây là nhóm lớn, không còn workflow hiện tại nhưng có thể cần đối chiếu; chờ user duyệt riêng.
  - Command: `rm -f` file temporary đã liệt kê, `rmdir to_be_resolved`, `rm -rf __pycache__`, `rm -f` 4 file one-shot; parse JSON notebook; compile worker nhúng trong notebook; `git diff --check`.
  - Thay đổi trực tiếp: xóa `to_be_resolved/` và toàn bộ nội dung, `tasks/smoke_fire_detection/__pycache__/`, `tasks/smoke_fire_detection/kaggle_train_pyronear2025_rfdetr_finetune.ipynb`, `tasks/smoke_fire_detection/firesense_cache.py`, `tasks/smoke_fire_detection/firesense_gate.py`, `tasks/smoke_fire_detection/smokeynet_cache.py`; sửa `agent_context.md`, `CHANGELOG.md`.
  - Thay đổi gián tiếp: không có.

2026-08-05 11:30:46 GMT+7
- Sửa workflow audit: DeepQuest vào nhánh pseudo-label có duyệt; FireAndSmoke v1 vào review đủ 100 ảnh; prediction chỉ thành nhãn dataset dẫn xuất khi reviewer chọn `adopt_teacher`; raw dataset không đổi.
- Thay đổi trực tiếp: AGENTS.md; agent_context.md; CHANGELOG.md; tasks/smoke_fire_detection/near_field_review.py; tasks/smoke_fire_detection/near_field_model_audit.py; tasks/smoke_fire_detection/near_field_model_audit_kaggle.ipynb.
- Thay đổi gián tiếp: không có file đầu ra workflow mới; cache __pycache__ sinh khi py_compile đã bị xóa.
- Command: `.venv/bin/python -m py_compile tasks/smoke_fire_detection/near_field_review.py tasks/smoke_fire_detection/near_field_model_audit.py`; notebook JSON/code-cell compile; source-record count DeepQuest/FireAndSmoke v1.
- Dọn artifact benchmark/checkpoint/cache cũ bị safety review từ chối; chưa có file artifact nào bị xóa trong lượt này.

- 2026-08-07 16:39:50 GMT+7
  - Nén dataset D-Fire phục vụ user tải về.
  - Command: `uname -a`; `zip -r ../../D-Fire.zip D-Fire` (Cwd: `datasets/smoke_fire_detection`); `ls -lh D-Fire.zip`.
  - Thay đổi trực tiếp: `CHANGELOG.md`.
  - Thay đổi gián tiếp: `D-Fire.zip` (2.9GB).


- 2026-08-12 18:53:42 GMT+7
  - Gộp 2 bundle review Kaggle audit v2 ở root (`review_error_0000.zip` 29,378 error + `review_pseudo_0000.zip` 1,047 pseudo) thành 1 workspace annotate duy nhất; 0 duplicate task_id, 0 overlap giữa 2 bundle, 30,425/30,425 ảnh resolve được về `datasets/smoke_fire_detection/`.
  - Tạo công cụ đánh nhãn/sửa/xóa bbox thật thay reviewer HTML tĩnh: `tasks/smoke_fire_detection/label_audit_tool.py` (stdlib Python, không cần pip install, Windows+Linux) + `tasks/smoke_fire_detection/label_audit_tool.html` (canvas editor: vẽ/di chuyển/resize/xóa bbox, đổi class, undo/redo, zoom/pan, note, autosave draft).
  - Subcommand: `import` (gộp bundle), `serve` (HTTP server đọc ảnh trực tiếp từ `datasets/`, không copy 2.9GB), `status` (tiến độ per source/issue), `export` (xuất ground truth + manifest).
  - Bbox nổi bật hơn: casing 2 lớp đảo theo luminance, palette ngoài dải màu tự nhiên của khói/lửa (smoke lime `#C6FF00`, fire magenta `#FF2BD6`, ignore trắng), corner bracket, chip chữ class, marching-ants, nhấp nháy, spotlight tối 62% vùng ngoài bbox, grayscale, boost sáng/contrast, ẩn bbox, 3 palette, độ dày viền điều chỉnh được.
  - Đánh dấu đã audit: `decisions.jsonl` append-only là nguồn sự thật (key = đường dẫn ảnh tương đối, bền qua mọi model run, khác `review_id` sinh theo run). Ảnh `confirmed`/`excluded` bị loại khỏi queue mặc định; `import` lần sau gắn `carried_over_status` — đã verify carry-forward. Export ghi marker ngay trong tên file `<stem>.audited.txt` (+ symlink `<stem>.audited.<ext>` với `--link-images`); raw dataset không bị rename/sửa.
  - Thêm class canonical `ignore=2` cho vùng khói/lửa mơ hồ (không tính TP/FN/FP), `export --drop-ignore` để bỏ nếu không dùng.
  - Nghiên cứu quy ước đánh nhãn vật thể vô định hình và viết `tasks/smoke_fire_detection/docs/label_convention_smoke_fire.md`: 11 rule R1-R11 dựa trên rule gốc FASDD (nguồn lớn nhất trong pool), PYRONEAR-2025 (chủ động bỏ mAP vì contour khói chủ quan, 5 annotator/ảnh, batch 150 ảnh), FIgLib SAM2-relabel, COCO `iscrowd` / Open Images `IsGroupOf`; kèm chiến lược audit (85 giờ nếu audit hết 30,425 ảnh → cắt `possible_geometry_mismatch` 9,504 ảnh vì metric lab là event recall chứ không phải hình học, còn 9,117 ảnh nhóm giá trị cao + pseudo; audit eval-split trước train-split; stratified 150-200 ảnh/source×issue để đo tỉ lệ lỗi có CI; pilot 30 ảnh; self-agreement 5%).
  - Verify bằng chromium headless (`--headless --screenshot`, 1600×950 / 1440×900 / 1366×768): 0 JS error, toàn bộ toggle render đúng, layout bar 3 row vừa khít ở 1366×768. Sửa layout 2 lần theo kết quả screenshot.
  - Command: `unzip -l`/`unzip -q` peek bundle; `.venv/bin/python -m py_compile tasks/smoke_fire_detection/label_audit_tool.py`; `.venv/bin/python tasks/smoke_fire_detection/label_audit_tool.py import --bundle review_error_0000.zip --bundle review_pseudo_0000.zip`; `... serve --port 8760 --host 127.0.0.1`; `... status`; `... export --link-images`; `curl` test `/api/bootstrap`, `/api/tasks`, `/api/progress`, `/image`, `POST /api/decision` (gồm case status sai và task_id lạ); `pdftotext -layout` để đọc rule FASDD; `TZ='Asia/Ho_Chi_Minh' date`.
  - Thay đổi trực tiếp: tạo `tasks/smoke_fire_detection/label_audit_tool.py`, `tasks/smoke_fire_detection/label_audit_tool.html`, `tasks/smoke_fire_detection/docs/label_convention_smoke_fire.md`; sửa `agent_context.md`, `CHANGELOG.md`.
  - Thay đổi gián tiếp: tạo `artifacts/smoke_fire_detection/near_field_label_audit/tasks.jsonl.gz` (11.4MB) + `workspace.json`; tạo rồi XÓA `artifacts/smoke_fire_detection/near_field_label_audit/decisions.jsonl` (1 decision test giả), `datasets/smoke_fire_detection/near_field_curated_ground_truth/` (export test), `tasks/smoke_fire_detection/__pycache__/`, 6 screenshot verify `/home/tts01/audit_ui*.png`, file peek tạm trong scratchpad.
  - Chưa làm, cần user quyết: xóa 2 zip root (2.9GB, đã hết vai trò vì queue nằm trong workspace và ảnh đọc từ `datasets/`); dọn `write_review_html`/`validate_decisions` trong `near_field_model_audit.py` và HTML reviewer trong `near_field_review.py` nay là dead path.

- 2026-08-12 19:35:32 GMT+7
  - Dọn codebase mạnh tay theo yêu cầu user. **artifacts 16G → 953M**, tổng giải phóng ~18GB.
  - Xóa: `artifacts/smoke_fire_detection/near_field_audit_sources.zip` (15GB, bản copy nguyên `datasets/smoke_fire_detection/` để upload Kaggle, đã hết vai trò); `review_error_0000.zip` + `review_pseudo_0000.zip` ở root (2.9GB, queue đã nằm trong workspace, ảnh đọc từ `datasets/`); 3 detector cache của experiment đã đóng + sidecar meta/errors (`figlib_detector_cache_pyronear_yolo11s_rapid_raccoon_dev`, `figlib_detector_cache_rfdetr_large_dfire_pyronear2025_ft_dev`, `figlib_detector_cache_smokeynet_reference_dev_subsample30`, ~74MB, mọi số đã chốt trong `model_benchmark_2x2.md` + gate/compare JSON); `near_field_round1_review.html`; `near_field_round1_review_queue.json`; `near_field_visual_audit_sample.json`; `tasks/smoke_fire_detection/near_field_model_audit.py`; `tasks/smoke_fire_detection/near_field_review.py` (cả 2 bị `label_audit_tool.py` thay hoàn toàn).
  - Giữ lại theo chỉ đạo user: toàn bộ checkpoint (`runs/*/weights/*.pt`, `*.pth`, `pyronear_*.pt`, `smokeynet_model.onnx`), config/metrics training (`training_config.json`, `args.yaml`, `results.csv`, `metrics.csv`, `test_metrics.json`, `val_metrics.json`, `training_protocol.json`), profile GPU/VRAM (6 `profile_*.json` + 2 `modal_t4_profile_*.json`), code xương sống (`dataset.py`, `eval.py`, `temporal_eval.py`, `modal_app.py`, 2 test), cache winner + FIRESENSE, `figlib_index.jsonl`/`figlib_split_manifest.json`/`figlib_dev_winner_lock.json`, mọi audit/lineage/group-split JSON, `human_review_labels.json`, toàn bộ `datasets/`.
  - Verify an toàn trước khi xóa zip: load lại `tasks.jsonl.gz` đủ 30,425 task / 7 source, spot-check ảnh resolve về `datasets/` không thiếu.
  - Sửa thiết kế `ignore` sau phản biện của user: `ignore` **không còn** được ghi vào file nhãn train (Ultralytics YOLO và RF-DETR không hỗ trợ ignore-region/loss-masking → class 2 sẽ bị học thành object thứ 3). Bỏ cờ `--drop-ignore`; export luôn tách `<stem>.audited.ignore.txt` (class id 2) và set `eval_safe=false` cho ảnh đó.
  - Thêm `label_source` cho mỗi quyết định: `dataset` / `teacher` / `human`, tự suy ra trong UI (mọi lần sửa tay → `human`; `A` thay thế → `teacher`; `R` → `dataset`). Manifest export đếm theo 3 loại + `eval_safe_count`. Rule ghi trong doc: eval split chỉ dùng `dataset`+`human`, `teacher` train-only vì teacher chính là `rfdetr_large_dfire` (winner) → circular evaluation.
  - Thêm phím/nút `W` = lấy bbox teacher + chốt GROUND TRUTH + sang ảnh sau trong 1 phím (đáp ứng yêu cầu "nút cho bbox mô hình dự đoán thành ground truth luôn").
  - Thêm checkbox `nuisance` (flag, không phải bbox) cho nến/bếp/TV/đèn/hơi nước/khí thải; `status` command in thêm breakdown `label_source` và `flags`.
  - Viết lại `tasks/smoke_fire_detection/docs/label_convention_smoke_fire.md`: thêm cây quyết định 3 nhánh, mở R1-R11 thành R1-R13 với ngưỡng đo được (R3 tách chỉ khi khác **gốc phát** — sửa lỗi rule FASDD "khác màu → khác object" sẽ tách sai 1 cột khói đổi màu theo độ cao; R3b trần ~6 bbox/ảnh; R8 fill ratio ≥0.35; R9 cấm bbox ≥90% ảnh khi khói thật <60%; R12 cấm bbox lồng cùng class), và thêm **20 edge case E1-E20** có câu trả lời cụ thể: lửa lẫn khói, vùng chuyển tiếp nửa lửa nửa khói, khói/lửa sau song sắt-hàng cây, khói mờ do phân giải kém (test gốc + hướng), lửa hình chữ L (fill ratio), khói bị lửa chiếu đỏ, lửa qua kính vs phản chiếu, lửa trên TV/poster, nến-bếp-đèn, mặt trời-đèn đường, hơi nước-khí thải ống khói, khói đen+trắng cùng cột, crop mép ảnh, bbox lồng/trùng, ảnh ban đêm, đường lửa rời rạc, nhóm ảnh augment, khói phủ gần hết ảnh, tàn lửa-than hồng, lửa nhỏ là nguồn cột khói lớn.
  - Verify: `py_compile`; curl test POST `label_source` hợp lệ/không hợp lệ; export end-to-end xác nhận file nhãn chính KHÔNG chứa class 2, file `.audited.ignore.txt` chứa class 2, `label_source`/`eval_safe`/`flag_counts` đúng; chromium headless 1440×900 xác nhận 0 JS error với nút `W` + checkbox nuisance + panel quy ước mới, sửa thêm 1 lỗi layout (chip header xuống dòng đè vào canvas).
  - Command: `du -sh`/`find`/`df -h` kiểm kê; `unzip -l` xác định nội dung `near_field_audit_sources.zip`; `grep` đối chiếu version notebook và cross-reference code; `rm -f`/`rm -rf` các mục đã liệt kê; `.venv/bin/python tasks/smoke_fire_detection/label_audit_tool.py {import,serve,status,export --link-images}`; `curl` các endpoint; `chromium --headless --screenshot`; `TZ='Asia/Ho_Chi_Minh' date`.
  - Thay đổi trực tiếp: sửa `tasks/smoke_fire_detection/label_audit_tool.py`, `tasks/smoke_fire_detection/label_audit_tool.html`, `tasks/smoke_fire_detection/docs/label_convention_smoke_fire.md`, `agent_context.md`, `CHANGELOG.md`; xóa các file đã liệt kê ở trên.
  - Thay đổi gián tiếp: tạo rồi XÓA `decisions.jsonl` (3 decision test) và `datasets/smoke_fire_detection/near_field_curated_ground_truth/` (export test), `__pycache__`, screenshot verify `/home/tts01/audit_*.png`, bản patch tạm của HTML trong scratchpad. `tasks.jsonl.gz` giữ nguyên bản import 30,425 task, trạng thái sạch 30,425 pending.
  - Chưa làm, cần user quyết: (1) `near_field_model_audit_kaggle.ipynb` trong repo là **v1**, không phải v2 đã sinh 2 bundle — nên export notebook v2 từ Kaggle về repo rồi xóa v1; (2) `datasets/smoke_fire_detection/PYRONEAR-2025` raw 9.2GB có thể xóa vì `PYRONEAR-2025-clean` đã dẫn xuất xong và lever cho kết quả âm đã đóng — chưa xóa vì user nói giữ dataset; (3) run-dir rác trên Modal volume vẫn còn, cần login Modal; (4) `docs/Annotation_practices.md` (root, 21KB) chưa đọc theo rule — có thể xung đột với quy ước vừa viết.

- 2026-08-12 20:27:22 GMT+7
  - **Datasets**: phát hiện 2 root song song vô lý (`datasets/near_field_fire_detection/FIRESENSE` và `datasets/smoke_fire_detection/*`) — gộp FIRESENSE vào `datasets/smoke_fire_detection/FIRESENSE`, xóa root rỗng `near_field_fire_detection`. Xóa `datasets/smoke_fire_detection/PYRONEAR-2025` raw (9.2GB, đã có `PYRONEAR-2025-clean` dẫn xuất, lever đã đóng kết quả âm). Xóa `tasks/smoke_fire_detection/near_field_model_audit_kaggle.ipynb` (v1, theo yêu cầu user — mất thì viết lại). `datasets/` giữ nguyên mọi dataset còn lại theo yêu cầu user (dùng cho train cháy rừng sau này).
  - **Artifacts reorganize**: `artifacts/smoke_fire_detection/` từ ~90 file JSON/checkpoint nằm chung 1 tầng → chia `checkpoints/<candidate>/` + `checkpoints/external/`, `benchmark/{gate,compare,temporal,diagnostic,profile}/`, `dataset_audit/`, `figlib/`, `detector_cache/`, `label_audit/` (đổi tên từ `near_field_label_audit`). Xóa sidecar `.errors.json` không dùng trong detector_cache.
  - Sửa mọi path cứng theo cấu trúc mới: `tasks/results_dashboard/backend/models.py`; `tasks/smoke_fire_detection/modal_app.py` (toàn bộ default arg path); `tasks/smoke_fire_detection/label_audit_tool.py` (default workspace). `tasks/results_dashboard/exporter/export.py` đổi `glob`→`rglob` + thêm helper `find_artifact()` cho 2 path cố định còn lại — tìm theo tên bất kể nằm thư mục con nào, chịu được tổ chức lại tiếp mà không cần sửa code.
  - **Viết lại `label_audit_tool.py`/`.html` theo phản biện của user** (2 điểm chính):
    1. **Bỏ hẳn class `ignore`.** User đã relabel D-Fire thật, xác nhận ambiguous case hiếm và data đủ lớn để không cần layer này. Tool quay về đúng 2 class `smoke=0`/`fire=1`; bỏ `eval_safe`, `.audited.ignore.txt`, checkbox nuisance, status `uncertain` — toàn bộ layer thêm ở lượt trước đã revert.
    2. **UI tối giản** — chỉ giữ chức năng cực kỳ cơ bản theo đúng yêu cầu: xem/xác nhận nhãn gốc, xem/xác nhận bbox model, vẽ/chọn/sửa kích thước/di chuyển/xóa bbox, đổi class, undo, loại ảnh. Bỏ hết theme màu mè (marching-ants, nhấp nháy, spotlight, grayscale, đổi palette, corner-bracket, chip nổi bật) của bản trước.
  - **Sửa hit-test bbox lồng nhau** theo yêu cầu cụ thể: click trong vùng chồng lấn chọn đúng bbox NHỎ NHẤT chứa điểm (không phải bbox vẽ sau/trên cùng); kéo bbox đang chọn chỉ di chuyển đúng bbox đó, bbox to trùm lên nó giữ nguyên tuyệt đối; thêm `Shift`+kéo để ép vẽ bbox mới ngay trong vùng đang có bbox được chọn (trường hợp cần thêm bbox nhỏ mới bên trong mà không vô tình di chuyển bbox đang chọn).
  - Thêm dropdown rules 2 khối trong panel phải (`<details>`): "Cách dùng + phím tắt" và "Luật vẽ bbox + edge case" — chép nguyên luật R1-R13 và toàn bộ 20 edge case vào ngay trong app, không phải nhớ hay mở file riêng.
  - Thêm **resume**: app lưu `task_id` đang xem + bộ lọc đã chọn vào `localStorage`; mở lại app tự nhảy về đúng ảnh đang làm dở.
  - Thêm 2 subcommand mới: `pack` (đóng gói ảnh + tool + launcher Windows/Linux thành 1 thư mục/zip tự chứa để label offline, không cần server/mạng) và `merge` (gộp `decisions.jsonl` từ pack về workspace chính, chặn task_id lạ, bỏ qua bản trùng y hệt).
  - Build pack thật, đầy đủ 30,425 ảnh (không filter): `/home/tts01/label_audit_offline.zip` (2.7GB) — chứa tool bản mới nhất, launcher `START_WINDOWS.bat`/`START_LINUX.sh`, README hướng dẫn mang về và merge lại.
  - Cập nhật `tasks/smoke_fire_detection/docs/label_convention_smoke_fire.md`: bỏ mọi nhắc `ignore`/nuisance-flag để khớp tool đã đơn giản hóa; thêm bảng tổng hợp case→quyết định (mục 4bis) cho các case lặp lại; sửa mục 6-7 phản ánh app mới (tool tối giản, offline pack).
  - Verify: `py_compile`; `curl` POST decision hợp lệ/không hợp lệ; export end-to-end xác nhận file nhãn chỉ chứa class 0/1; **chromium headless dispatch pointer event thật** (pointerdown/pointermove/pointerup, không phải đọc code) để verify nested-bbox: vẽ box to → vẽ box nhỏ trong đó → click chọn đúng box nhỏ → kéo box nhỏ (box to không đổi) → chọn box to, kéo (box nhỏ không đổi) → Shift+kéo trong vùng box đang chọn tạo box thứ 3 — toàn bộ PASS; pack chạy standalone từ thư mục pack (không cần repo root) test đủ serve/POST/merge/export.
  - Command: `mv`/`rmdir`/`rm -rf` tổ chức lại datasets và artifacts; `grep` xác nhận sạch path cũ; `.venv/bin/python tasks/smoke_fire_detection/label_audit_tool.py {import,serve,status,export,pack,merge}`; `curl` các endpoint; `chromium --headless` dispatch pointer event qua script nhúng tạm; `zip`/`shutil.make_archive` qua lệnh `pack --zip`; `TZ='Asia/Ho_Chi_Minh' date`.
  - Thay đổi trực tiếp: viết lại toàn bộ `tasks/smoke_fire_detection/label_audit_tool.py`, `label_audit_tool.html`; sửa `tasks/smoke_fire_detection/docs/label_convention_smoke_fire.md`, `tasks/results_dashboard/exporter/export.py`, `tasks/results_dashboard/backend/models.py`, `tasks/smoke_fire_detection/modal_app.py`, `agent_context.md`, `CHANGELOG.md`; xóa `tasks/smoke_fire_detection/near_field_model_audit_kaggle.ipynb`.
  - Thay đổi gián tiếp: di chuyển toàn bộ nội dung `artifacts/smoke_fire_detection/` vào cấu trúc thư mục con mới (không mất file, chỉ đổi đường dẫn); gộp `datasets/near_field_fire_detection/FIRESENSE` → `datasets/smoke_fire_detection/FIRESENSE`; xóa `datasets/smoke_fire_detection/PYRONEAR-2025` raw (9.2GB); tạo `/home/tts01/label_audit_offline/` + `.zip` (2.9GB + 2.7GB, ngoài repo); tạo rồi xóa toàn bộ output test (decisions.jsonl test, export test, pack test, screenshot verify) trong quá trình kiểm thử.
  - Chưa làm, cần user quyết: (1) chưa verify `modal_app.py`/dashboard chạy thật với path mới — cần Modal/GPU, ngoài khả năng phiên này, chỉ verify bằng grep; (2) `docs/Annotation_practices.md` (root) user đã cho phép đọc và xác nhận không phải quy ước bắt buộc của user — đã đọc, ghi nhận là tài liệu tham khảo chung về annotation quality, không mâu thuẫn với quy ước đã chốt; (3) pack offline hiện tại là FULL 30,425 ảnh (2.7GB) — nếu chỉ cần label một phần, dùng `--issue`/`--source`/`--limit` khi pack để nhỏ hơn.
