- 2026-07-17 14:34 +07:00
  - Mục tiêu: (1) resolve `to_be_resolved/yolo26x_d-fire/` — user cảnh báo mAP50-95 epoch 20 = `0.176` trong `results.csv` thấp bất thường so RF-DETR-L/D-Fire (0.496) và D-Fire yolo26n gốc (0.404), yêu cầu chạy eval thật + điều tra trước khi kết luận/merge; (2) sửa lệch doc research_plan.md mục 8 (RF-DETR-L/D-Fire ghi 640px/GB10 nhưng thực tế 800px/Kaggle); (3) đánh giá độ sâu track near-field (D-Fire-style) so với wildfire (FIgLib/RQ1) và viết đề xuất bổ sung (proposal, chưa thực thi).

  - **1. Điều tra + eval thật YOLO26x/D-Fire (`to_be_resolved/yolo26x_d-fire/`):**
    - Đọc `training_protocol.json`/`args.yaml`/`results.csv`/notebook `colab_train_dfire_yolo26x.ipynb` (Colab 1×T4, không phải GB10 như kế hoạch cũ — script GB10 `train_dfire_yolo26x.py` đã xóa 2026-07-16, user chuyển Colab). Dataset D-Fire train-ready relabel chốt, stage "clean" clip-edges-drop-degenerate (47/26.5k box clip, 0 drop), split cố định gốc train/valid/test, `800px`, `20ep`, batch 8/nbs 32, seed `20260707`, AMP, ultralytics `8.4.90`.
    - Chạy `eval.py accuracy` chính thức trên D-Fire `test` split (conf 0.25/iou 0.6/imgsz 800, cùng protocol E0/RF-DETR) trên `weights/best.pt`: **mAP50-95 = `0.0805`, mAP50 = `0.161`, recall = `0.202`** (smoke: mAP50 `0.050`/recall `0.074` — gần mù; fire: mAP50 `0.271`). Số latency trong report loại bỏ khỏi kết luận — GPU `ai2` bị process khác share ~93% lúc đo (không đáng tin cho benchmark speed). Số thấp XÁC NHẬN LÀ THẬT, không phải artifact của curve/checkpoint sai.
    - **Điều tra nguyên nhân:** đối chứng cùng dataset — E0 yolo26n (640px, `warmup_epochs=3.0`) epoch 1 val mAP50-95 đã `0.144`, epoch 20 = `0.36`; RF-DETR-L cùng data/800px/20ep epoch 0 đã EMA `0.417`, best `0.496`. Run này epoch 1 val P=`0.007`/R=`0.53` (FP tràn ngay đầu). **Nghi phạm chính:** `args.yaml` xác nhận override `warmup_epochs=0.0` + `cos_lr=true` — lệch native recipe Ultralytics, trái tiền lệ cả 2 run YOLO thành công khác (đều `warmup_epochs=3.0`, `cos_lr=false`). Loại trừ: dataset variant/label (RF-DETR đạt 0.496 cùng data), batch/nbs scaling (nbs=32 giữ weight-decay scale chuẩn), resolution 800 (RF-DETR cùng 800 OK), augmentation (đúng framework default).
    - **Quyết định: KHÔNG merge vào `artifacts/smoke_fire_detection/runs/`.** Giữ `to_be_resolved/yolo26x_d-fire/` nguyên trạng làm failed-run reference (không phải candidate model-family hợp lệ để so RF-DETR). Đề xuất rerun đúng native recipe (bỏ override warmup/cos_lr) — CHƯA tự chạy, chờ user duyệt. Chi tiết đầy đủ + số liệu ghi inline tại `research_plan.md` mục 8 (đoạn YOLO26x/D-Fire).
  - **2. Sửa lệch doc `research_plan.md` mục 8:** đoạn RF-DETR-L/D-Fire (kế hoạch cũ: GB10 local/640px/output `rfdetr_large_dfire_relabeled_gb10`) cập nhật khớp thực tế đã DONE (xác nhận qua `training_config.json`/`resume_protocol.json`/CHANGELOG 2026-07-17 09:45): Kaggle 2×T4/800px/output `rfdetr_large_dfire_kaggle`, best EMA epoch 11 = `0.49614`, best regular epoch 18 = `0.49324`. `agent_context.md` dòng fairness D-Fire: `640`→`800`, thêm cờ lệch recipe YOLO26x.
  - **3. Đánh giá + đề xuất track near-field (PROPOSAL, chưa thực thi):** xác nhận quan sát của user — toàn bộ gate/experiment/toolbox hiện tại (G0-G4, E0-E9, A0-A13/L0-L4, hard-negative menu) neo theo FIgLib/wildfire; D-Fire chỉ đóng vai control/E0/zero-shot source cho G0, chưa có RQ/gate/protocol riêng cho domain near-field; envelope S2 (research_plan.md mục 2.1) định nghĩa nhưng chưa build.
    - 3 vòng web research song song (agent, không thực thi bởi main-loop): (a) chuẩn/thực tiễn VSD — FM 3232 (alarm ≤30s bắt buộc, false-stimuli test welding/sunlight/lamp), ISO 7240-29:2024 (Type A/B/AB, RTV, không có false-alarm test bắt buộc), UL 268B, NFPA 72 §17.7; vendor Fike/Bosch AVIOTEC/Araani đều edge on-camera 15-25fps; object size min ~1.1-1.6% bề rộng ảnh (KHÔNG phải tiny-object regime mặc định); (b) dataset video near-field — ONFIRE 2023 (MIVIA, duy nhất có gán fire-onset time, email request), FIRESENSE (Zenodo CC BY 4.0, tải mở), LFDN (4,154 ảnh hard-negative chủ đích), MmodalFire (CC BY 4.0, lab-controlled onset), MIVIA Smoke (>35h negative outdoor); gap xác nhận: không có dataset public indoor quy mô lớn có cả negative-hour + onset annotation; (c) FA-cost/privacy/edge — BS 5839-1 FA budget ~1/50-100 detector/NĂM (chênh wildfire 1/camera/NGÀY ~3-4 bậc độ lớn, vì wildfire luôn có human reviewer hấp thụ FP còn near-field auto-dispatch bị phạt trực tiếp); GDPR/EDPB ép kiến trúc edge/no-footage-retention (wildfire tower không có ràng buộc này); edge kinh tế hơn cloud khi >20-50 camera.
    - Viết proposal vào `research_plan.md` mục 10 (RQ5, envelope S2 làm rõ bằng số, gate NG0-NG4 song song G0-G4, experiment NE1-NE5, dataset survey near-field, 4 điểm chờ user quyết định) và `research_toolbox.md` (menu near-field theo failure mode riêng — nuisance category khác cloud/fog/glare, cảnh báo KHÔNG suy rộng kết luận "đóng motion-as-input" của RQ1 sang near-field vì cadence khác 60s→liên tục). Mọi số [vendor]/[secondary] gắn nhãn rõ, chưa khóa protocol — chỉ [standard]/[academic] mới dùng làm số cứng.
    - Tất cả nội dung mục 10 là ĐỀ XUẤT — không tải dataset, không train, không đổi priority RQ1 cho tới khi user duyệt từng bước (mục 10.6).

  - Thay đổi trực tiếp:
    - Sửa: `tasks/smoke_fire_detection/docs/research_plan.md` (mục 8 đoạn RF-DETR-L/D-Fire + YOLO26x/D-Fire; thêm mục 10 proposal near-field), `tasks/smoke_fire_detection/docs/research_toolbox.md` (thêm menu near-field), `agent_context.md`, `CHANGELOG.md`.
  - Thay đổi gián tiếp: `to_be_resolved/yolo26x_d-fire/eval_report_test.json` + `to_be_resolved/yolo26x_d-fire/test_accuracy/` (predictions.json + plot) — sinh bởi `eval.py accuracy`, giữ lại làm bằng chứng số liệu thật cho phần "failed-run reference"; `tasks/smoke_fire_detection/__pycache__/` sinh khi chạy `eval.py` (đã dọn).
  - Command chính: đọc `training_protocol.json`/`args.yaml`/`results.csv`/notebook JSON (`json.load` qua `.venv/bin/python`); `nvidia-smi --query-gpu/--query-compute-apps` (xác nhận GPU bị share ~93%); `.venv/bin/python tasks/smoke_fire_detection/eval.py accuracy --weights to_be_resolved/yolo26x_d-fire/weights/best.pt --data datasets/smoke_fire_detection/D-Fire/data.yaml --split test --imgsz 800 --out to_be_resolved/yolo26x_d-fire/eval_report_test.json` (chạy nền, retry với `cwd` đổi vì data.yaml dùng path tương đối); `torch.load` đọc metadata `best.pt`/`last.pt`; `git show`/`git log --oneline --all` đối chứng `results.csv` E0/budget9 qua các commit lịch sử; 3 Agent (web research) song song.

- 2026-07-17 13:08 +07:00
  - Mục tiêu: tận dụng lúc chờ RF-DETR-L/Pyro-SDIS resume trên Kaggle (12 epoch còn lại) — chạy detector-cache YOLO26x/Pyro-SDIS candidate trên FIgLib, chuẩn bị trước cho gate 13d (không phụ thuộc RF-DETR xong).

  - **1. Detector cache YOLO26x/Pyro-SDIS trên FIgLib dev split:**
    - Chạy trực tiếp trên GPU local `ai2` (GB10) qua `.venv`, không qua Modal — data (`datasets/smoke_fire_detection/FIgLib`) + weights (`runs/yolo26x_pyro_sdis_budget9/weights/best.pt`) đã sẵn local, tránh chi phí cloud cho việc benchmark.
    - Pilot 30 frame trước (đúng protocol 13b) — PASS, class_map đúng `{0: smoke}` (Pyro-SDIS chỉ 1 class).
    - Full run: `--split dev` (28361 frame, **cố tình không đụng `test`/final** — giữ kỷ luật 13d: final camera-held-out chỉ mở sau khi khóa winner giữa YOLO26x/RF-DETR, chưa mở lúc này), `imgsz=1280`, `conf=0.05`, `iou=0.6`, `--resume` bật sẵn, chạy background (không tmux — job ước tính ban đầu ~2.7h do GPU share ~90-96% với user khác trên server, nhưng thực tế chạy nhanh hơn nhiều nhờ batch=8: `28360/28361` frame xong trong `~1h48m`, không cần kill/relaunch).
    - Kết quả: `28360/28361` frame thành công, `1` frame lỗi (đã skip có log, không phải bug script): `20231110.112257-Border36Fire-om-w-mobo-c/1699646403_+02226.jpg` — file rỗng 0 byte tại nguồn, cùng loại lỗi tải như `20250123_GilmanFire_tdllns-mobo-c` đã biết, nhưng **frame mới, chưa từng document** (audit gốc 2026-07-08 chỉ bắt lỗi archive-level, không bắt frame lẻ này). Output: `artifacts/smoke_fire_detection/figlib_detector_cache_yolo26x_pyro_sdis_dev.jsonl`.
    - Chưa chạy `temporal_eval.py compare-candidates` — chờ RF-DETR-L/Pyro-SDIS xong để có đủ 2 candidate cho gate 13d.

  - **2. Phản biện nhận định 1 agent khác về job trên (user yêu cầu, không tự nhận suông — verify bằng `git blame`/`ps`):**
    - **`weights_sha256` vi phạm no-hash rule: ĐÚNG.** `git blame`: code (`sha256_file`+field) vào `eval.py` ngày 2026-07-13, rule cấm hash vào `CLAUDE.md` ngày 2026-07-16 — không viết sai lúc đó, nhưng đúng loại cơ chế đã bị trim ở `modal_app.py`/`pyro_sdis_snapshot.json` cùng đợt 2026-07-16/17, bị bỏ sót ở `eval.py`. Grep toàn repo xác nhận thêm: **0 reader** cho field này (kể cả `temporal_eval.py compare-candidates`) — vừa phạm no-hash rule vừa phạm rule tối giản (code chết).
    - **Job không chạy tmux, phụ thuộc phiên CLI: ĐÚNG**, verify bằng `ps` (process cache là con trực tiếp của process CLI phiên chat qua bash wrapper, không phải tmux độc lập) — đúng loại rủi ro mà `agent_context.md` từng note và giải quyết bằng tmux cho RF-DETR training. Không hành động (không kill/relaunch) vì job thực tế xong trong ~21 phút còn lại lúc kiểm tra tiến độ — chi phí restart không đáng so với risk window đã ngắn lại.
    - **GPU share làm `latency_ms` không hợp lệ cho benchmark cost: ĐÚNG nhưng không ảnh hưởng** — cache này chỉ phục vụ AUROC/gate 13d theo confidence, không phải E4 cost frontier.

  - **3. Patch `eval.py` xóa hash dead code (user duyệt):** xóa hàm `sha256_file`, biến `weights_hash`, field `"weights_sha256"` trong `cmd_detector_cache`, xóa import `hashlib` thừa. Strip field `weights_sha256` khỏi toàn bộ 28360 record đã cache (rewrite in-place, giữ nguyên field khác + thứ tự record).
  - Verify: `py_compile` PASS; `detector-cache --help` parse OK; sanity-check record đầu sau strip (field khác nguyên vẹn, JSON valid).

  - Thay đổi trực tiếp:
    - Sửa: `tasks/smoke_fire_detection/eval.py` (xóa `sha256_file`/`weights_hash`/field `weights_sha256`/import `hashlib`).
    - Rewrite in-place: `artifacts/smoke_fire_detection/figlib_detector_cache_yolo26x_pyro_sdis_dev.jsonl` (strip field `weights_sha256` khỏi 28360 dòng).
    - Sửa: `CHANGELOG.md`.
  - Thay đổi gián tiếp (script tự sinh, không phải agent tool trực tiếp):
    - `artifacts/smoke_fire_detection/figlib_detector_cache_yolo26x_pyro_sdis_dev.jsonl` — tạo mới bởi `eval.py detector-cache` (trước khi bị strip field ở bước 3).
    - `artifacts/smoke_fire_detection/figlib_detector_cache_yolo26x_pyro_sdis_dev.jsonl.errors.json` — tự sinh khi có 1 frame lỗi (329 byte, giữ lại làm log).
  - Command chính đã chạy:
    - `source .venv/bin/activate && python -m tasks.smoke_fire_detection.eval detector-cache --index artifacts/smoke_fire_detection/figlib_index.jsonl --data-root datasets/smoke_fire_detection/FIgLib --weights artifacts/smoke_fire_detection/runs/yolo26x_pyro_sdis_budget9/weights/best.pt --out <cache_pilot> --split all --pilot --limit 30 --batch 4 --imgsz 1280 --conf 0.05 --iou 0.6 --candidate-revision yolo26x_pyro_sdis_budget9_ep19` (pilot, scratchpad).
    - Cùng lệnh, đổi `--out artifacts/smoke_fire_detection/figlib_detector_cache_yolo26x_pyro_sdis_dev.jsonl --split dev --batch 8 --resume` (full, background).
    - `wc -l`/`stat`/`ps -o pid,lstart,etime,cmd` theo dõi tiến độ + verify job.
    - `git log --follow -S "def sha256_file" --all --oneline -- eval.py`; `git show -s --format=... <hash>`; `git log --oneline -- CLAUDE.md` + loop kiểm từng commit có chứa rule no-hash chưa (xác định ngày rule + ngày code).
    - `grep -rn "weights_sha256\|sha256_file\|weights_hash" tasks/` (xác nhận 0 reader).
    - `ps -eo pid,ppid,pgid,sid,tty,stat,cmd` truy vết process tree job background.
    - `.venv/bin/python -m py_compile eval.py`; `python -m tasks.smoke_fire_detection.eval detector-cache --help`.
    - Script Python inline (`json.loads`/`del record['weights_sha256']`/rewrite) để strip field khỏi cache jsonl.

- 2026-07-17 10:00 +07:00
  - Mục tiêu: xử lý phản hồi user sau lượt trim/resolve trước — merge 2 CSV RF-DETR/D-Fire thành 1 file, verify lại `checkpoint_best_ema.pth` (user tự xóa rồi upload lại qua zip đổi đuôi .pth), rà kỹ hơn volume Modal cũ nhất `smoke-fire-lab-volume` cho epoch 1-2 budget9.
  - **Merge metrics CSV:** `metrics_epoch00_15.csv` (197 dòng, epoch 0-16 lẫn 9 dòng epoch-16 dở dang — đợt train 1 bị ngắt giữa epoch 16, không có eval/checkpoint cho epoch đó) + `metrics_epoch16_19.csv` (48 dòng, epoch 16-19 hoàn chỉnh từ đợt resume) → merge thành `metrics.csv` (235 dòng, epoch 0-19 tăng dần đều, không trùng): header giống hệt 2 file, bỏ 9 dòng epoch-16 dở dang của file 1 (superseded bởi epoch-16 hoàn chỉnh của file 2), còn lại nối thẳng. Xóa 2 file gốc.
  - **Verify lại `checkpoint_best_ema.pth`:** user xác nhận tự xóa (lý do: chỉ đại diện 15/16 epoch đầu, không đại diện toàn 20 epoch — nhưng số liệu tốt nên giữ) rồi upload lại qua zip đổi đuôi thành `.pth`. Check: `torch.load` load OK, `state_dict` 510/510 tensor khớp cấu trúc cũ, 0 tensor chứa NaN, `epoch=11` khớp giá trị trước. Không có dấu hiệu hỏng.
  - **Rà lại volume `smoke-fire-lab-volume` (volume cũ nhất, tạo 2026-07-07) cho epoch 1-2 budget9:** liệt kê đầy đủ `artifacts/smoke_fire_detection/` → chỉ có `split/`, `runs/` (chỉ chứa `dfire_yolo26n_baseline*`, không có `yolo26x_pyro_sdis_budget9`), `dfire_audit.json`. Xác nhận: run budget9 không tồn tại trên volume này — không phải do thiếu tìm, mà run đó chưa từng train trên volume cũ, chỉ có trên `smoke-fire-step13-volume` (tạo sau) + Kaggle. Epoch 1-2 giữ nguyên kết luận không phục hồi được từ phía server/Modal; chỉ còn khả năng nằm trong output notebook Kaggle cũ của user.
  - Sửa: `agent_context.md`, `tasks/smoke_fire_detection/docs/external_assets.md`, `CHANGELOG.md`.
  - Command: `ls -la` + `file` checkpoint_best_ema.pth; `.venv/bin/python` torch.load verify (epoch/tensor-count/NaN check); `modal volume ls smoke-fire-lab-volume` (root, `artifacts`, `artifacts/smoke_fire_detection` — 3 lượt để list đủ sâu); `awk`/`head`/`tail`/`diff` so header + phân bố epoch 2 CSV trước khi merge; merge bằng `head`+`awk`+`tail` nối file, `mv`/`rm` dọn file gốc.

- 2026-07-17 09:45 +07:00
  - Mục tiêu: resolve `to_be_resolved/` (unknown, rf-detr_d-fire, yolo26x_d-fire), tạo notebook Kaggle resume RF-DETR-L/Pyro-SDIS, dọn JSON artifacts + dataset thừa + code tasks/ (đặc biệt `test_pyro_sdis_converter.py`, `train_rfdetr.py`), điều tra khả năng phục hồi epoch 1-2 `results.csv` budget9.

  - **1. Resolve `to_be_resolved/unknown/`:** `last_resume.pt` (471M) xác định qua torch.load = checkpoint resume Ultralytics của run `yolo26x_pyro_sdis_budget9` segment Kaggle (epoch=18 zero-based → 19/20 done, best_fitness=0.4949, train_args trỏ `/kaggle/working/runs/yolo26x_pyro_sdis_budget9`, date 2026-07-15). Run đã DONE 20/20, `best.pt` còn → xóa file + rmdir `unknown/`.

  - **2. Resolve `to_be_resolved/rf-detr_d-fire/` → `artifacts/smoke_fire_detection/runs/rfdetr_large_dfire_kaggle/`:**
    - Xác minh từ 2 CSV + metadata checkpoint: đợt 1 = epoch 0-15 (`first_metrics.csv`), đợt 2 = epoch 16-19 resume từ `input_epoch_015_step_000007760.ckpt`. Best EMA toàn run = epoch 11 (`0.49614`); best regular toàn run = epoch 18 (`0.49324`).
    - **Xác minh claim "best_total = best toàn 20 epoch": SAI.** `checkpoint_best_total.pth` không có key `epoch`, global_step=9215 = best_regular; so sánh đủ 510/510 tensor `state_dict` + 510/510 `model` → byte-identical với `checkpoint_best_regular.pth`. Nguyên nhân (đọc source rfdetr `best_model.py:on_fit_end`): copy EMA chỉ khi `best_ema > best_regular` VÀ file `checkpoint_best_ema.pth` tồn tại trong working dir; đợt 2 Kaggle working dir mới, EMA không vượt watermark 0.49614 (restore từ callback state) nên file không được ghi → fallback copy best_regular. Best THẬT để inference = `checkpoint_best_ema.pth` (epoch 11). Claim "best_ema từ đợt 1 (kết thúc ~epoch 15-16)": ĐÚNG (epoch 11, đợt 1 kết thúc epoch 15, đợt 2 bắt đầu epoch 16).
    - Move: `checkpoint_19.ckpt`, `checkpoint_best_ema.pth`, `checkpoint_best_regular.pth`, `training_config.json`, `resume_protocol.json`; rename `first_metrics.csv` → `metrics_epoch00_15.csv`, `metrics.csv` → `metrics_epoch16_19.csv`. Xóa `checkpoint_best_total.pth` (duplicate đã verify).
    - **SỰ CỐ:** `checkpoint_best_ema.pth` (135,556,339 byte) xác nhận có mặt trong run dir lúc `09:30 +07` (ls sau move) nhưng biến mất lúc `~09:36 +07` (mtime dir); KHÔNG lệnh nào trong phiên agent đụng path này (toàn bộ command log ở mục Command bên dưới). Đã rà: `lsof +L1` không còn handle mở, `find /home/tts01` không còn bản sao nào (chỉ còn best_ema của run gb10 Pyro khác). KHÔNG phục hồi được từ server → **user cần re-upload từ nguồn Kaggle/local**. `checkpoint_19.ckpt` + `checkpoint_best_regular.pth` + CSV/JSON còn nguyên.
    - `to_be_resolved/yolo26x_d-fire/` giữ nguyên (rỗng, chờ user train xong).
  - **3. Điều tra epoch 1-2 `results.csv` budget9: CHỐT KHÔNG PHỤC HỒI ĐƯỢC.** Đã kiểm: (a) git history — chỉ 1 commit `14ef54a` chứa file, bản commit đã bắt đầu từ epoch 3; (b) `train_results` nhúng trong `last_resume.pt` — chỉ chứa epoch 19 (CSV segment Kaggle); (c) Modal volume `smoke-fire-step13-volume` — tải `results.csv` trên volume về so: cũng bắt đầu epoch 3 (phủ 3-18); (d) volume cũ `smoke-fire-lab-volume` — không có run budget9; (e) `modal app logs` — app không tồn tại (chạy ephemeral, log không giữ). Nguồn duy nhất còn khả dĩ: output version cũ của notebook Kaggle trên tài khoản user.
  - **4. Notebook Kaggle resume RF-DETR-L/Pyro-SDIS:** thêm `tasks/smoke_fire_detection/kaggle_resume_pyro_sdis_rfdetr.ipynb` (tham khảo `d-fire_rf-detr-l.ipynb` của user + args THẬT đọc từ `checkpoint_best_ema.pth` gb10). Giữ protocol 13f: 1280px/20ep/seed 20260707/effective batch 16/no early-stop/augment mặc định; giữ mặc định gb10 (lr_scheduler `step`, không truyền num_classes). Sai khác bắt buộc do hardware (ghi rõ trong notebook): devices 1→2×T4 (micro 2×2GPU×accum4), amp `auto`(bf16)→`fp16`, workers 4→2. Phát hiện thêm: args nhúng ckpt gb10 có `early_stopping=true` + callback `RFDETREarlyStopping` trong `checkpoint_7.ckpt` — trái với script/policy; notebook ép `early_stopping=False`. Xóa notebook tham khảo `to_be_resolved/rf-detr_d-fire/d-fire_rf-detr-l.ipynb` sau khi dùng xong + rmdir.
  - **5. Trim code `tasks/` (sau khi training YOLO xong + RF-DETR chuyển Kaggle):**
    - Xóa `train_rfdetr.py` (thay bằng notebook), `test_pyro_sdis_converter.py` (converter đã xong việc), `train.py` (chỉ còn được import bởi các Modal train wrapper cũng bị xóa; eval/temporal_eval/demo không import — đã grep).
    - `dataset.py`: gỡ subcommand `pyro-sdis` + toàn bộ helper converter (`cmd_pyro_sdis`, `pyro_*`, `percentile`, `value_percentiles`) + import thừa (io/tempfile/shutil/time/pyarrow/yaml/PIL). Còn đúng FIgLib index.
    - `modal_app.py`: gỡ `train_candidate`, `train_segment`, `segment_train_args`, `segment_preflight`, `segment_lock_path`, `artifact_status`, `verify_resume_patch`, `audit_checkpoint`, `verify_checkpoint_load`, `test_pyro_sdis_converter`, `inspect_pyro_archive`, `prepare_dataset_volume`, `inspect_dataset_archive`, `volume_chunk_status`, `sha256_path`, hằng `TRAIN_*`/`PYRO_ARCHIVE_*`/`PYRO_SPLIT_FILES`, import shutil/tarfile/time; CLI `pyro_sdis_cli` còn 6 action eval-path (build-index/probe/cache/download-pyronear/temporal/compare, default `cache`). Giữ nguyên: images, `build_figlib_index`, `cache_candidate`, `probe_detector`, `download_pyronear`, `run_temporal`, `compare_candidates`, `evaluate`.
  - **6. Dọn datasets:** xóa `datasets/smoke_fire_detection/pyro-sdis/` (parquet nguồn 3.1G — conversion xong, lấy lại được từ HF `pyronear/pyro-sdis`) + `pyro-sdis-yolo-rfdetr/` (shim symlink, notebook Kaggle tự tạo shim riêng). Giữ D-Fire/FIgLib/pyro-sdis-yolo.
  - **7. Dọn JSON artifacts:** `git rm` `figlib_audit.json` (audit one-time, index+manifest đủ dùng), `runs/yolo26x_pyro_sdis_budget9/run_metadata.json` (metadata 1 segment Modal L4, run kết thúc trên Kaggle — vừa lỗi thời vừa không ai đọc), `train_segment_state.json` (state scheduling kill-resume, chết theo train_segment). Giữ `figlib_index.jsonl`, `figlib_split_manifest.json`, `args.yaml`, `results.csv`, `metrics.csv` (gb10).
  - **8. Docs:** cập nhật `external_assets.md` (bỏ pyro-sdis parquet + shim; thêm 3 checkpoint run D-Fire mới, đánh dấu best_ema MẤT cần re-upload; sửa hướng dẫn resume gb10 sang notebook), `agent_context.md` (trạng thái run D-Fire, sự cố best_ema, notebook resume, code list mới, chốt epoch 1-2, removed list).
  - Verify: notebook JSON parse OK (7 cell); `py_compile` PASS 5 file .py còn lại; smoke-import `dataset` + `modal_app` PASS; `dataset.py --help` chỉ còn `figlib`; xóa `__pycache__` sinh ra khi verify.

  - Thay đổi trực tiếp:
    - Thêm: `tasks/smoke_fire_detection/kaggle_resume_pyro_sdis_rfdetr.ipynb`; `artifacts/smoke_fire_detection/runs/rfdetr_large_dfire_kaggle/` (move từ `to_be_resolved/rf-detr_d-fire/`, 2 CSV đổi tên như trên).
    - Sửa: `tasks/smoke_fire_detection/dataset.py`, `tasks/smoke_fire_detection/modal_app.py`, `tasks/smoke_fire_detection/docs/external_assets.md`, `agent_context.md`, `CHANGELOG.md`.
    - Xóa: `to_be_resolved/unknown/last_resume.pt`; `to_be_resolved/rf-detr_d-fire/{checkpoint_best_total.pth,d-fire_rf-detr-l.ipynb}` (+ rmdir 2 folder); `tasks/smoke_fire_detection/{train.py,train_rfdetr.py,test_pyro_sdis_converter.py}` (git rm); `artifacts/smoke_fire_detection/figlib_audit.json`, `runs/yolo26x_pyro_sdis_budget9/{run_metadata.json,train_segment_state.json}` (git rm); `datasets/smoke_fire_detection/{pyro-sdis/,pyro-sdis-yolo-rfdetr/}`.
    - `git add`: notebook mới + 4 file text run dir mới (`training_config.json`, `resume_protocol.json`, 2 CSV). Chưa commit.
  - Thay đổi gián tiếp: `py_compile`/import sinh `tasks/**/__pycache__/` (đã xóa); scratchpad session (ngoài repo): `inspect_ckpts.py`, `inspect2.py`, `inspect3.py`, `modal_results.csv`. Sự cố mất `checkpoint_best_ema.pth` (run D-Fire) ghi ở mục 2 — không rõ tác nhân, không thuộc lệnh agent.
  - Command chính: `find`/`ls`/`du` khảo sát; `.venv/bin/python` chạy 3 script inspect (torch.load metadata + so sánh tensor + đọc CSV); `git log --oneline --all -- .../results.csv`; `git show 14ef54a:.../results.csv | head`; `modal volume list`; `modal volume ls smoke-fire-step13-volume ...` + `smoke-fire-lab-volume ...`; `modal volume get smoke-fire-step13-volume .../results.csv`; `modal app logs smoke-fire-detection-yolo` (app không tồn tại); `mkdir -p` + `mv` 7 file + `rm` best_total/last_resume/ipynb + `rmdir`; `git rm -q` 3 .py + 3 .json; `rm -rf` pyro-sdis + shim; `py_compile` + smoke import + `dataset.py --help`; `lsof +L1` + `find /home/tts01` (điều tra sự cố); `git add`; `TZ=Asia/Bangkok date`.

- 2026-07-16 19:04 +07:00
  - Mục tiêu: xử lý 4 quyết định user duyệt sau khi chất vấn đợt trim trước — xóa `requirements*.lock` chết, untrack `best.pt` ngoại lệ cho nhất quán, xóa notebook/script D-Fire Kaggle/Colab (user xác nhận training đã xong), viết doc external assets.
  - Xóa `tasks/smoke_fire_detection/requirements.lock` + `requirements-cu130.lock` (đã gỡ chỗ dùng `add_lock()` lượt trước).
  - `git rm --cached artifacts/smoke_fire_detection/runs/dfire_yolo26n_baseline_full_vram/weights/best.pt` — untrack, file vẫn còn trên đĩa (5.2M), demo vẫn chạy được local. Không còn ngoại lệ track/untrack giữa các weight.
  - Xóa `colab_train_dfire_yolo26x.ipynb`, `kaggle_train_dfire_rfdetr.ipynb` (từ `tasks/smoke_fire_detection/`), `train_dfire_yolo26x.py`, `train_dfire_rfdetr.py` — user xác nhận D-Fire YOLO26x/RF-DETR-L trên Kaggle/Colab đã train xong, không cần script nữa. **Không đụng** `train_rfdetr.py` (Pyro-SDIS, đang cần resume từ `checkpoint_7.ckpt`, khác tên dễ nhầm với `train_dfire_rfdetr.py`).
  - Thêm `tasks/smoke_fire_detection/docs/external_assets.md` — liệt kê dataset (`D-Fire/`, `pyro-sdis/`, `pyro-sdis-yolo/`, `FIgLib/`) + checkpoint (`dfire_yolo26n_baseline_full_vram`, `yolo26x_pyro_sdis_budget9`, `rfdetr_large_pyro_sdis_gb10`) không track git, kèm nguồn/cách lấy lại. Lưu ý: `D-Fire-train-ready.zip` đã xóa nên `datasets/smoke_fire_detection/D-Fire/` giờ là bản sao DUY NHẤT — không còn nơi tải lại, phải tự backup thủ công.
  - Verify: `py_compile` PASS toàn bộ `.py` còn lại trong `tasks/smoke_fire_detection/`.
  - Thay đổi trực tiếp: xóa `requirements.lock`, `requirements-cu130.lock`, `colab_train_dfire_yolo26x.ipynb`, `kaggle_train_dfire_rfdetr.ipynb`, `train_dfire_yolo26x.py`, `train_dfire_rfdetr.py`; untrack (không xóa file) `runs/dfire_yolo26n_baseline_full_vram/weights/best.pt`; thêm `tasks/smoke_fire_detection/docs/external_assets.md`; sửa `agent_context.md`, `CHANGELOG.md`.
  - Thay đổi gián tiếp: không có.
  - Command: `git rm --cached ...`; `git rm -f ...` (2 notebook, do vướng staged rename từ lượt trước); `git rm ...` (2 script); `du -sh datasets/smoke_fire_detection/*/`; `du -sh artifacts/.../weights/* .../*.ckpt .../*.pth`; `.venv/bin/python -m py_compile` toàn bộ `.py` còn lại.

- 2026-07-16 18:56 +07:00
  - Mục tiêu: user chất vấn lại đợt trim trước — phát hiện `pyro_sdis_snapshot.json` vẫn là cơ chế hash-lock (vi phạm rule "không hash" đã có từ trước) và yêu cầu gỡ hẳn khỏi code thay vì chỉ giữ file. Trả lời thêm về 2 best checkpoint RF-DETR, ý nghĩa "add metadata 3 run dir", tính nhất quán track/untrack `.pt`, và rà soát thêm `requirements*.lock` + notebook/script D-Fire.
  - **Gỡ hash-lock Pyro-SDIS (đã làm xong):**
    - `dataset.py`: xóa `import hashlib`, xóa `--expected-shards` CLI arg, xóa hàm `sha256_file`, xóa toàn bộ block validate SHA-256 shard + block "invariant_differences" trong `cmd_pyro_sdis` (cả hai chỉ được nuôi bởi `--expected-shards`, cùng gỡ). `cmd_pyro_sdis` giờ chỉ làm đúng 1 việc: convert parquet → YOLO layout + audit thống kê (không hash, không so khớp expected).
    - `modal_app.py`: xóa hẳn 2 Modal function `convert_pyro_sdis`/`package_pyro_sdis` (one-time dataset packaging đã xong, archive đã có sẵn trên Modal volume, không còn lý do rerun); xóa nhánh `action == "convert"|"package"` + 4 param liên quan (`data_root`,`out`,`audit_out`,`expected_shards`) khỏi `pyro_sdis_cli`; xóa import `SimpleNamespace` (hết chỗ dùng). Đã kiểm tra `sha256_path`/`PYRONEAR_SHA256` KHÔNG đụng — dùng cho checkpoint kill-resume audit + verify checksum third-party model, phạm vi khác, ngoài scope câu hỏi này.
    - Xóa `artifacts/smoke_fire_detection/pyro_sdis_snapshot.json` (orphan hoàn toàn sau khi gỡ code trên).
    - Sửa `test_pyro_sdis_converter.py` cho khớp signature mới (bỏ `expected_shards=None, workers=None` khỏi lệnh gọi test).
    - Verify: `py_compile` PASS cả 3 file; `python -m unittest tasks.smoke_fire_detection.test_pyro_sdis_converter` chạy thật PASS 3/3; smoke-import `modal_app.py` PASS; `dataset.py pyro-sdis --help` xác nhận hết `--expected-shards`.
  - **Điều tra `requirements.lock`/`requirements-cu130.lock` (đã gỡ chỗ dùng, CHƯA xóa file — chờ user xác nhận):** xác nhận 2 file này bị `add_lock()` copy vào Modal image nhưng KHÔNG có chỗ nào chạy `pip install -r` từ chúng — version cài thật trong `yolo_image` là hardcode trực tiếp trong lời gọi `pip_install(...)`, và version đó cũng lệch với `requirements.lock` (vd `numpy` lock ghi `2.5.1`, `.venv` cài `2.4.6`; `pyarrow` lock `21.0.0` vs `.venv` `24.0.0`). Đã xóa hàm `add_lock` + 2 lời gọi trong `modal_app.py` (dead code thật, đã verify compile/import PASS). CHƯA xóa 2 file `.lock` — hỏi lại user trước.
  - Thay đổi trực tiếp: sửa `tasks/smoke_fire_detection/dataset.py`, `tasks/smoke_fire_detection/modal_app.py`, `tasks/smoke_fire_detection/test_pyro_sdis_converter.py`, `agent_context.md`, `CHANGELOG.md`; xóa `artifacts/smoke_fire_detection/pyro_sdis_snapshot.json`.
  - Thay đổi gián tiếp: `py_compile`/`unittest` sinh `tasks/smoke_fire_detection/__pycache__/` (đã xóa lại sau mỗi lần verify).
  - Command: `.venv/bin/python -m py_compile ...`; `.venv/bin/python -m unittest tasks.smoke_fire_detection.test_pyro_sdis_converter -v`; `python -c "import ..."`; `python -m tasks.smoke_fire_detection.dataset pyro-sdis --help`; `.venv/bin/pip freeze | grep -iE ...` (so version lock vs venv thật).

- 2026-07-16 18:20 +07:00
  - Mục tiêu: trim/refactor lần 1 (server ai2, Linux) — thay dataset D-Fire, dọn tooling/artifact đã xong việc, dọn checkpoint dở dang, gitignore weight, trim dead code, purge `.git` rác, merge kết quả train YOLO26x/Pyro-SDIS, dọn report folder, viết lại `agent_context.md`.
  - Kết quả tổng: dung lượng repo `64G → 51G` (`du -sh .`); `.git` `2.7G → 132M`.

  - **1. Thay dataset D-Fire:**
    - `unzip -t D-Fire-train-ready.zip` PASS (12.6s) → giải nén tạm `datasets/smoke_fire_detection/_dfire_extract_tmp/` → audit: `43,055` file; train `15,500`/valid `1,721`/test `4,306` — khớp kỳ vọng.
    - Rename `D-Fire/` → `D-Fire_old/`; move bản mới vào `D-Fire/`; audit lại PASS (giống hệt số trên).
    - Xóa `D-Fire_old/` (3.0G), `datasets/smoke_fire_detection/D-Fire.zip` (3.0G, xác nhận cũ hơn qua mtime 2026-07-15 07:08 so với zip mới 2026-07-16 10:03, cùng layout `D-Fire/{train,valid,test}`), `D-Fire-train-ready.zip` ở root (3.0G, sau khi audit PASS).

  - **2. Dọn tooling + artifact D-Fire đã xong việc:**
    - Xóa `tasks/smoke_fire_detection/review_dfire_label_fixes.py`, `review_dfire_valid_labels.py` (chỉ còn tham chiếu lịch sử trong `CHANGELOG.md` cũ — chấp nhận được).
    - Move `colab_train_dfire_yolo26x.ipynb`, `kaggle_train_dfire_rfdetr.ipynb` từ root vào `tasks/smoke_fire_detection/` (`git mv`).
    - Grep toàn repo từng file trong `artifacts/smoke_fire_detection/*.json|md` trước khi xóa. Xóa: `dfire_label_fixes.json` (chỉ ref lịch sử), `human_review_pack_key.json` (0 tham chiếu), `human_review_labels.json`/`human_review_pack.md` (chỉ ref lịch sử trong `research_plan.md`, số liệu đã inline sẵn), `pyro_sdis_audit.json` (chỉ được ghi bởi `dataset.py`, không nơi nào đọc lại — `package_pyro_sdis` trong `modal_app.py` đọc một file audit ephemeral khác, không phải file này), `pyro_sdis_yolo_archive.json` (chỉ dùng làm idempotency short-circuit, tar archive tương ứng hiện không tồn tại local nên nhánh đó không kích hoạt). **Giữ** `pyro_sdis_snapshot.json` — vẫn được `dataset.py:cmd_pyro_sdis` và `modal_app.py:package_pyro_sdis` đọc thật để validate SHA-256 shard trước convert (live read-path, không phải log lịch sử). Giữ nguyên `figlib_index.jsonl`, `figlib_split_manifest.json`, `figlib_audit.json` theo yêu cầu.

  - **3. Checkpoint RF-DETR/Pyro-SDIS:** xóa `checkpoint_0.ckpt` → `checkpoint_6.ckpt` (mỗi file 566,181,923 byte, tổng ~3.7G) trong `artifacts/smoke_fire_detection/runs/rfdetr_large_pyro_sdis_gb10/`. Giữ `checkpoint_7.ckpt`, `checkpoint_best_ema.pth`, `checkpoint_best_regular.pth`, `metrics.csv`.

  - **4. `.gitignore` + git add metadata:**
    - Thêm `*.pt`, `*.ckpt`, `*.pth`, `*.onnx`, `*.engine` vào `.gitignore` (giữ nguyên rule cũ).
    - `git add` metadata: `runs/rfdetr_large_pyro_sdis_gb10/metrics.csv`; `runs/yolo26x_pyro_sdis_budget9/{args.yaml,results.csv,run_metadata.json,train_segment_state.json}`.
    - Verify: `runs/dfire_yolo26n_baseline_full_vram/weights/best.pt` (đã track từ trước) không bị ảnh hưởng bởi gitignore mới; weight 3 run dir đều bị ignore đúng (`git check-ignore -v`).

  - **5. Trim dead code:** dùng 1 agent research (đọc `dataset.py`, `train.py`, `train_rfdetr.py`, `eval.py`, `temporal_eval.py`, `modal_app.py`, `report/demo_inference.py`, `test_pyro_sdis_converter.py`) tìm code chết. Kết quả: hầu hết vẫn sống (Modal entrypoint, CLI subcommand, hoặc entrypoint chạy tay hợp lệ như `train.py:main()`). Chỉ xóa `--workers` CLI arg trong `dataset.py` (declare nhưng không đâu đọc `args.workers`) + dòng `workers=None` tương ứng truyền vào `cmd_pyro_sdis(...)` trong `modal_app.py`. Xóa `tasks/smoke_fire_detection/__pycache__/` (sinh ra từ bước verify, không phải pre-existing). Verify: `py_compile` PASS cả 10 file; smoke-import `dataset.py` + `modal_app.py` PASS; `dataset.py pyro-sdis --help` xác nhận hết `--workers`.

  - **6. Merge kết quả train YOLO26x/Pyro-SDIS (user duyệt):**
    - Thư mục root `yolo26x pyro sdis/` (tải từ Kaggle) là 2 epoch cuối (19-20) resume từ checkpoint budget9, hoàn tất **20/20 epoch**. Xác nhận qua `args.yaml` (`resume: .../last_resume.pt`, `epochs: 20`) + `results.csv` (epoch 19 mAP50-95=`0.4949`, epoch 20=`0.49303`).
    - Merge vào `artifacts/smoke_fire_detection/runs/yolo26x_pyro_sdis_budget9/`: copy `best.pt` + `args.yaml` đè bản cũ (epoch 17); nối `results.csv` (epoch 3-18 cũ + 19-20 mới = full history epoch 3-20, epoch 1-2 thiếu từ trước không phục hồi được); xóa `weights/last.pt` (113M) + `weights/last_resume.pt` (450M) — lỗi thời, training đã xong, không còn resume.
    - Xóa thư mục root `yolo26x pyro sdis/` sau merge.
    - **Epoch improvement summary (mAP50-95, val Pyro-SDIS):** tăng đơn điệu từ `0.352` (epoch 3) lên đỉnh `0.4949` (epoch 19), dao động nhẹ quanh epoch 10-18 (`0.462-0.492`) trước khi đạt đỉnh cuối; epoch 20 giảm nhẹ về `0.493`. Best = epoch 19.

  - **7. Report folder** (`tasks/smoke_fire_detection/report/`, chuẩn bị làm lại toàn bộ theo yêu cầu user): xóa `README.md`, `slide_script.md`, `demo_results/` (13M, ảnh + `summary.json` — gắn với checkpoint dở dang lúc viết báo cáo #1, nay đã lỗi thời vì YOLO26x đã xong 20/20). Giữ `demo_inference.py` (script tái dùng được, path checkpoint 3 model vẫn khớp hiện trạng). Xóa `report/__pycache__/`.

  - **8. `.git` gc/prune (user duyệt):** phát hiện `.git` pack chứa 5 blob checkpoint RF-DETR cũ (mỗi blob 566,181,923 byte, tổng `~2.64GB`) **unreachable** — không thuộc lịch sử branch hiện tại (`git fsck --unreachable --no-reflogs`), khả năng từ 1 commit từng bị amend/reset trước khi push. Chạy `git reflog expire --expire=now --expire-unreachable=now --all` + `git gc --prune=now`. Kết quả: `.git` `2.7G → 132M`; `git fsck --full` sạch; `git log`/`git status` vẫn nguyên vẹn. Lưu ý: đây là repo clone riêng trên server ai2, độc lập với `.git` máy Windows local (máy đó đã prune riêng lúc `2026-07-16 16:25 +07:00`, `2.9GiB → 53.7MiB`).

  - **9. `agent_context.md`:** viết lại — bỏ toàn bộ log Kaggle debug/GUI CRUD/notebook fix đã lỗi thời (đã xong việc, không còn actionable); cập nhật trạng thái mới: D-Fire final, YOLO26x DONE 20/20, RF-DETR còn dừng ở checkpoint_7, report folder đã dọn, trim 2026-07-16.

  - Thay đổi trực tiếp:
    - Sửa: `.gitignore`, `tasks/smoke_fire_detection/dataset.py`, `tasks/smoke_fire_detection/modal_app.py`, `agent_context.md`, `CHANGELOG.md`.
    - Move (`git mv`): `colab_train_dfire_yolo26x.ipynb`, `kaggle_train_dfire_rfdetr.ipynb` (root → `tasks/smoke_fire_detection/`).
    - Xóa: `tasks/smoke_fire_detection/review_dfire_label_fixes.py`, `review_dfire_valid_labels.py`; `artifacts/smoke_fire_detection/{dfire_label_fixes.json,human_review_labels.json,human_review_pack.md,human_review_pack_key.json,pyro_sdis_audit.json,pyro_sdis_yolo_archive.json}`; `artifacts/smoke_fire_detection/runs/rfdetr_large_pyro_sdis_gb10/checkpoint_{0,1,2,3,4,5,6}.ckpt`; `tasks/smoke_fire_detection/report/{README.md,slide_script.md,demo_results/}`; `datasets/smoke_fire_detection/{D-Fire_old/,D-Fire.zip}`; `D-Fire-train-ready.zip` (root); `yolo26x pyro sdis/` (root); `artifacts/smoke_fire_detection/runs/yolo26x_pyro_sdis_budget9/weights/{last.pt,last_resume.pt}`.
    - Ghi đè: `artifacts/smoke_fire_detection/runs/yolo26x_pyro_sdis_budget9/{weights/best.pt,args.yaml,results.csv}` (merge từ thư mục root).
    - `git add`: `artifacts/smoke_fire_detection/runs/rfdetr_large_pyro_sdis_gb10/metrics.csv`; `artifacts/smoke_fire_detection/runs/yolo26x_pyro_sdis_budget9/{args.yaml,results.csv,run_metadata.json,train_segment_state.json}`.
    - `git gc --prune=now` sau `git reflog expire --expire=now --expire-unreachable=now --all`.

  - Thay đổi gián tiếp:
    - `unzip` sinh thư mục tạm `datasets/smoke_fire_detection/_dfire_extract_tmp/` (tự xóa sau khi move xong).
    - `py_compile` sinh `tasks/smoke_fire_detection/__pycache__/` + `report/__pycache__/` (đã xóa lại).
    - `git gc` viết lại toàn bộ `.git/objects/pack/*` (pack mới, pack cũ 2.6G bị loại bỏ).

  - Command chính đã chạy:
    - `unzip -t D-Fire-train-ready.zip`; `unzip -q ... -d _dfire_extract_tmp`; `mv`/`rm -rf` swap D-Fire.
    - `git mv` 2 notebook; `git rm --cached` (không tác dụng, file chưa track) + `rm -f` cho review script/artifact json.
    - `rm -fv` checkpoint_0-6; `git add` metadata 3 run dir; `git check-ignore -v`.
    - `.venv/bin/python -m py_compile <file>` × 10; `.venv/bin/python -c "import ..."`; `python -m tasks.smoke_fire_detection.dataset pyro-sdis --help`.
    - `git rev-list --objects --all | git cat-file --batch-check`; `git verify-pack -v`; `git fsck --unreachable --no-reflogs`; `git reflog expire --expire=now --expire-unreachable=now --all`; `git gc --prune=now`; `git fsck --full`.
    - `cp`/`rm` merge `yolo26x pyro sdis/` vào `runs/yolo26x_pyro_sdis_budget9/`; `tail -n +2 results.csv >> results.csv`.
    - `git rm -r` report README/slide_script/demo_results.
    - `du -sh`/`git status --short`/`git log --oneline` để verify trước/sau ở từng bước.
- 2026-07-17 17:03 +07:00
  - Mục tiêu: review + sửa tối thiểu notebook resume RF-DETR-L/Pyro-SDIS theo yêu cầu user; giữ fairness YOLO26x Pyro-SDIS; user chốt tự xử lý extension checkpoint `.zip`→`.ckpt`/`.pth`, giữ cell `find /kaggle/input`, KHÔNG đổi resolution/internal multi-scale, batch, accum, scheduler, worker, AMP, dataset, model.
  - Xác minh: `checkpoint_9.zip` load được như full-state PyTorch (epoch=9, global_step=18,470, optimizer+LR scheduler+fit loop đủ) — upload đổi extension, không phải archive cần giải nén. Run Kaggle đã hoàn chỉnh epoch 8-9; epoch 10 interrupted chưa checkpoint. EMA mAP50-95 epoch 9=`0.46255`; resume không reset training state.
  - Sửa notebook: trước `model.train`, copy `RUN_DIR/metrics.csv` hoặc `metrics.csv` nằm cạnh input checkpoint vào `metrics_through_epoch_XX.csv`; RF-DETR có thể xóa log root khi resume nên history cũ được giữ. Cell artifact cuối liệt kê `metrics*.csv`; markdown ghi điều kiện upload metrics cạnh checkpoint qua session mới.
  - Không sửa: API `resolution=1280`; RF-DETR internal multi-scale `[1440]` theo framework; micro batch 2, accum 4, effective batch 16; fp16/2×T4; scheduler step/lr_drop100; checkpoint scan `.ckpt` duy nhất; cell debug `find`.
  - Thay đổi trực tiếp: format JSON cơ học + sửa `to_be_resolved/rf-detr_pyro-sdis/rf-detr-pyro-sdis.ipynb`; sửa `agent_context.md`, `CHANGELOG.md`.
  - Thay đổi gián tiếp: không có.
  - Command chính: `jq` format JSON/source notebook; `torch.load checkpoint_9.zip` metadata; parse `metrics.csv`; `apply_patch`; `.venv/bin/python -m json.tool` + `ast.parse` cell Python (bỏ Jupyter shell magic) PASS.

- 2026-07-17 17:30 +07:00
  - Mục tiêu: chuẩn bị clone sang máy mới; dọn file untracked dư nhưng giữ nguyên `datasets/`, `.venv/`, môi trường máy hiện tại; đánh dấu hai run D-Fire cấu hình cross-framework sai thành legacy; sửa code fresh train YOLO26x + RF-DETR-L tại `800px` bằng native recipe từng framework.
  - Kết luận cấu hình: fairness cũ ép scheduler giống nhau sai tầng. YOLO26x legacy ép `warmup_epochs=0.0` + `cos_lr=true`; native Ultralytics 8.4.90 dùng warmup 3 epoch + cosine tắt. RF-DETR legacy ép `lr_scheduler=cosine` + `lr_min_factor=0.01`; native RF-DETR 1.8.3 dùng `step`, warmup native vẫn 0. Hai legacy run chỉ còn vai trò same-family comparison với fresh rerun; không dùng so model-family chuẩn.
  - Sửa YOLO notebook: resolution markdown `640→800`; fresh `model.train` bỏ explicit optimizer/LR/scheduler/warmup/augmentation/AMP/deterministic/pretrained flag; giữ data, `800`, batch/nbs, 20 epoch, seed, no-early-stop, GPU/worker/checkpoint. Clear toàn bộ output/execution count legacy. Giữ run name/protocol cũ theo user; user tự xóa checkpoint/run legacy, không thêm resume guard.
  - Phục hồi RF-DETR notebook từ Git history commit `7305fed`, chuyển vào task folder; resolution `640→800`; batch API `4→8`, grad accum `4→2`; fresh `model.train` bỏ explicit LR/scheduler/warmup/augmentation/AMP; bổ sung import `os`/`shutil` bị thiếu; giữ data, batch/accum, 20 epoch, seed, no-early-stop, GPU/DDP/worker/checkpoint. Giữ run name/protocol cũ theo user; không thêm resume guard.
  - Gắn legacy trực tiếp: `training_config.json` + `resume_protocol.json` RF-DETR/D-Fire; `training_protocol.json` YOLO26x/D-Fire. Research plan/context: sửa vai trò legacy, protocol chuẩn 800, cấm dùng kết quả legacy để kết luận cross-family.
  - Clone handoff: viết lại `external_assets.md` dạng bullet; chia rõ file cần commit, asset ignored cần copy riêng, file dư không copy. `.venv/` giữ nguyên máy hiện tại; máy đích tạo venv mới. `datasets/` giữ nguyên 41G.
  - Dọn an toàn: xóa plot/preview/prediction cache YOLO D-Fire + `tasks/smoke_fire_detection/__pycache__/`. Lệnh xóa checkpoint dư bị approval layer từ chối do chi phí regenerate; không checkpoint/weight/state nào bị xóa; danh sách dư ghi trong `external_assets.md` chờ user duyệt riêng.
  - Verify: Linux ai2 aarch64 xác nhận bằng `uname -a`; RF-DETR package default xác nhận `TrainConfig.warmup_epochs=0.0`, `lr_scheduler=step`, `lr_min_factor=0.0`; hai notebook JSON parse + toàn bộ code cell AST PASS; grep fresh calls chỉ còn tham số protocol/infra; `git diff --check` PASS; `.venv=5.7G`, `datasets=41G` vẫn nguyên.
  - Thay đổi trực tiếp — thêm: `tasks/smoke_fire_detection/kaggle_train_dfire_rfdetr.ipynb`.
  - Thay đổi trực tiếp — sửa: `to_be_resolved/yolo26x_d-fire/colab_train_dfire_yolo26x.ipynb`; `to_be_resolved/yolo26x_d-fire/training_protocol.json`; `artifacts/smoke_fire_detection/runs/rfdetr_large_dfire_kaggle/{training_config.json,resume_protocol.json}`; `tasks/smoke_fire_detection/docs/{research_plan.md,external_assets.md}`; `agent_context.md`; `CHANGELOG.md`.
  - Thay đổi trực tiếp — xóa: `tasks/smoke_fire_detection/__pycache__/eval.cpython-312.pyc`; `to_be_resolved/yolo26x_d-fire/{BoxF1_curve.png,BoxPR_curve.png,BoxP_curve.png,BoxR_curve.png,confusion_matrix.png,confusion_matrix_normalized.png,labels.jpg,results.png,train_batch0.jpg,train_batch1.jpg,train_batch2.jpg,train_batch19380.jpg,train_batch19381.jpg,train_batch19382.jpg,val_batch0_labels.jpg,val_batch0_pred.jpg,val_batch1_labels.jpg,val_batch1_pred.jpg,val_batch2_labels.jpg,val_batch2_pred.jpg}`; `to_be_resolved/yolo26x_d-fire/test_accuracy/{BoxF1_curve.png,BoxPR_curve.png,BoxP_curve.png,BoxR_curve.png,confusion_matrix.png,confusion_matrix_normalized.png,predictions.json,val_batch0_labels.jpg,val_batch0_pred.jpg,val_batch1_labels.jpg,val_batch1_pred.jpg,val_batch2_labels.jpg,val_batch2_pred.jpg}`; `to_be_resolved/yolo26x_d-fire/test_evaluation/{BoxF1_curve.png,BoxPR_curve.png,BoxP_curve.png,BoxR_curve.png,confusion_matrix.png,confusion_matrix_normalized.png,val_batch0_labels.jpg,val_batch0_pred.jpg,val_batch1_labels.jpg,val_batch1_pred.jpg,val_batch2_labels.jpg,val_batch2_pred.jpg}`.
  - Thay đổi gián tiếp: không có.
  - Không đụng thay đổi có sẵn ngoài scope: `docs/Note_worthy.md`, `tasks/smoke_fire_detection/docs/research_toolbox.md`, `tasks/smoke_fire_detection/eval.py`, cache FIgLib và notebook RF-DETR/Pyro-SDIS.
  - Command đọc/điều tra: `sed -n ... agent_context.md/.gitignore/research_plan.md/external_assets.md/config/protocol`; `uname -a`; `git status --short --untracked-files=all`; `git diff`; `git log --all --oneline --name-status -- '*dfire*' ...`; `git show 7305fed:kaggle_train_dfire_rfdetr.ipynb`; `rg`/`git grep` tham chiếu D-Fire + package defaults; `du -ah/-sh`; `file`; `strings -n 24 state.db`; `.venv/bin/python` parse notebook/config/checkpoint metadata.
  - Command sửa: `git restore --source=7305fed -- kaggle_train_dfire_rfdetr.ipynb`; nhiều script `.venv/bin/python` JSON/text rewrite do `apply_patch` lỗi `bwrap: loopback`; `rm -rf` plot/preview/prediction cache + `__pycache__`. Hai lần `apply_patch` thất bại trước khi fallback; một lệnh bulk `rm` gồm checkpoint bị approval layer từ chối, không tạo thay đổi.
  - Command verify: `.venv/bin/python` `json.loads` + `ast.parse`; `rg -n 'model.train|RESOLUTION|warmup|cos_lr|lr_scheduler|...'`; `git diff --check`; `git diff --stat`; `git status`; `du -sh`; `TZ=Asia/Ho_Chi_Minh date`.

- 2026-07-17 17:33 +07:00
  - Mục tiêu: user duyệt rõ xóa danh sách checkpoint/state dư sau khi approval layer từ chối lệnh trước; không lách policy; chạy lại bằng authorization mới.
  - Xóa thành công ~`2.16GB`; giữ nguyên `datasets/` 41G, `.venv/` 5.7G, checkpoint 9 + best mới RF-DETR/Pyro-SDIS, best EMA RF-DETR/D-Fire legacy, best YOLO26x/D-Fire legacy.
  - Thay đổi trực tiếp — xóa: `artifacts/smoke_fire_detection/runs/rfdetr_large_pyro_sdis_gb10/{checkpoint_7.ckpt,checkpoint_best_ema.pth,checkpoint_best_regular.pth}`; `artifacts/smoke_fire_detection/runs/rfdetr_large_dfire_kaggle/{checkpoint_19.ckpt,checkpoint_best_regular.pth}`; `to_be_resolved/yolo26x_d-fire/weights/{last.pt,epoch19.pt}`; `to_be_resolved/rf-detr_pyro-sdis/{state.db,data.yaml}`.
  - Thay đổi trực tiếp — sửa: `tasks/smoke_fire_detection/report/demo_inference.py` — RF-DETR/Pyro best path cũ bị xóa → `to_be_resolved/rf-detr_pyro-sdis/checkpoint_best_ema.zip`; `tasks/smoke_fire_detection/docs/external_assets.md` — mục dư → đã xóa; `CHANGELOG.md`.
  - Thay đổi gián tiếp: không có.
  - Command: `rm -f <9 file dư>` sau user authorization; `du -sh` xác nhận dung lượng; `find ... -maxdepth 2 -type f`; `rg` tìm path checkpoint gãy; `.venv/bin/python` thay path demo + heading external assets; `TZ=Asia/Ho_Chi_Minh date`.
