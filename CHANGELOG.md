## 2026-07-21 16:16 GMT+7 — Thực thi song song 2 track (far-field SOTA anchor + near-field probe), auto-mode không hỏi lại việc reversible

- Bối cảnh: user chốt 2 quy tắc mới — (1) dataset/checkpoint chỉ lấy nếu KHÔNG cần form duyệt thủ công (HF/GitHub public hoặc form auto-grant instant là OK; ONFIRE/LFDN loại vì cần email/form review thủ công); (2) không hỏi lại các quyết định reversible/rẻ nữa — tự triển khai phương án tốt nhất, chỉ hỏi khi ảnh hưởng thật đến deadline/compute cost/performance. Agent chuyển sang thực thi trực tiếp, không dừng xin duyệt từng bước.
- **Verify cơ chế cấp quyền (loại ONFIRE/LFDN, giữ lại phần còn lại):** `curl`+GitHub/HF API xác nhận `pyronear/yolo11s_rapid-raccoon_v8.1.0` ungated (HF `gated:false`); SmokeyNet ONNX qua Dockerfile `sagecontinuum/sage-smoke-detection` (repo đã đổi org, phải tra `api.github.com/repositories/404460520` mới ra full_name/branch đúng — URL cũ trong research trước đã 404) tải trực tiếp `https://s3-west.nrp-nautilus.io/smokeynet/model.onnx` (public S3 bucket, không form); FIRESENSE Zenodo record `836749` xác nhận `access_right=open`, `license=cc-by-4.0`, 820MB; PYRONEAR-2025 xác nhận KHÔNG nằm trên HF — data thật ở Google Drive folder (repo `joseg20/wildfires2025` trỏ tới, tên paper trong repo khác tên arXiv nhưng cùng domain/tác giả/mô tả — suy luận hợp lý là bản title cũ, chưa 100% chắc chắn) — `gdown` list folder thành công không cần auth (không "Request access" trong HTML), coi là ungated hợp quy tắc user.
- **L0 error-slice trên winner (mới, code giữ lại):** thêm subcommand `temporal_eval.py error-slice` (bbox area/short-side vs confidence, offset-band AUROC, per-camera AUROC, FP audit candidates, visibility cross-reference) — chạy trên `rfdetr_large_dfire` dev+final. Kết quả chính: correlation area-vs-confidence yếu tuyến tính (Pearson 0.19 dev/0.065 final) NHƯNG bucket mean-confidence tăng đơn điệu rõ theo area (0.39→0.69 dev qua 6 bucket) — xác nhận định lượng tiny-object vẫn là nguyên nhân miss chính cho winner cụ thể (không chỉ cho detector cũ như trước). 26% positive frame dev hoàn toàn không có detection (conf<0.05). Per-camera AUROC: 107/143 camera dev có đủ nhãn 2 phía, median 0.876, ~10 camera chronic thấp (0.5-0.6) — không phải nhiễu ngẫu nhiên theo từng fire. Restore `human_review_labels.json` từ git history (`14ef54a4^`) để cross-reference visibility (30/40 sequence khớp dev). Chạy thêm `diagnose` (subcommand có sẵn) trên winner dev lần đầu tiên: 53/359 sequence low-AUROC (33 silent/13 confused/7 other) — cải thiện rõ so baseline cũ (207/509).
- **NE4 error-slice D-Fire (throwaway script, không giữ trong repo, chỉ giữ JSON artifact):** bbox area/short-side histogram theo class trên toàn bộ D-Fire train/valid/test. Kết quả: median short-side smoke ~29-31% chiều rộng ảnh, fire ~5.5% — cách xa ngưỡng vendor near-field (1.1-1.6%) 20-27 lần và xa cả median FIgLib far-field (0.735% area). **Kết luận NE0/NE4: D-Fire KHÔNG phải tiny-object domain — không copy ưu tiên P2/tiny-object của FIgLib sang near-field.** Artifact: `artifacts/smoke_fire_detection/ne4_dfire_scale_histogram.json`.
- **SOTA anchor far-field (Gói A):**
  - Tải `pyronear/yolo11s_rapid-raccoon_v8.1.0` (best.pt, ungated, README/manifest xác nhận `imgsz=1024`, `single_cls=true`, train trên `pyronear/pyro-dataset` — cùng leakage-caveat như `pyronear_yolov8s` cũ). Chạy `eval.py detector-cache --backend yolo --class-names smoke --imgsz 1024` trên FIgLib dev — đang chạy nền (`run_in_background`), ~28,361 frame, ETA ~1-1.5h.
  - Tải SmokeyNet `model.onnx` (222MB), viết mới `tasks/smoke_fire_detection/smokeynet_cache.py` (onnxruntime+Pillow, giữ tách khỏi eval.py vì input 2-frame tile-classifier khác hẳn box-detector abstraction) — replicate đúng preprocessing gốc (`sagecontinuum/sage-smoke-detection/src/inference.py`: resize 1392×1856 → crop 1040 dòng dưới → tile 224×224 overlap 20 → 45 tile/frame → normalize (x/255-0.5)/0.5 → input float64 batch×45×2×3×224×224). Pilot 5 frame verify đúng sau khi sửa dtype float32→float64 (lỗi ONNX INVALID_ARGUMENT ban đầu).
    - **Phát hiện chi phí:** CPU-only (không có onnxruntime-gpu build cho aarch64+CUDA13 qua pip; `onnxruntime-gpu` không có wheel phù hợp) — latency thật ~20-24s/frame trên 20-core CPU (ORT đã dùng hết multi-thread mặc định). Full dev set (~27.6k pair) sẽ mất ~7 ngày — vi phạm ràng buộc thời gian user. **Quyết định: chạy subsample 30 sequence** (chọn bằng `random.Random(20260707).sample` trên `dev_sequence_ids` của split manifest — dùng lại seed chuẩn của dự án, không phải layer mới), ETA ~14h, khớp "qua đêm/thời gian chết" user cho phép. Caveat bắt buộc ghi trong report: SmokeyNet là reference/subsample, KHÔNG so ngang hàng cỡ mẫu với winner.
    - **Sự cố + sửa:** lần chạy đầu tiên bug 2 lớp — (1) biến `SEQ_IDS` gán từ file debug lẫn cả dòng "n dev sequences: 359" (không chỉ danh sách ID) → `--sequence-ids` bị nhiễu, phải dừng job, tách lại file sạch; (2) script gốc không gọi `out_file.flush()` (khác `eval.py` có flush mỗi frame) → sau 57 phút chạy thật (CPU 1300%) chỉ 10 dòng được ghi xuống đĩa, rủi ro mất toàn bộ tiến độ nếu job chết giữa chừng trong ca chạy 14h không giám sát. Đã dừng job, thêm `flush()` mỗi frame + `--resume` (bỏ qua frame đã có, giống `eval.py`) + progress print mỗi 5 frame, chạy lại từ đầu qua `systemd-run --user --scope` (khớp quy ước dự án cho job dài không giám sát, xem tiền lệ `giai-doan3-cache-r2.scope`).
  - FIRESENSE (Zenodo record 836749, CC BY 4.0, mở, đã tải+giải nén thật): 49 video (27 fire pos/neg, 22 smoke pos/neg, khớp cấu trúc plan) → `datasets/near_field_fire_detection/FIRESENSE/{fire_videos,smoke_videos}/{pos,neg}/*.avi`, 1.2GB. Chưa chạy detector qua video (cần trích frame — hoãn, ưu tiên PYRONEAR-2025/far-field trước).
- **Gói B — PYRONEAR-2025 data-lever:** cài `gdown`, xác nhận Google Drive folder public thật (không "Request access" trong HTML, `gdown.download_folder(skip_download=True)` liệt kê được 11,543 file không cần auth) — cấu trúc `Pyro25/images/{train,val,test}/*.jpg` + folder `videos/` + bundle tiện lợi `pyronear2025.zip` (**9.31GB**, xác nhận qua log tải thật, không phải suy đoán). Đang tải zip nền (~11-12MB/s, ETA ~13-15 phút). Chưa lọc FIgLib-source/dedup, chưa fine-tune — bước tiếp theo sau khi giải nén xong.
- **Tạo mới `tasks/near_field_fire_detection` không cần thiết** — theo đúng quyết định plan cũ (NE1-NE5 tái dùng `temporal_eval.py`), chỉ tạo `datasets/near_field_fire_detection/` cho dữ liệu mới; code phân tích vẫn ở `tasks/smoke_fire_detection/`.
- File trực tiếp tạo: `tasks/smoke_fire_detection/smokeynet_cache.py`; `artifacts/smoke_fire_detection/ne4_dfire_scale_histogram.json`; `datasets/near_field_fire_detection/FIRESENSE/` (49 file video).
- File trực tiếp sửa: `tasks/smoke_fire_detection/temporal_eval.py` (thêm subcommand `error-slice`), `CHANGELOG.md` (mục này).
- File trực tiếp phục hồi từ git history: `artifacts/smoke_fire_detection/human_review_labels.json` (từ commit `14ef54a4^`).
- File gián tiếp tạo (lệnh CLI tự ghi, đang/đã chạy): `l0_error_slice_rfdetr_large_dfire_{dev,final}.json`, `diagnose_rfdetr_large_dfire_dev.json`, `figlib_detector_cache_pyronear_yolo11s_rapid_raccoon_dev.jsonl` (đang chạy), `figlib_detector_cache_smokeynet_reference_dev_subsample30.jsonl` (đang chạy), `pyronear_yolo11s_rapid_raccoon.pt`, `smokeynet_model.onnx` (đã copy vào `artifacts/smoke_fire_detection/`).
- Chưa làm / còn treo: lọc+giải nén PYRONEAR-2025, fine-tune winner trên PYRONEAR-2025 (Gói B), chạy detector qua FIRESENSE video, cập nhật report SOTA comparison (`model_benchmark_2x2.md` mục 9) với số Pyronear yolo11s/SmokeyNet mới, cập nhật `agent_context.md`. Câu hỏi treo cho user (không tự quyết vì đụng kỷ luật "final split one-shot"): có mở lại `--split test` cho 2 candidate reference mới (Pyronear yolo11s, SmokeyNet) hay chỉ báo cáo trên dev — cần sửa `figlib_dev_winner_lock.json` allowlist nếu đồng ý.

## 2026-07-21 16:35 GMT+7 — PYRONEAR-2025 giải nén + lọc leakage + resolve class semantics, chuẩn bị fine-tune (Gói B)

- **Giải nén thật:** 145,657 file, 9.2GB (`unzip -tq` xác nhận zip gốc không lỗi). Cấu trúc thật KHÁC README trong archive (README hứa `metadata.json` mỗi video — thực tế 0 file `metadata.json` nào tồn tại, chỉ có `images/*.jpg` + `labels/*.txt` per video-folder). Split: train 470 video/59,283 ảnh, val 59/12,206, test 63/9,238 (+ folder `wtfa/` 198 file, review false-positive nội bộ Pyronear, không dùng để train).
  - **Sự cố thao tác 09:28-09:29:** lệnh giải nén bị double-background (`&`+`disown` LỒNG bên trong `run_in_background: true`) khiến harness báo "completed" khi tiến trình `unzip` thật vẫn đang chạy nền mồ côi — phát hiện qua `ps aux` thấy process còn sống, `du -sh` chỉ 956MB lúc "báo xong". Đã đặt lại wait đúng cách (`until ! ps aux | grep ...`), xác nhận xong thật ở 145,657 file/9.2GB.
- **Xác nhận nguồn:** `README.md` trong archive dẫn thẳng arXiv:2402.05349v2, tác giả Mateo Lostanlen (PyroNear) — đúng 100% paper đã cite trong `research_plan.md`, không còn nghi ngờ "có thể chỉ là bản title cũ" như research trước.
- **Lọc leakage FIgLib-source — số đo được (không phải suy đoán):** so exact-match tên video-folder với `figlib_split_manifest.json` (`sequence_ids` đầy đủ 511, cả dev+final) → **315/592 video-folder (53%) trùng thẳng sequence_id FIgLib của mình** (vd `20190924_FIRE_hp-s-mobo-c`). Xác nhận cảnh báo leakage ở `research_plan.md` mục 8 bước 12 là thật, không phải thận trọng thừa. Sau loại trừ: 277 video sạch (train 217, val 28, test 32).
  - **Caveat:** chỉ loại bằng exact-match tên folder, KHÔNG chạy dHash dedup ảnh-đối-ảnh (rủi ro tồn dư: video đổi tên nhưng cùng nguồn gốc/camera — chưa loại được). Đủ tốt cho fine-tune thử nghiệm; nếu số cải thiện sát biên quyết định, cần dedup kỹ hơn trước khi tin.
- **Resolve class semantics (không có trong doc, phải tự điều tra):** label có 2 giá trị `class_id` (`0` và `1`), tưởng là smoke/fire nhưng xem ảnh trực tiếp (2 sample, `Read` tool) — CẢ HAI đều là khói (không có lửa/flame nào), không phân biệt được bằng mắt. Verify bằng thống kê: nhóm theo prefix tên nguồn (AWF, ADF, pyronear_force, brison, courmettes, croix, valbonne, ...) → **mỗi nguồn thuần 100% một class_id duy nhất** (AWF luôn 0, brison luôn 1, ...) — kết luận: đây là artifact của pipeline gán nhãn khác nhau giữa các nguồn upstream, KHÔNG phải phân loại smoke/fire thật (giống hệt tình huống `pyro-sdis` cũ: `class_id=1` dù `nc=1`). **Quyết định: remap cả 0 và 1 → `0` (smoke)** khi convert. Dataset này không có nhãn fire/flame nào (đúng scope paper: smoke-plume only).
- **Convert + lọc (script tạm `filter_pyronear2025.py`, KHÔNG giữ trong repo — theo đúng tiền lệ `pyro-sdis`/converter khác đã trim sau khi dùng xong):** copy ảnh + remap label → `datasets/smoke_fire_detection/PYRONEAR-2025-clean/{train,val,test}/{images,labels}/` + `data.yaml` + `filter_report.json` (ghi lại rule loại trừ + rationale class remap để không mất context khi code đã xóa). Số cuối: train 32,784 ảnh/32,159 box, val 8,260/7,316, test 4,267/3,628 — quy mô ngang Pyro-SDIS (29,537 train).
- **Chưa làm (điểm dừng có chủ đích, cần user quyết vì đụng compute cost/deadline thật):** chưa convert sang COCO format (RF-DETR cần COCO, không nhận thẳng YOLO txt — `eval.py rfdetr-accuracy` dùng `rfdetr.evaluation.coco_eval.CocoEvaluator`), chưa viết training script/notebook, CHƯA chọn venue GPU (local GB10 đang bận 2 job/~14h nữa, Modal cần chi phí, Kaggle cần thao tác tay không có CLI trên server này), chưa quyết continue-train từ checkpoint winner hay train từ đầu trên D-Fire+PYRONEAR-2025-clean gộp. Hỏi user trong chat trước khi tốn compute thật.
- File trực tiếp tạo (giữ trong repo): `datasets/smoke_fire_detection/PYRONEAR-2025/` (9.2GB, raw archive giải nén — sẽ đánh giá gitignore, quá lớn để track), `datasets/smoke_fire_detection/PYRONEAR-2025-clean/` (4.6GB, dataset đã lọc+convert).
- File trực tiếp tạo (scratch, không thuộc repo): `filter_pyronear2025.py` tại scratchpad.
- File trực tiếp sửa: `CHANGELOG.md` (mục này).
- `.gitignore` không cần sửa: `datasets/` đã ignore toàn bộ từ trước (dòng gốc), 2 folder mới tự động không track — verify bằng `git status --short datasets/` (rỗng).
- Chưa cập nhật: `agent_context.md` (sẽ gộp cùng lượt cập nhật report cuối, tránh sửa nhiều lần).

## 2026-07-21 16:44 GMT+7 — User chọn Kaggle (thủ công) cho fine-tune; chuẩn bị notebook + package upload

- User trả lời `AskUserQuestion` (venue GPU cho fine-tune Gói B): chọn **Kaggle thủ công** — không dùng Modal (tránh chi phí thật) hay xếp hàng GB10 local (đang bận ~14h).
- **Điều tra API `rfdetr==1.8.3` trước khi viết notebook (đọc source package trong venv, không đoán):** xác nhận `RFDETRLarge(resolution=..., pretrain_weights=<path .pth>)` là cơ chế đúng để continue-train từ checkpoint bất kỳ (không giới hạn COCO-pretrained gốc) — khác hẳn `resume=` trong `.train()` (chỉ dùng cho full-state `.ckpt` Lightning của CHÍNH lần train này, phục hồi khi Kaggle session đứt giữa chừng). Đọc docstring `training/module_model.py`/`training/callbacks/best_model.py` xác nhận rõ 2 cơ chế tách biệt, không share state.
- **Lấy lại 2 template notebook Kaggle cũ từ git history** (`kaggle_train_dfire_rfdetr.ipynb` tại commit `4718212^`, `kaggle_train_pyro_sdis_rfdetr.ipynb` tại commit `1ebd079`) để giữ đúng convention project (shim `val→valid` bằng symlink, resume-scan full-state `.ckpt`, effective batch 16 = 2/GPU×2GPU×accum4, fp16, seed 20260707) — không tự nghĩ lại từ đầu.
- **Viết mới `tasks/smoke_fire_detection/kaggle_train_pyronear2025_rfdetr_finetune.ipynb`:** continue-train `RFDETRLarge(resolution=800, pretrain_weights=checkpoint_best_total.pth)` trên `PYRONEAR-2025-clean` (chỉ dataset mới, không gộp lại D-Fire — đúng khung "data-only lever" A12, control = winner hiện tại). **Resolution giữ 800px khớp training gốc winner** (không phải 1280 — đó là imgsz lúc eval FIgLib, khác biến). Epoch budget = 8, judgment call đầu tiên chưa validate, ghi rõ trong markdown cell. Early-stop tắt, augmentation mặc định + HorizontalFlip 0.5, không dùng FIgLib chọn checkpoint (chọn theo PYRONEAR-2025-clean val, khớp nguyên tắc toàn dự án).
- **Đóng gói cho user tải thủ công lên Kaggle:** zip `datasets/smoke_fire_detection/PYRONEAR-2025-clean/` (4.6GB, đang nén nền, `zip -1` mức nén thấp cho nhanh) → scratchpad, cần user tự copy ra khỏi máy để upload Kaggle Dataset. Checkpoint winner (`checkpoint_best_total.pth`, 130MB, không cần zip) trỏ thẳng `artifacts/smoke_fire_detection/runs/rfdetr_large_dfire/checkpoint_best_total.pth`.
- **Việc user phải tự làm (ngoài khả năng agent — không có Kaggle credential/CLI trên server):** upload 2 Kaggle Dataset (data + checkpoint), tạo notebook mới trên Kaggle UI paste nội dung file `.ipynb`, chỉnh Accelerator = GPU T4 x2, Internet = on, chạy tay, tải kết quả checkpoint về khi xong.
- **Sau khi có checkpoint mới (việc của agent, chưa làm):** copy vào `artifacts/smoke_fire_detection/runs/rfdetr_large_dfire_pyronear2025_ft/`, chạy `eval.py detector-cache` + `temporal_eval.py g0`/`compare-candidates` so với winner gốc trên FIgLib dev (không đụng final/test — vẫn theo kỷ luật one-shot).
- File trực tiếp tạo: `tasks/smoke_fire_detection/kaggle_train_pyronear2025_rfdetr_finetune.ipynb`.
- File trực tiếp tạo (scratch, không thuộc repo): `pyronear2025-clean-kaggle-upload.zip` (đang nén), `build_pyronear2025_finetune_notebook.py`.
- Còn treo: chờ user tải zip ra khỏi máy + tự chạy Kaggle; câu hỏi mở final-split (mục 16:16) vẫn chưa trả lời.
