## 2026-07-21 16:16 GMT+7 — Thực thi song song 2 track (far-field SOTA anchor + near-field probe), auto-mode không hỏi lại việc reversible

- Bối cảnh: user chốt 2 quy tắc mới — (1) dataset/checkpoint chỉ lấy nếu KHÔNG cần form duyệt thủ công (HF/GitHub public hoặc form auto-grant instant là OK; ONFIRE/LFDN loại vì cần email/form review thủ công); (2) không hỏi lại các quyết định reversible/rẻ nữa — tự triển khai phương án tốt nhất, chỉ hỏi khi ảnh hưởng thật đến deadline/compute cost/performance. Agent chuyển sang thực thi trực tiếp, không dừng xin duyệt từng bước.
- **Verify cơ chế cấp quyền (loại ONFIRE/LFDN, giữ lại phần còn lại):** `curl`+GitHub/HF API xác nhận `pyronear/yolo11s_rapid-raccoon_v8.1.0` ungated (HF `gated:false`); SmokeyNet ONNX qua Dockerfile `sagecontinuum/sage-smoke-detection` (repo đã đổi org, phải tra `api.github.com/repositories/404460520` mới ra full_name/branch đúng — URL cũ trong research trước đã 404) tải trực tiếp `https://s3-west.nrp-nautilus.io/smokeynet/model.onnx` (public S3 bucket, không form); FIRESENSE Zenodo record `836749` xác nhận `access_right=open`, `license=cc-by-4.0`, 820MB; PYRONEAR-2025 xác nhận KHÔNG nằm trên HF — data thật ở Google Drive folder (repo `joseg20/wildfires2025` trỏ tới, tên paper trong repo khác tên arXiv nhưng cùng domain/tác giả/mô tả — suy luận hợp lý là bản title cũ, chưa 100% chắc chắn) — `gdown` list folder thành công không cần auth (không "Request access" trong HTML), coi là ungated hợp quy tắc user.
- **L0 error-slice trên winner (mới, code giữ lại):** thêm subcommand `temporal_eval.py error-slice` (bbox area/short-side vs confidence, offset-band AUROC, per-camera AUROC, FP audit candidates, visibility cross-reference) — chạy trên `rfdetr_large_dfire` dev+final. Kết quả chính: correlation area-vs-confidence yếu tuyến tính (Pearson 0.19 dev/0.065 final) NHƯNG bucket mean-confidence tăng đơn điệu rõ theo area (0.39→0.69 dev qua 6 bucket) — xác nhận định lượng tiny-object vẫn là nguyên nhân miss chính cho winner cụ thể (không chỉ cho detector cũ như trước). 26% positive frame dev hoàn toàn không có detection (conf<0.05). Per-camera AUROC: 107/143 camera dev có đủ nhãn 2 phía, median 0.876, ~10 camera chronic thấp (0.5-0.6) — không phải nhiễu ngẫu nhiên theo từng fire. Restore `human_review_labels.json` từ git history (`14ef54a4^`) để cross-reference visibility (30/40 sequence khớp dev). Chạy thêm `diagnose` (subcommand có sẵn) trên winner dev lần đầu tiên: 53/359 sequence low-AUROC (33 silent/13 confused/7 other) — cải thiện rõ so baseline cũ (207/509).
- **NE4 error-slice D-Fire (throwaway script, không giữ trong repo, chỉ giữ JSON artifact):** bbox area/short-side histogram theo class trên toàn bộ D-Fire train/valid/test. Kết quả: median short-side smoke ~29-31% chiều rộng ảnh, fire ~5.5% — cách xa ngưỡng vendor near-field (1.1-1.6%) 20-27 lần và xa cả median FIgLib far-field (0.735% area). **Kết luận NE0/NE4: D-Fire KHÔNG phải tiny-object domain — không copy ưu tiên P2/tiny-object của FIgLib sang near-field.** Artifact: `artifacts/smoke_fire_detection/ne4_dfire_scale_histogram.json`.
- **SOTA anchor far-field (Gói A):**
  - Tải `pyronear/yolo11s_rapid-raccoon_v8.1.0` (best.pt, ungated, README/manifest xác nhận `imgsz=1024`, `single_cls=true`, train trên `pyronear/pyro-dataset` — cùng leakage-caveat như `pyronear_yolov8s` cũ). Chạy `eval.py detector-cache --backend yolo --class-names smoke --imgsz 1024` trên FIgLib dev — đang chạy nền (`run_in_background`), ~28,361 frame, ETA ~1-1.5h.
  - Tải SmokeyNet `model.onnx` (222MB), viết mới `tasks/smoke_fire_detection/smokeynet_cache.py` (onnxruntime+Pillow, giữ tách khỏi eval.py vì input 2-frame tile-classifier khác hẳn box-detector abstraction) — replicate đúng preprocessing gốc (`sagecontinuum/sage-smoke-detection/src/inference.py`: resize 1392×1856 → crop 1040 dòng dưới → tile 224×224 overlap 20 → 45 tile/frame → normalize (x/255-0.5)/0.5 → input float64 batch×45×2×3×224×224). Pilot 5 frame verify đúng sau khi sửa dtype float32→float64 (lỗi ONNX INVALID_ARGUMENT ban đầu).
    - **Phát hiện chi phí:** CPU-only (không có onnxruntime-gpu build cho aarch64+CUDA13 qua pip; `onnxruntime-gpu` không có wheel phù hợp) — latency thật ~20-24s/frame trên 20-core CPU (ORT đã dùng hết multi-thread mặc định). Full dev set (~27.6k pair) sẽ mất ~7 ngày — vi phạm ràng buộc thời gian user. **Quyết định: chạy subsample 30 sequence** (chọn bằng `random.Random(20260707).sample` trên `dev_sequence_ids` của split manifest — dùng lại seed chuẩn của dự án, không phải layer mới), ETA ~14h, khớp "qua đêm/thời gian chết" user cho phép. Caveat bắt buộc ghi trong report: SmokeyNet là reference/subsample, KHÔNG so ngang hàng cỡ mẫu với winner.
    - **Sự cố + sửa:** lần chạy đầu tiên bug 2 lớp — (1) biến `SEQ_IDS` gán từ file debug lẫn cả dòng "n dev sequences: 359" (không chỉ danh sách ID) → `--sequence-ids` bị nhiễu, phải dừng job, tách lại file sạch; (2) script gốc không gọi `out_file.flush()` (khác `eval.py` có flush mỗi frame) → sau 57 phút chạy thật (CPU 1300%) chỉ 10 dòng được ghi xuống đĩa, rủi ro mất toàn bộ tiến độ nếu job chết giữa chừng trong ca chạy 14h không giám sát. Đã dừng job, thêm `flush()` mỗi frame + `--resume` (bỏ qua frame đã có, giống `eval.py`) + progress print mỗi 5 frame, chạy lại từ đầu qua `systemd-run --user --scope` (khớp quy ước dự án cho job dài không giám sát, xem tiền lệ `giai-doan3-cache-r2.scope`).
  - FIRESENSE (Zenodo record 836749, CC BY 4.0, mở, đã tải+giải nén thật): 49 video (27 fire pos/neg, 22 smoke pos/neg, khớp cấu trúc plan) → `datasets/near_field_fire_detection/FIRESENSE/{fire_videos,smoke_videos}/{pos,neg}/*.avi`, 1.2GB. Chưa chạy detector qua video (cần trích frame — hoãn, ưu tiên PYRONEAR-2025/far-field trước).
- **Gói B — PYRONEAR-2025 data-lever:** cài `gdown`, xác nhận Google Drive folder public thật (không "Request access" trong HTML, `gdown.download_folder(skip_download=True)` liệt kê được 11,543 file không cần auth) — cấu trúc `Pyro25/images/{train,val,test}/*.jpg` + folder `videos/` + bundle tiện lợi `pyronear2025.zip` (**9.31GB**, xác nhận qua log tải thật, không phải suy đoán). Đang tải zip nền (~11-12MB/s, ETA ~13-15 phút). Chưa lọc FIgLib-source/dedup, chưa fine-tune — bước tiếp theo sau khi giải nén xong.
- **Tạo mới `tasks/near_field_fire_detection` không cần thiết** — theo đúng quyết định plan cũ (NE1-NE5 tái dùng `temporal_eval.py`), chỉ tạo `datasets/near_field_fire_detection/` cho dữ liệu mới; code phân tích vẫn ở `tasks/smoke_fire_detection/`.
- File trực tiếp tạo: `tasks/smoke_fire_detection/smokeynet_cache.py`; `artifacts/smoke_fire_detection/ne4_dfire_scale_histogram.json`; `datasets/near_field_fire_detection/FIRESENSE/` (49 file video).
- File trực tiếp sửa: `tasks/smoke_fire_detection/temporal_eval.py` (thêm subcommand `error-slice`), `CHANGELOG.md` (mục này).
- File trực tiếp phục hồi từ git history: `artifacts/smoke_fire_detection/human_review_labels.json` (từ commit `14ef54a4^`).
- File gián tiếp tạo (lệnh CLI tự ghi, đang/đã chạy): `l0_error_slice_rfdetr_large_dfire_{dev,final}.json`, `diagnose_rfdetr_large_dfire_dev.json`, `figlib_detector_cache_pyronear_yolo11s_rapid_raccoon_dev.jsonl` (đang chạy), `figlib_detector_cache_smokeynet_reference_dev_subsample30.jsonl` (đang chạy), `pyronear_yolo11s_rapid_raccoon.pt`, `smokeynet_model.onnx` (đã copy vào `artifacts/smoke_fire_detection/`).
- Chưa làm / còn treo: lọc+giải nén PYRONEAR-2025, fine-tune winner trên PYRONEAR-2025 (Gói B), chạy detector qua FIRESENSE video, cập nhật report SOTA comparison (`model_benchmark_2x2.md` mục 9) với số Pyronear yolo11s/SmokeyNet mới, cập nhật `agent_context.md`. Câu hỏi treo cho user (không tự quyết vì đụng kỷ luật "final split one-shot"): có mở lại `--split test` cho 2 candidate reference mới (Pyronear yolo11s, SmokeyNet) hay chỉ báo cáo trên dev — cần sửa `figlib_dev_winner_lock.json` allowlist nếu đồng ý.

## 2026-07-21 16:35 GMT+7 — PYRONEAR-2025 giải nén + lọc leakage + resolve class semantics, chuẩn bị fine-tune (Gói B)

- **Giải nén thật:** 145,657 file, 9.2GB (`unzip -tq` xác nhận zip gốc không lỗi). Cấu trúc thật KHÁC README trong archive (README hứa `metadata.json` mỗi video — thực tế 0 file `metadata.json` nào tồn tại, chỉ có `images/*.jpg` + `labels/*.txt` per video-folder). Split: train 470 video/59,283 ảnh, val 59/12,206, test 63/9,238 (+ folder `wtfa/` 198 file, review false-positive nội bộ Pyronear, không dùng để train).
  - **Sự cố thao tác 09:28-09:29:** lệnh giải nén bị double-background (`&`+`disown` LỒNG bên trong `run_in_background: true`) khiến harness báo "completed" khi tiến trình `unzip` thật vẫn đang chạy nền mồ côi — phát hiện qua `ps aux` thấy process còn sống, `du -sh` chỉ 956MB lúc "báo xong". Đã đặt lại wait đúng cách (`until ! ps aux | grep ...`), xác nhận xong thật ở 145,657 file/9.2GB.
- **Xác nhận nguồn:** `README.md` trong archive dẫn thẳng arXiv:2402.05349v2, tác giả Mateo Lostanlen (PyroNear) — đúng 100% paper đã cite trong `research_plan.md`, không còn nghi ngờ "có thể chỉ là bản title cũ" như research trước.
- **Lọc leakage FIgLib-source — số đo được (không phải suy đoán):** so exact-match tên video-folder với `figlib_split_manifest.json` (`sequence_ids` đầy đủ 511, cả dev+final) → **315/592 video-folder (53%) trùng thẳng sequence_id FIgLib của mình** (vd `20190924_FIRE_hp-s-mobo-c`). Xác nhận cảnh báo leakage ở `research_plan.md` mục 8 bước 12 là thật, không phải thận trọng thừa. Sau loại trừ: 277 video sạch (train 217, val 28, test 32).
  - **Caveat:** chỉ loại bằng exact-match tên folder, KHÔNG chạy dHash dedup ảnh-đối-ảnh (rủi ro tồn dư: video đổi tên nhưng cùng nguồn gốc/camera — chưa loại được). Đủ tốt cho fine-tune thử nghiệm; nếu số cải thiện sát biên quyết định, cần dedup kỹ hơn trước khi tin.
- **Resolve class semantics (không có trong doc, phải tự điều tra):** label có 2 giá trị `class_id` (`0` và `1`), tưởng là smoke/fire nhưng xem ảnh trực tiếp (2 sample, `Read` tool) — CẢ HAI đều là khói (không có lửa/flame nào), không phân biệt được bằng mắt. Verify bằng thống kê: nhóm theo prefix tên nguồn (AWF, ADF, pyronear_force, brison, courmettes, croix, valbonne, ...) → **mỗi nguồn thuần 100% một class_id duy nhất** (AWF luôn 0, brison luôn 1, ...) — kết luận: đây là artifact của pipeline gán nhãn khác nhau giữa các nguồn upstream, KHÔNG phải phân loại smoke/fire thật (giống hệt tình huống `pyro-sdis` cũ: `class_id=1` dù `nc=1`). **Quyết định: remap cả 0 và 1 → `0` (smoke)** khi convert. Dataset này không có nhãn fire/flame nào (đúng scope paper: smoke-plume only).
- **Convert + lọc (script tạm `filter_pyronear2025.py`, KHÔNG giữ trong repo — theo đúng tiền lệ `pyro-sdis`/converter khác đã trim sau khi dùng xong):** copy ảnh + remap label → `datasets/smoke_fire_detection/PYRONEAR-2025-clean/{train,val,test}/{images,labels}/` + `data.yaml` + `filter_report.json` (ghi lại rule loại trừ + rationale class remap để không mất context khi code đã xóa). Số cuối: train 32,784 ảnh/32,159 box, val 8,260/7,316, test 4,267/3,628 — quy mô ngang Pyro-SDIS (29,537 train).
- **Chưa làm (điểm dừng có chủ đích, cần user quyết vì đụng compute cost/deadline thật):** chưa convert sang COCO format (RF-DETR cần COCO, không nhận thẳng YOLO txt — `eval.py rfdetr-accuracy` dùng `rfdetr.evaluation.coco_eval.CocoEvaluator`), chưa viết training script/notebook, CHƯA chọn venue GPU (local GB10 đang bận 2 job/~14h nữa, Modal cần chi phí, Kaggle cần thao tác tay không có CLI trên server này), chưa quyết continue-train từ checkpoint winner hay train từ đầu trên D-Fire+PYRONEAR-2025-clean gộp. Hỏi user trong chat trước khi tốn compute thật.
- File trực tiếp tạo (giữ trong repo): `datasets/smoke_fire_detection/PYRONEAR-2025/` (9.2GB, raw archive giải nén — sẽ đánh giá gitignore, quá lớn để track), `datasets/smoke_fire_detection/PYRONEAR-2025-clean/` (4.6GB, dataset đã lọc+convert).
- File trực tiếp tạo (scratch, không thuộc repo): `filter_pyronear2025.py` tại scratchpad.
- File trực tiếp sửa: `CHANGELOG.md` (mục này).
- `.gitignore` không cần sửa: `datasets/` đã ignore toàn bộ từ trước (dòng gốc), 2 folder mới tự động không track — verify bằng `git status --short datasets/` (rỗng).
- Chưa cập nhật: `agent_context.md` (sẽ gộp cùng lượt cập nhật report cuối, tránh sửa nhiều lần).

## 2026-07-21 16:44 GMT+7 — User chọn Kaggle (thủ công) cho fine-tune; chuẩn bị notebook + package upload

- User trả lời `AskUserQuestion` (venue GPU cho fine-tune Gói B): chọn **Kaggle thủ công** — không dùng Modal (tránh chi phí thật) hay xếp hàng GB10 local (đang bận ~14h).
- **Điều tra API `rfdetr==1.8.3` trước khi viết notebook (đọc source package trong venv, không đoán):** xác nhận `RFDETRLarge(resolution=..., pretrain_weights=<path .pth>)` là cơ chế đúng để continue-train từ checkpoint bất kỳ (không giới hạn COCO-pretrained gốc) — khác hẳn `resume=` trong `.train()` (chỉ dùng cho full-state `.ckpt` Lightning của CHÍNH lần train này, phục hồi khi Kaggle session đứt giữa chừng). Đọc docstring `training/module_model.py`/`training/callbacks/best_model.py` xác nhận rõ 2 cơ chế tách biệt, không share state.
- **Lấy lại 2 template notebook Kaggle cũ từ git history** (`kaggle_train_dfire_rfdetr.ipynb` tại commit `4718212^`, `kaggle_train_pyro_sdis_rfdetr.ipynb` tại commit `1ebd079`) để giữ đúng convention project (shim `val→valid` bằng symlink, resume-scan full-state `.ckpt`, effective batch 16 = 2/GPU×2GPU×accum4, fp16, seed 20260707) — không tự nghĩ lại từ đầu.
- **Viết mới `tasks/smoke_fire_detection/kaggle_train_pyronear2025_rfdetr_finetune.ipynb`:** continue-train `RFDETRLarge(resolution=800, pretrain_weights=checkpoint_best_total.pth)` trên `PYRONEAR-2025-clean` (chỉ dataset mới, không gộp lại D-Fire — đúng khung "data-only lever" A12, control = winner hiện tại). **Resolution giữ 800px khớp training gốc winner** (không phải 1280 — đó là imgsz lúc eval FIgLib, khác biến). Epoch budget = 8, judgment call đầu tiên chưa validate, ghi rõ trong markdown cell. Early-stop tắt, augmentation mặc định + HorizontalFlip 0.5, không dùng FIgLib chọn checkpoint (chọn theo PYRONEAR-2025-clean val, khớp nguyên tắc toàn dự án).
- **Đóng gói cho user tải thủ công lên Kaggle:** zip `datasets/smoke_fire_detection/PYRONEAR-2025-clean/` (4.6GB, đang nén nền, `zip -1` mức nén thấp cho nhanh) → scratchpad, cần user tự copy ra khỏi máy để upload Kaggle Dataset. Checkpoint winner (`checkpoint_best_total.pth`, 130MB, không cần zip) trỏ thẳng `artifacts/smoke_fire_detection/runs/rfdetr_large_dfire/checkpoint_best_total.pth`.
- **Việc user phải tự làm (ngoài khả năng agent — không có Kaggle credential/CLI trên server):** upload 2 Kaggle Dataset (data + checkpoint), tạo notebook mới trên Kaggle UI paste nội dung file `.ipynb`, chỉnh Accelerator = GPU T4 x2, Internet = on, chạy tay, tải kết quả checkpoint về khi xong.
- **Sau khi có checkpoint mới (việc của agent, chưa làm):** copy vào `artifacts/smoke_fire_detection/runs/rfdetr_large_dfire_pyronear2025_ft/`, chạy `eval.py detector-cache` + `temporal_eval.py g0`/`compare-candidates` so với winner gốc trên FIgLib dev (không đụng final/test — vẫn theo kỷ luật one-shot).
- File trực tiếp tạo: `tasks/smoke_fire_detection/kaggle_train_pyronear2025_rfdetr_finetune.ipynb`.
- File trực tiếp tạo (scratch, không thuộc repo): `pyronear2025-clean-kaggle-upload.zip` (đang nén), `build_pyronear2025_finetune_notebook.py`.
- Còn treo: chờ user tải zip ra khỏi máy + tự chạy Kaggle; câu hỏi mở final-split (mục 16:16) vẫn chưa trả lời.

## 2026-07-21 17:36 GMT+7 — Pyronear yolo11s_rapid-raccoon_v8.1.0 xong: AUROC vượt winner có ý nghĩa thống kê (leakage-caveat)

- Cache FIgLib dev hoàn tất: 28,360/28,361 frame (1 frame lỗi rỗng quen thuộc, giống mọi candidate khác).
- `temporal_eval.py g0`: **AUROC(smoke) = 0.8695** (winner = 0.8317) → gate PASS.
- `temporal_eval.py compare-candidates` (winner vs Pyronear yolo11s, paired bootstrap 1000 resample): Δ = **+0.0378**, CI event `[0.0211, 0.0533]`, CI camera `[0.0202, 0.0556]` — **cả 2 CI không chứa 0, "candidate_better" theo đúng winner_rule đã dùng cho 13d.** Đây là kết quả thống kê thật, không phải chênh lệch ngẫu nhiên.
- **Caveat bắt buộc, không được bỏ qua khi viết report:** checkpoint này (như `pyronear_yolov8s` cũ) chưa xác minh lineage — không loại trừ khả năng đã thấy ảnh FIgLib/HPWREN lúc train (model card chỉ ghi train trên `pyronear/pyro-dataset`, không công khai). Số vượt trội có thể một phần đến từ leakage, không phải chỉ do mô hình tốt hơn thật. Vai trò: reference/upper-bound, KHÔNG thay winner chính thức (đúng nguyên tắc mục 8 bước 12 `research_plan.md` — "nếu Pyronear dẫn đầu, ghi nhận làm reference, winner chính thức chỉ chọn giữa model lineage sạch").
- Artifact: `gate_g0_auroc_pyronear_yolo11s_rapid_raccoon.json`, `compare_winner_vs_pyronear_yolo11s_dev.json`.
- SmokeyNet subsample vẫn chạy nền (174/2366 pair lúc ghi mục này).
- File gián tiếp tạo: `figlib_detector_cache_pyronear_yolo11s_rapid_raccoon_dev.jsonl(.errors.json)`, `gate_g0_auroc_pyronear_yolo11s_rapid_raccoon.json`, `compare_winner_vs_pyronear_yolo11s_dev.json`.
- File trực tiếp sửa: `CHANGELOG.md` (mục này).

## 2026-07-21 17:46 GMT+7 — `results_dashboard`: thêm backend video-demo trên server SSH, chuyển frontend sang 4 tab, phát hiện+sửa lỗi gitignore chặn toàn bộ source

- Bối cảnh: user yêu cầu (lượt riêng, không liên quan track benchmark 2×2 chính) chuyển `tasks/results_dashboard/` từ site tĩnh (chạy được cả local) sang: (1) demo chạy model thật trên **video** thay vì ảnh, (2) chia tab theo chức năng thay vì 1 page dài, (3) giữ ảnh benchmark cũ (vì cache loser đã xóa, không tái tạo được video đa-model), (4) chạy exclusively server SSH (Windows local host model để sau). Câu hỏi phụ về VRAM multi-model: đã trả lời trong chat (mỗi model resident chiếm phần riêng, cộng dồn theo process; GB10 unified memory ~128GB nên không đáng lo ở quy mô 3 model hiện tại).
- Lập plan trước (không code), user duyệt nguyên plan, thực thi trong lượt tiếp theo.
- **Backend mới `tasks/results_dashboard/backend/`:** FastAPI+uvicorn, tái dùng `tasks/smoke_fire_detection/eval.py:load_backend` để load YOLO/RF-DETR (không viết lại logic infer). `models.py` đăng ký cứng 3 candidate demo video (`rfdetr_large_dfire`, `yolo26n_dfire_control`, `pyronear_yolo11s_rapid_raccoon` — path checkpoint verify tồn tại thật trên đĩa trước khi hardcode). `videos.py`: catalog FIgLib = 6 sequence curated sẵn từ `frontend/public/data/frame_comparison.json` (tái dùng lựa chọn cũ, verify `frame_key` map thẳng file gốc `datasets/smoke_fire_detection/FIgLib/`); catalog FIRESENSE = quét `datasets/near_field_fire_detection/FIRESENSE/*.avi` (49 file). `render.py`: decode frame (FIgLib đọc file JPEG gốc; FIRESENSE decode qua `cv2.VideoCapture` + stride 3, ghi frame tạm ra `tempfile.mkdtemp` rồi predict bằng path — tránh mọi ambiguity BGR/RGB vì `eval.py` luôn nhận path, không nhận numpy array) → vẽ box bằng `cv2.rectangle`/`putText` (màu theo class, label lấy từ `class_map` override — QUAN TRỌNG: checkpoint `pyronear_yolo11s_rapid_raccoon` tự gán nhãn nội bộ generic `item`, nếu dùng `class_name` thẳng từ detection sẽ hiển thị sai) → pipe raw frame vào `ffmpeg` subprocess encode h264 (FIgLib 4fps timelapse, FIRESENSE 10fps) → cache `.mp4`+timeline JSON (confidence per frame) vào `backend/render_cache/`. `jobs.py`: queue 1-worker thread (GPU vốn time-slice tuần tự, không cần concurrency giả), dedup job đang chạy trùng (video,model), trả ngay nếu cache đã có sẵn (không render lại). `app.py`: mount `/api/models`, `/api/videos`, `POST /api/render`, `GET /api/render/{job_id}`, static `/media` (render_cache) + `dist/` (frontend build) cùng 1 port — 1 origin duy nhất, không cần CORS.
- **Test mới `backend/test_backend.py`** (13→21 test tổng cộng cùng exporter, không cần GPU): video catalog (FIRESENSE scan, resolve_video lỗi đúng KeyError), cache naming (sanitize `:`/`/`), job queue (submit→running→done, submit→error, dedup cache-hit) — mock `render.run_render`/`render.is_cached` bằng `unittest.mock.patch`.
- **Verify thật (không chỉ chạy test giả):** cài `fastapi`+`uvicorn[standard]`+`python-multipart` vào `.venv`; khởi động uvicorn thật port 8731; `curl` toàn bộ endpoint; `POST /api/render` thật 2 lần — FIgLib `20200911_FIRE_lp-e-mobo-c` × `yolo26n_dfire_control` (21 frame, ra mp4 960×640 h264 5.25s) và FIRESENSE `smoke_videos/pos/testpos02.818.avi` × cùng model (576 frame sau stride, mp4 960×636 10fps 57.6s) — cả 2 job hoàn tất, poll đúng trạng thái `queued→running→done`. Trích 1 frame bằng `ffmpeg` + xem bằng `Read` tool: box vẽ đúng vị trí khói trên ảnh camera fisheye, xác nhận pipeline đúng chức năng (không chỉ đúng cú pháp).
- **Frontend chuyển sang 4 tab** (`App.tsx` viết lại hoàn toàn): `Demo video` (mặc định, `VideoDemo.tsx` mới — chọn domain FIgLib/FIRESENSE, chọn video, tick tối đa 3 model, poll job, hiện video + tóm tắt đỉnh confidence với ngưỡng demo 0.5 ghi rõ KHÔNG phải operating point đã khóa của report chính), `Benchmark ảnh` (giữ nguyên `FrameComparison` cũ — thay thế việc so loser trên video vì cache loser đã xóa), `Kết quả` (gộp Overview+SourceDomain+2×2 matrix+ForestPlot+OperatingPoints), `Hiệu năng` (LatencyPareto+ResolutionSensitivity+SotaTable). Xóa CSS `.app-nav` không còn dùng.
- **Việt hóa header lẫn tiếng Anh** (giữ nguyên thuật ngữ không có bản dịch chuẩn: AUROC, VRAM, latency, operating point, source-domain): `ForestPlot`, `LatencyPareto`, `OperatingPoints`, `ResolutionSensitivity`, `SourceDomainTables`.
- `vite.config.ts` thêm `server.proxy` cho `/api`+`/media` → `http://127.0.0.1:8731` (chỉ áp dụng `npm run dev`; production build backend tự mount `dist/` nên không cần proxy).
- Build production (`tsc -b && vite build`) không lỗi type, bundle 1.05MB (cảnh báo chunk-size từ trước, không phải do lượt này). Grep bundle JS xác nhận string tab mới (`Demo video`, `FIRESENSE`, `Chạy demo`...) có trong `dist/assets/*.js` — sanity-check gián tiếp vì không có browser tool để chụp màn hình thật.
- **PHÁT HIỆN LỖI NGOÀI SCOPE, ĐÃ SỬA:** `.gitignore` dòng 7 cũ (`tasks/results_dashboard/`) chặn TOÀN BỘ thư mục — `git ls-files`/`git log --all -- tasks/results_dashboard` xác nhận **chưa từng có file nào trong thư mục này được commit**, mâu thuẫn trực tiếp với `agent_context.md` cũ ghi "source (exporter+src) đã stage vào git" (thông tin đó sai, hoặc đã bị revert bởi thay đổi gitignore sau đó — không rõ nguyên nhân gốc). Nếu không sửa, toàn bộ code viết trong lượt này (backend, VideoDemo.tsx, mọi sửa đổi) sẽ không bao giờ vào được git. Đã sửa `.gitignore` thành 5 dòng cụ thể (`frontend/node_modules/`, `frontend/dist/`, `frontend/public/data/`, `frontend/*.tsbuildinfo`, `backend/render_cache/`), verify bằng `git check-ignore -v` — source giờ trackable, generated dir vẫn ignore đúng. **CHƯA commit** (user không yêu cầu) — `git status` hiện `?? tasks/results_dashboard/` (untracked, chờ user tự review).
- Command đã chạy (rút gọn, đầy đủ xem lịch sử tool call): `.venv/bin/pip install fastapi "uvicorn[standard]" python-multipart`; `.venv/bin/python -m unittest tasks.results_dashboard.backend.test_backend tasks.results_dashboard.exporter.test_export -v`; `npx tsc --noEmit -p tsconfig.app.json`; `npm run build`; `.venv/bin/python -m uvicorn tasks.results_dashboard.backend.app:app --host 127.0.0.1 --port 8731` (nohup, vẫn đang chạy nền sau lượt này); nhiều `curl` tới `/api/models`, `/api/videos`, `POST /api/render`, `/api/render/{job_id}`; `ffprobe`/`ffmpeg -vf select` để verify output.
- File trực tiếp tạo: `tasks/results_dashboard/backend/{models,videos,render,jobs,app,test_backend}.py`, `tasks/results_dashboard/frontend/src/components/VideoDemo.tsx`.
- File trực tiếp sửa: `tasks/results_dashboard/frontend/src/App.tsx` (viết lại), `.../components/components.css` (thêm CSS tab/video-demo, xóa `.app-nav`), `.../components/{ForestPlot,LatencyPareto,OperatingPoints,ResolutionSensitivity,SourceDomainTables}.tsx` (chỉ header text), `.../vite.config.ts` (proxy), `tasks/results_dashboard/README.md` (viết lại kiến trúc), `agent_context.md` (mục results_dashboard), `.gitignore` (mục lỗi nêu trên), `CHANGELOG.md` (mục này).
- File gián tiếp tạo (lệnh tự sinh, generated/gitignore, không phải source): `tasks/results_dashboard/frontend/dist/` (npm run build), `tasks/results_dashboard/backend/render_cache/{figlib__20200911_FIRE_lp-e-mobo-c__yolo26n_dfire_control,firesense__smoke_videos__pos__testpos02.818.avi__yolo26n_dfire_control}.{mp4,json}` (2 job test thật, giữ lại làm cache demo có sẵn), `__pycache__/` dưới `backend/`+`exporter/` (từ chạy unittest), gói pip mới trong `.venv` (`fastapi`, `uvicorn`, `starlette`, `uvloop`, `websockets`, `httptools`, `python-multipart`).
- Chưa làm / còn treo (xem `README.md` mục Backlog + `agent_context.md`): Windows local qua Modal web endpoint (cần user duyệt chi phí GPU-giây trước khi triển khai); sửa `build_evaluations` để export đúng số final split; live streaming thay pre-render; SmokeyNet vào demo video (cần onnxruntime-gpu aarch64+CUDA13, hiện chưa có wheel phù hợp); user tự xem qua SSH tunnel để xác nhận layout/tương tác thật (agent không có browser tool); user tự review+commit `tasks/results_dashboard/` (hiện untracked do lỗi gitignore vừa sửa).

## 2026-07-21 18:21 GMT+7 — Review dashboard: LAN, upload/camera clip, trạng thái nghiên cứu, khôi phục ignore

- Quyết định user: dashboard tiếp tục Linux/server-only; không làm Windows/Modal; `.gitignore` phải chặn toàn bộ `tasks/results_dashboard/`.
- Mạng: server cũ chỉ bind `127.0.0.1:8731`; đã dừng PID `2224488`, chạy lại bằng transient user service `results-dashboard.service`, bind `0.0.0.0:8731`. Verify `ss`: listener mọi IPv4; curl loopback và `192.168.1.186:8731` đều HTTP 200. Link LAN: `http://<LAN-IP>:8731/`; thiết bị khác còn phụ thuộc cùng subnet/firewall TCP 8731.
- Input video: giữ FIgLib 6 sequence mặc định + FIRESENSE 49 video. Thêm raw-body upload MP4/WebM/MOV/AVI/MKV, giới hạn 250 MiB, filename/path traversal guard, OpenCV validate frame/container, metadata persistent trong `render_cache/uploads/`.
- Camera laptop: thêm `getUserMedia` + `MediaRecorder`; quay clip WebM rồi upload/render. Không gọi là live stream. UI cảnh báo rõ: camera browser cần HTTPS hoặc `localhost`; HTTP qua IP LAN thường bị chặn camera, upload vẫn dùng được.
- Render: nguồn FIRESENSE/upload dùng chung decoder; lấy mỗi 3 frame; output FPS theo source FPS/3, cap 1–30. FIgLib giữ timelapse 4 FPS.
- Kết quả UI: thêm `Trạng thái nghiên cứu` dạng collapsed details; headline gọn, nội dung gồm benchmark PASS, final PASS, operating-point replay vượt FA budget, resolution/L0/spatial diagnostics, legacy run thất bại→fresh native, Pyro-SDIS audit, Pyronear YOLO11s leakage-caveat, PYRONEAR-2025/SmokeyNet/FIRESENSE/backlog.
- Caveat mới hiển thị mặc định dạng badge: 20 epoch + 1 seed; chưa chứng minh hội tụ công bằng, iso-compute, ranking ổn định qua seed. Không đổ nội dung dài trực tiếp ra view supervisor.
- Exporter sửa lỗi: `gate_*_final.json` nay thành `split=final, variant=primary`; suffix spatial `_dev/_final` parse đúng. `--skip-frames` giữ `frame_comparison.json` hiện có thay vì xóa trắng. Thêm Pyronear YOLO11s reference metadata.
- Verify unit: 25/25 pass, không GPU: `.venv/bin/python -m unittest tasks.results_dashboard.backend.test_backend tasks.results_dashboard.exporter.test_export -v`.
- Verify export: trước/sau đều 6 frame-comparison sequence; final export đúng winner `0.834729`, control `0.755121`, Pyronear YOLOv8s `0.848560`.
- Verify build: `npm run build` pass 2 lần (trước/sau edge-case fix); bundle cuối `index-Y-WPjgV5.js` 1.062 MB, chỉ cảnh báo chunk >500 kB.
- Verify upload/render thật: upload cache FIgLib 270 KiB thành `smoke-test.mp4`; OpenCV đọc 21 frame/4 FPS/5.25s; render `yolo26n_dfire_control` xong 7 sampled frame; output 960×640, 4/3 FPS, 5.25s; bundle production qua IP LAN chứa string camera/research status.
- Giới hạn còn thật: chưa click/nhìn layout bằng browser; chưa test camera permission trên laptop vật lý; service systemd có warning thiếu inotify watch descriptor nhưng vẫn active/running.
- Command đọc/diagnostic: `sed -n ... agent_context.md`; `uname -a`; `git status --short`; `rg --files tasks/results_dashboard`; `ps -ef | rg ...`; `ip -4 -brief address show`; `rg -n ... model_benchmark_2x2.md research_plan.md`; `nl -ba`/`sed` các file dashboard; `find artifacts/... -name 'gate_g0_auroc_*.json'`; `git check-ignore -v`; `ss -ltnp`; `systemctl --user status`; `journalctl --user-unit=results-dashboard.service`.
- Command sửa: wrapper `apply_patch` thử 2 lần nhưng fail do `bwrap: loopback: Failed RTM_NEWADDR`; chuyển sang `git apply --unidiff-zero --check` + `git apply --unidiff-zero`. Một số patch thử đầu lỗi `No valid patches`/`corrupt patch`, không đổi file.
- Command generated/run: `.venv/bin/python tasks/results_dashboard/exporter/export.py --skip-frames`; `npm run build`; `kill 2224488`; `nohup ... uvicorn --host 0.0.0.0 --port 8731` (child bị harness dừng, log rỗng); `systemd-run --user --unit=results-dashboard --working-directory=... .venv/bin/python -m uvicorn ... --host 0.0.0.0 --port 8731`; `curl` GET `/api/videos`, GET `/api/models`, POST `/api/videos/upload`, POST/GET `/api/render`; `ffprobe`; `jq`.
- File trực tiếp tạo: `tasks/results_dashboard/frontend/src/components/VideoInput.tsx`, `tasks/results_dashboard/frontend/src/components/ResearchStatus.tsx`.
- File trực tiếp sửa: `tasks/results_dashboard/backend/{app.py,videos.py,render.py,test_backend.py}`; `tasks/results_dashboard/exporter/{export.py,test_export.py,candidates_meta.json,caveats_registry.json}`; `tasks/results_dashboard/frontend/src/{App.tsx,theme.css}`; `tasks/results_dashboard/frontend/src/lib/theme.ts`; `tasks/results_dashboard/frontend/src/components/{VideoDemo.tsx,Overview.tsx,ComparisonMatrix.tsx,components.css}`; `tasks/results_dashboard/README.md`; `agent_context.md`; `CHANGELOG.md`.
- File trực tiếp chạm rồi khôi phục đúng nội dung user muốn: `.gitignore` — final giữ `tasks/results_dashboard/`; không còn diff Git.
- File gián tiếp cập nhật bởi exporter: `tasks/results_dashboard/frontend/public/data/{candidates,caveats,comparisons,evaluations,frame_comparison,manifest,operating_points,profiles}.json`.
- File gián tiếp tạo bởi build: `tasks/results_dashboard/frontend/dist/index.html`, `dist/assets/index-CRopt_Yn.css`, `dist/assets/index-Y-WPjgV5.js`, `dist/data/{candidates,caveats,comparisons,evaluations,frame_comparison,manifest,operating_points,profiles}.json`; Vite dọn bundle hash cũ trong `dist/assets/`.
- File gián tiếp tạo bởi upload/render: `tasks/results_dashboard/backend/render_cache/uploads/a7f9dc4fd50b4103bf049285e67c53f6.mp4{,.json}`; `render_cache/upload__a7f9dc4fd50b4103bf049285e67c53f6.mp4__yolo26n_dfire_control.{mp4,json}`; `__pycache__/` dưới backend/exporter.
- File scratch ngoài repo: `/tmp/results_dashboard_8731.log`, `/tmp/results_dashboard_upload_response.json`.
- Git final: `tasks/results_dashboard/` bị ignore toàn bộ, không hiện untracked; không commit. Artifact benchmark/SmokeyNet đang đổi từ job khác: không chạm, không gom vào dashboard.

## 2026-07-21 18:49 GMT+7 — Dashboard: giảm registry video còn 2 model, benchmark ảnh 2×3, glossary

- Xác minh semantics trước sửa: `MODEL_CONFIGS` là registry lazy-load; khởi động backend không nạp weight GPU. `get_predictor()` chỉ nạp model khi gọi lần đầu, rồi giữ trong `_loaded` đến khi restart. Trước sửa API báo RF-DETR + YOLO26n `resident=true`, Pyronear `false`.
- Quyết định user: bỏ YOLO26n khỏi model có thể chọn ở demo video; giữ RF-DETR-L winner + Pyronear YOLO11s reference.
- Backend: xóa `yolo26n_dfire_control` khỏi `MODEL_CONFIGS`; thêm test khóa exact registry 2 model. Không xóa checkpoint/cache/artifact YOLO26n; control vẫn cần trong benchmark tĩnh và lịch sử thí nghiệm.
- Frontend demo: `MAX_SELECTED_MODELS` 3→2; text/UI tự hiển thị tối đa 2.
- Benchmark ảnh: dữ liệu hiện có đúng 6 candidate; CSS desktop cố định `repeat(2, minmax(0, 1fr))` → 2 cột×3 hàng; màn ≤760px tự về 1 cột.
- Glossary: component collapsed toàn cục ngay dưới tab bar; giải thích CI/CI95, CI Δ chứa 0, bootstrap, AUROC, mAP50/mAP50–95, P/R/F1, FA/h, TTD, operating point, N-of-M/EMA, FPS/latency p50/p95, VRAM/resident, dev/final, seed, bbox/IoU, in/out-of-domain, SOTA.
- Clutter: glossary mặc định đóng; chỉ một dòng `Thuật ngữ và viết tắt — CI, CI95, AUROC...` trên mọi tab.
- README đồng bộ: 2 model lazy-load; YOLO26n chỉ benchmark tĩnh; sửa yêu cầu weight 3→2; bỏ backlog parser final đã lỗi thời.
- Verify test: `.venv/bin/python -m unittest tasks.results_dashboard.backend.test_backend tasks.results_dashboard.exporter.test_export -v` → 26/26 pass.
- Verify build: `npm run build` pass; Vite tạo `index-wicbI3Xy.js` 1.065 MB + `index-BakVL-fH.css` 11.37 kB; cảnh báo chunk >500 kB như trước.
- Restart: `systemctl --user restart results-dashboard.service`; service active, PID mới `2304032`, RSS ~76 MiB.
- Verify API sau restart: đúng 2 model; cả RF-DETR và Pyronear đều `resident=false`. PID dashboard không xuất hiện trong danh sách compute-app GPU của `nvidia-smi` → model VRAM cũ đã giải phóng.
- Verify production qua LAN IP: HTML trỏ bundle mới; JS chứa `tối đa 2`, `Thuật ngữ và viết tắt`, `CI / CI95`; CSS chứa lưới 2 cột và glossary.
- Command đọc/diagnostic: `sed -n agent_context.md`; `sed`/ `rg` dashboard source/README/CSS; `systemctl --user status results-dashboard.service`; `curl /api/models`; `jq .candidate_ids_shown`; `nvidia-smi --query-compute-apps`; `ps`; `find dist`; `git diff --check`; `git check-ignore`; `git status --short`.
- Command sửa: wrapper `apply_patch` fail do `bwrap: loopback: Failed RTM_NEWADDR`; dùng `git apply --unidiff-zero --check` + `git apply --unidiff-zero`.
- Command run/generated: unit test command trên; `npm run build`; theo dõi build bằng `ps` + `timeout 30 tail --pid=2302656 -f /dev/null`; `systemctl --user restart results-dashboard.service`; curl HTML/JS/CSS/API; `nvidia-smi`.
- File trực tiếp tạo: `tasks/results_dashboard/frontend/src/components/Glossary.tsx`.
- File trực tiếp sửa: `tasks/results_dashboard/backend/{models.py,test_backend.py}`; `tasks/results_dashboard/frontend/src/{App.tsx}`; `tasks/results_dashboard/frontend/src/components/{VideoDemo.tsx,components.css}`; `tasks/results_dashboard/README.md`; `agent_context.md`; `CHANGELOG.md`.
- File gián tiếp tạo/cập nhật bởi build: `tasks/results_dashboard/frontend/dist/index.html`; `dist/assets/{index-wicbI3Xy.js,index-BakVL-fH.css}`; `dist/data/{candidates,caveats,comparisons,evaluations,frame_comparison,manifest,operating_points,profiles}.json`; `frontend/{tsconfig.app.tsbuildinfo,tsconfig.node.tsbuildinfo}`; Vite xóa bundle hash cũ trong `dist/assets/`.
- File gián tiếp cập nhật bởi test: `tasks/results_dashboard/backend/__pycache__/`, `tasks/results_dashboard/exporter/__pycache__/`.
- Git: toàn bộ `tasks/results_dashboard/` vẫn bị `.gitignore` chặn theo yêu cầu; không commit; không chạm artifact/job nghiên cứu khác.
