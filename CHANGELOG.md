- **2026-07-14 07:57:04 +0700**
  - **Task:** poll Modal training `yolo26x_pyro_sdis_budget9`; xác minh resume/checkpoint.
  - **Result:** container `ta-01KXERQ6TWZND3DTZS7A4W5N8R`; epoch `4/20`, batch `4095/7385`; L4; batch `4`; imgsz `1280`.
  - **Checkpoint:** `last_resume.pt` epoch 3; optimizer/EMA/scaler/scheduler/train args/updates đủ; ngắt giữa epoch 4 sẽ lặp epoch 4.
  - **Files trực tiếp:** `agent_context.md`; `CHANGELOG.md`.
  - **Files gián tiếp:** Modal log/training tiếp tục trên Volume; không stop/redeploy/sửa Volume.

- **2026-07-14 09:40:19 +0700**
  - **Task:** trim code theo user duyệt; giữ training/resume/inference weight.
  - **Code:** bỏ D-Fire, hard-negative, tiling/motion probes, smoke/resume-gate, CPU preflight, staging cache/direct-copy, checkpoint manifest/hash/snapshot; giữ `train_segment`, full-state `last_resume.pt`, `best.pt`/`last.pt`, Pyro/FIgLib eval.
  - **Sửa:** Modal evaluate mặc định budget9/best.pt + Pyro val + imgsz 1280; evaluator hỗ trợ YAML trỏ thư mục ảnh.
  - **Test:** syntax 6/6; converter 3/3; parser; Modal import; segment args; directory split; unused import; diff check PASS.
  - **Files trực tiếp:** `tasks/smoke_fire_detection/dataset.py`; `eval.py`; `temporal_eval.py`; `train.py`; `modal_app.py`; `agent_context.md`; `CHANGELOG.md`.
  - **Files gián tiếp:** xóa Python bytecode cache `tasks/smoke_fire_detection/__pycache__`; temp test dirs tự xóa.
  - **Modal:** không deploy; không stop/poll container; không sửa Volume/checkpoint/weight.

- **2026-07-14 09:45:00 +0700**
  - **Task:** rút gọn tài liệu; xóa timeline và trạng thái outdated; giữ snapshot run cuối, checkpoint contract, code trim, next step.
  - **Commands:** `Get-Content agent_context.md -Encoding utf8`; `Select-String CHANGELOG.md`; `Get-Date`; `apply_patch`.
  - **Files trực tiếp:** `agent_context.md`; `CHANGELOG.md`.
  - **Files gián tiếp:** không có.

- **2026-07-14 09:59 +0700**
  - **Task:** trả lời câu hỏi kế hoạch (đọc research_plan/research_toolbox/agent_context, không đổi plan); dọn cache tự sinh theo quy tắc tối giản.
  - **Commands:** `nvidia-smi -L`; `ls tasks/smoke_fire_detection/`; `rm -rf tasks/smoke_fire_detection/__pycache__`.
  - **Files trực tiếp:** xóa `tasks/smoke_fire_detection/__pycache__/`; append `CHANGELOG.md`.
  - **Files gián tiếp:** không có.

- **2026-07-14 10:34 +0700**
  - **Task:** đồng nhất số liệu FIgLib giữa các tài liệu; tạo split dev/final; bỏ hash lock + cost ledger theo quyết định user; thêm rule chống complexity layer + rule static/volatile doc; sửa dẫn chiếu hỏng trong research_plan.
  - **Commands:** `.venv/bin/python tasks/smoke_fire_detection/dataset.py figlib --data-root datasets/smoke_fire_detection/FIgLib --index-out artifacts/smoke_fire_detection/figlib_index.jsonl --audit-out artifacts/smoke_fire_detection/figlib_audit.json --manifest-out artifacts/smoke_fire_detection/figlib_split_manifest.json`; grep/đọc `dataset.py`, `pyro_sdis_audit.json`.
  - **Kết quả verify:** 511 folder, 510 sequence có frame hợp lệ, 143 camera-string, 40362 frame; camera 136→143 do parser fallback brand-token cho 71 folder dạng dash (parser change, không phải data change); split camera-disjoint seed 20260707: dev 359 seq/108 cam, final 152 seq/35 cam, overlap 0; 13a gate PASS khớp audit (`invariant_differences={}`).
  - **Files trực tiếp:** `CLAUDE.md` (thêm rule mục 5 + mục 10); `tasks/smoke_fire_detection/docs/research_plan.md` (cập nhật số liệu, bỏ hash lock/SHA shard/cost ledger, sửa toàn bộ dẫn chiếu agent_context/artifact chết, đánh dấu 13a done, human review done); `agent_context.md`; `CHANGELOG.md`.
  - **Files gián tiếp:** `artifacts/smoke_fire_detection/figlib_index.jsonl`, `figlib_audit.json`, `figlib_split_manifest.json` (script tự sinh).
  - **Modal:** không deploy/stop/poll; không đụng Volume/checkpoint.

- **2026-07-14 11:12 +0700**
  - **Task:** bỏ hoàn toàn budget cap $20/free-quota-stop-rule theo yêu cầu user (không giữ constraint chi phí train nào); giải thích lại cascade/multi-head/Frigate bằng ngôn ngữ dễ hiểu (chỉ trả lời chat, không đổi code/plan cho phần này).
  - **Files trực tiếp:** `tasks/smoke_fire_detection/docs/research_plan.md` (xóa dòng "Chi phí" 13, sửa 13c/13e/kiểm-thử-bắt-buộc bỏ chữ "budget"/"cap"); `agent_context.md` (xóa dòng Budget); `CHANGELOG.md`.
  - **Files gián tiếp:** không có.

- **2026-07-14 13:16:08 +0700**
  - **Task:** phản biện hệ thống nhận diện gần–xa; sửa hiểu lầm FIgLib `60s`/Modal/GPU/RQ1; research distillation, small-object architecture, dynamic/coarse-to-fine/temporal inference; bổ sung architecture ablation plan.
  - **Kết quả:** A/B/C đổi thành taxonomy A–I; gần–xa đổi thành scale/visibility continuum; `13d` xác nhận chưa có winner; distillation phân loại theo inference graph; thêm S0–S3 deployment envelope, G4 architecture, E8a detector KD.
  - **Architecture:** inventory A0–A13; ưu tiên data-scale control → P2/stride-4 → P2↔P5 compute-match → adaptive ROI; kernel/neck/head/loss/formulation/temporal/routing/KD; ladder L0–L4; iso-config + iso-cost; scale/event/cost metric; pass/stop rule.
  - **Research web:** paper gốc FPN, PANet, EfficientDet/BiFPN, TridentNet, SAHI, AutoFocus, QueryDet, DynamicDet, ScaleKD, Localization Distillation, Focal Loss, ATSS, Dynamic Head, FGFA, MEGA, SmokeyNet, PYRONEAR.
  - **Commands:** `sed -n '1,240p' agent_context.md`; `uname -a`; `git status --short`; `sed` research plan/toolbox + `tail CHANGELOG.md`; `wc -l`; `git diff`; `nl -ba`; `rg`; `apply_patch`; `git diff --check`; `TZ=Asia/Ho_Chi_Minh date`.
  - **Verify:** Linux ARM64 detected; `git diff --check` PASS; doc-only, không chạy Python/model/test; không poll/stop/deploy Modal.
  - **Files trực tiếp:** `tasks/smoke_fire_detection/docs/research_plan.md`; `agent_context.md`; `CHANGELOG.md`.
  - **Files gián tiếp:** không có.

- **2026-07-14 15:05:55 +0700**
  - **Task:** audit lineage 3 model (`pyronear/yolov8s`, `dfire_yolo26n_baseline_full_vram`, `yolo26x.pt`/Pyro-SDIS fine-tune). Chỉ research + đọc git history, không Modal/GPU/benchmark.
  - **Kết quả chính:** claim cũ trong research_plan.md ("Pyronear train trên pyro-sdis, không dính HPWREN/FIgLib") SAI/không có nguồn — model card HF + repo `pyro-vision` + paper PYRONEAR-2025 đều không công bố dataset train của checkpoint `pyronear/yolov8s`. Theo decision rule: lineage không sạch → loại Pyronear khỏi toàn bộ luồng so sánh (kể cả zero-shot control).
  - **D-Fire (`dfire_yolo26n_baseline_full_vram`):** khôi phục từ git history (`args.yaml` tại commit `ceee019`, script gốc tại `02c7a59`/`faeff13^`) — model `yolo26n.pt` (base COCO), fine-tune D-Fire (`gaiasd/DFireDataset`, nguồn Belo Horizonte fire simulation + UFMG/Serra Verde surveillance + Internet, không liên quan HPWREN/FIgLib/Pyro-SDIS), imgsz `640`, seed `20260707`, val carved 10% từ train gốc (stratified theo label type), test = D-Fire test gốc giữ nguyên, augmentation Ultralytics default (mosaic 1.0, hsv/translate/scale/fliplr default, không override).
  - **YOLO26x:** base `yolo26x.pt` pretrain COCO 640×640 (Ultralytics official, MuSGD, batch 128) — xác nhận qua `docs.ultralytics.com/models/yolo26` + arXiv 2606.03748; fine-tune Pyro-SDIS đúng theo research_plan hiện có (imgsz 1280/epochs 20/patience 5/seed 20260707/batch 4), augmentation cũng Ultralytics default (train.py không override) — cùng augmentation policy với D-Fire, khác imgsz/epoch/patience theo domain.
  - **Fairness gap phát hiện thêm:** Pyro-SDIS chỉ có train/val (không test riêng) trong khi D-Fire có train/val/test — YOLO26x chọn epoch VÀ report Pyro val cùng một split; D-Fire baseline train imgsz 640 vs YOLO26x imgsz 1280 — so sánh tuyệt đối giữa 2 model nội bộ bị confound bởi resolution nếu không tách riêng theo domain.
  - **Web research:** `huggingface.co/pyronear/yolov8s` (model card + raw README + file list), `huggingface.co/datasets/pyronear/pyro-sdis`, `github.com/pyronear/pyro-vision`, arXiv 2402.05349 (PYRONEAR-2025), `github.com/gaiasd/DFireDataset`, `docs.ultralytics.com/models/yolo26`, arXiv 2606.03748.
  - **Commands:** `find`/`grep` D-Fire/yolo26x refs; `git log --all --oneline`; `git log --all --diff-filter=A --name-only`; `git show <hash>:<path>` (args.yaml, train_dfire_yolo.py, dfire_baseline_report.md); `git log -p -- dataset.py` cho split D-Fire; `TZ=Asia/Ho_Chi_Minh date`.
  - **Files trực tiếp:** `tasks/smoke_fire_detection/docs/research_plan.md` (sửa 3 chỗ: lineage check mục 12, baseline 13b, dev candidate 13d — bỏ Pyronear); `CHANGELOG.md`.
  - **Files gián tiếp:** không có. Không tạo file mới/artifact/hash/manifest; không chạy Modal/GPU/benchmark.

- **2026-07-14 15:35:47 +0700**
  - **Task:** user override quyết định lineage: giữ Pyronear trong so sánh (leakage-caveat thay vì loại); ghi nhận kế hoạch relabel D-Fire; verify lại seed 2 model theo chất vấn user.
  - **Verify seed:** D-Fire — `args.yaml` của run thật (git `ceee019`) ghi `seed: 20260707`, `deterministic: true`; YOLO26x — `train.py` dòng 491 hard-gate `ValueError` nếu `seed != 20260707`, khớp poll log 07:57. Chưa đọc `train_args` trong checkpoint budget9 trên Modal Volume (ngoài scope, không đụng Modal) — bằng chứng là code gate + poll log, không phải checkpoint trực tiếp.
  - **Sửa plan:** mục 12 lineage check — đổi decision từ "loại Pyronear" sang "giữ, mọi số FIgLib/Pyro-SDIS mang leakage-caveat, trục so ít rủi ro nhất là D-Fire test"; 13b — thêm lại Pyronear vào baseline cache; 13d — Pyronear vào dev nhưng không được khóa winner từ số FIgLib khi lineage chưa xác minh, final chạy với vai trò reference; mục 8.1 — thêm ghi nhận kế hoạch relabel D-Fire + ràng buộc so sánh (FIgLib axis so được, D-Fire mAP axis vỡ nếu đổi test label, closed results giữ làm control lịch sử, kỳ vọng gain không nằm ở scale-gap).
  - **Commands:** `git show ceee019:...args.yaml | grep seed`; `sed -n '488,497p' train.py`; `TZ=Asia/Ho_Chi_Minh date`; `git diff --check` (PASS).
  - **Files trực tiếp:** `tasks/smoke_fire_detection/docs/research_plan.md` (4 chỗ: mục 12, 13b, 13d, 8.1); `CHANGELOG.md`.
  - **Files gián tiếp:** không có. Không tạo file mới; không chạy Modal/GPU/benchmark.

- **2026-07-14 16:00:22 +0700**
  - **Task:** trả lời phản biện tiếp theo của user — (1) số mAP D-Fire 0.684 đang so với cái gì, (2) relabel thực ra chỉ <1% ảnh, (3) plan đã có candidate model family ngoài YOLO (RF-DETR...) chưa, (4) giải thích rõ rủi ro imgsz zero-shot của Pyronear.
  - **Kết quả:** xác nhận 0.684 KHÔNG bị khóa vào gate/thống kê nào khác trong plan (G0/G1 dùng AUROC từ detector cache, độc lập label D-Fire) — sửa lại framing "mất tính so sánh" trước đó, quá thận trọng cho quy mô <1%; relabel giờ ghi nhận đơn giản hơn (report song song 2 mAP, không cần đóng băng test); xác nhận **plan hiện KHÔNG có candidate model family ngoài YOLO** ở track chọn winner (13a-13e) — transformer chỉ xuất hiện như module thay thế nội bộ (A3/L4), sau G4; thêm gap note.
  - **Không sửa thêm gì về Pyronear imgsz** (câu hỏi 4 chỉ yêu cầu giải thích thêm trong chat, không đổi plan).
  - **Commands:** `git diff --check` (PASS); `TZ=Asia/Ho_Chi_Minh date`.
  - **Files trực tiếp:** `tasks/smoke_fire_detection/docs/research_plan.md` (mục 8.1 sửa lại relabel framing; mục 13 thêm gap note model family); `CHANGELOG.md`.
  - **Files gián tiếp:** không có. Không tạo file mới; không chạy Modal/GPU/benchmark.

- **2026-07-14 16:41:12 +0700**
  - **Task:** verify thật imgsz train của `pyronear/yolov8s` (câu hỏi user: "có cách nào kiểm tra rồi ghi vào không") — không cần Modal/GPU, chỉ đọc metadata file model đã export.
  - **Commands:** tải `yolov8s.onnx` (44.5MB) từ `huggingface.co/pyronear/yolov8s` về scratchpad session (ngoài repo); `.venv/bin/pip install onnx` (tạm thời, không ghi vào `requirements.lock`); `onnx.load(...).metadata_props`; `.venv/bin/pip uninstall -y onnx` + xóa file scratchpad ngay sau khi đọc xong (dọn sạch, không để lại dependency/artifact thừa).
  - **Kết quả:** metadata Ultralytics export xác nhận `imgsz=[1024,1024]`, `names={0:'smoke'}`, `stride=32`, `date=2024-05-30`, `description` trỏ path nội bộ `pyronear-mlops/data/03_model_input/yolov8/full/datasets/data.yaml`. Phát hiện thêm: ngày export (2024-05-30) **sớm hơn** ngày `pyro-sdis` tự nhận "full release January 2025" trên dataset card → không thể đồng nhất tuyệt đối checkpoint này với bản `pyro-sdis` HF hiện tại (nhiều khả năng cùng nguồn camera nhưng khác snapshot). Không đổi kết luận leakage risk (vẫn chưa loại trừ HPWREN/ALERTWildfire), chỉ thay giả thuyết imgsz từ "không rõ" thành "1024 xác nhận".
  - **Sửa plan:** mục 12 (thêm bullet verify), 13b (Pyronear cache 2 lượt `imgsz=1024` số chính + `1280` số phụ, bỏ giả thuyết 640).
  - **Files trực tiếp:** `tasks/smoke_fire_detection/docs/research_plan.md`; `CHANGELOG.md`.
  - **Files gián tiếp:** không có trong repo (file `.onnx` tải về nằm ở scratchpad ngoài project, đã xóa sau khi đọc; `onnx` package cài tạm vào `.venv` rồi gỡ ngay, không đổi `requirements.lock`).

- **2026-07-14 18:18:59 +0700**
  - **Task:** mở candidate DETR-family cho track 13d (session mới, server local DGX Spark, KHÔNG phải Modal): research RF-DETR vs RT-DETR + đo headroom GB10 + probe memory thực nghiệm. DỪNG trước khi viết code training — chờ user chốt 3 điểm (model, variant, dataset).
  - **Platform detect (không đoán):** server `ai2`, Linux 6.17.0-1014-nvidia aarch64, GPU GB10, driver 580.142, CUDA 13.0; unified memory xác nhận — torch `total_memory=121.69GiB` = toàn bộ RAM, `mem_get_info` khớp `free -h`; `nvidia-smi` không báo tổng board memory nhưng vẫn báo per-process. Env: Python 3.12.3, torch 2.12.1+cu130 (CUDA OK), ultralytics 8.4.90 (`RTDETR` import OK), tmux 3.4, `Linger=no`, `KillUserProcesses` comment (default no), disk trống 2.9T.
  - **Research RF-DETR (GitHub API + web; 3 agent research bị session-limit kill nên tự curl):** PyPI mới nhất `rfdetr==1.8.3` (wheel py3-none-any; release 2026-06-29); 3 issue resume cũ #460/#881/#998 đều closed (02–04/2026); NHƯNG fix resume fused-AdamW (#1028 → PR #1193) merge 2026-07-08 SAU release 1.8.3 → bản PyPI vẫn dính, và `fused_optimizer=True` là default trong `config.py`; #1044 (training restart Linux) + #1199 (lr/lr_encoder bị ignore v1.8.3) đang OPEN. `rfdetr[train]` kéo `pytorch_lightning>=2.6` + `transformers>=5.1` + `roboflow` + `rf100vl`. Resolution phải chia hết `patch_size×num_windows` (=56 variant chuẩn) → không train được 1280 (chỉ 1232/1288). Lineup: N/S/M/L Apache-2.0 (30.5–33.9M params, native 384–704); XL/2XL (126M) PML 1.0 — license yêu cầu platform plan Roboflow còn hiệu lực + telemetry/license-check nhúng, cấm bypass → loại cho lab local. Repo `rf-detr-plus` private.
  - **Domain evidence:** RF-DETR trong smoke/fire = 0 kết quả published (web + arXiv). RT-DETR: MDPI Fire 2024 (stock YOLOv8X vs RT-DETR-X trên wildfire smoke): YOLOv8X thắng overall AP@0.5=0.900, RT-DETR-X thắng small-object (detection rate 0.983); RT-DETR-Smoke (MDPI Fire 2025) là variant modified — tham khảo thấp. Cộng đồng ultralytics: RT-DETR AMP có thể NaN, 24GB GPU chật ở 2048px (discussion #4977).
  - **Probe memory thực nghiệm (script scratchpad `rtdetr_mem_probe.py`, ngoài repo):** RTDETR `rtdetr-x.pt` train Pyro-SDIS imgsz=1280 batch=4 AMP default, `fraction=0.02` (148 iter) + final val 4099 ảnh: CUDA max_allocated **14.89GiB** / max_reserved **16.71GiB**; VmHWM main process **7.97GiB**; delta system-used ≈ **19GiB** (30→49GiB, pool unified) → trong ngân sách 25GB, margin ~6GB. Tốc độ train **1.1 it/s** → ~1.9h/epoch → 20 epochs ≈ **~40h**; val inference 53.5ms/ảnh @1280. Loss finite sau 148 iter (AMP chưa thấy NaN — mẫu ngắn, chưa kết luận).
  - **Commands chính:** `uname -a`; `nvidia-smi -L`; `nvidia-smi`; `free -h`; `.venv/bin/pip index versions rfdetr`; `.venv/bin/pip download --no-deps rfdetr` (wheel vào scratchpad, đọc METADATA); curl GitHub API (issues/releases/CHANGELOG/config.py/detr.py repo roboflow/rf-detr); curl Semantic Scholar API (2 DOI MDPI); WebSearch (RT-DETR wildfire, PML 1.0, RF-DETR fire, ultralytics imgsz); probe chạy nền + poll `nvidia-smi --query-compute-apps`, `/proc/<pid>/status`; `TZ=Asia/Ho_Chi_Minh date`.
  - **Files trực tiếp:** `CHANGELOG.md` (entry này). Không sửa code/plan nào khác.
  - **Files gián tiếp:** `datasets/smoke_fire_detection/pyro-sdis-yolo/labels/train.cache` + `val.cache` (ultralytics tự sinh khi scan — giữ, tăng tốc run sau); `runs/detect/rtdetr_mem_probe/` (259MB, ultralytics tự sinh ở repo root — ĐÃ XÓA sau khi ghi số liệu); `rtdetr-x.pt` + `yolo26n.pt` (AMP-check) tải về scratchpad ngoài repo (session temp, tự dọn). Không cài package mới vào `.venv`.

- **2026-07-14 19:51:22 +0700**
  - **Task:** phiên mới (user rời server qua đêm, không giám sát). Verify state venv/tmux từ session trước; launch RF-DETR-L/Pyro-SDIS thật (13f); verify relabel D-Fire; viết + launch YOLO26x/D-Fire relabeled (companion, ngoài track 13c); theo yêu cầu bổ sung của user, viết + launch RF-DETR-L/D-Fire relabeled (companion thứ 3, so sánh cùng-domain với YOLO26x/D-Fire).
  - **Verify state đầu phiên:** `.venv/bin/pip show rfdetr` → version hiển thị `1.8.3` nhưng `direct_url.json` xác nhận đúng commit `e877b758c309ca749c739c477cb96f1668af0e9f` (develop), `module_model.py` có `fused_optimizer` fix — pin từ session trước còn nguyên. `tmux ls` chỉ còn `detach_test` (session test giả) → `tmux kill-session -t detach_test`. D-Fire relabel verify bằng `find datasets/smoke_fire_detection/D-Fire/{train,test}/labels -newermt "2026-07-14 00:00:00" -type f`: `125` file train + `56` file test đổi mtime hôm nay (tổng `181`), còn lại mtime gốc `~2022-04-29` — khớp claim relabel đã áp dụng.
  - **RF-DETR-L/Pyro-SDIS (13f) — LAUNCH THẬT:** tmux `train_rfdetr_pyrosdis`, `.venv/bin/python tasks/smoke_fire_detection/train_rfdetr.py` (default args = protocol khóa: resolution 1280/epochs 20/batch4×accum4/seed 20260707/patience 5). Script tự tạo shim `datasets/smoke_fire_detection/pyro-sdis-yolo-rfdetr/` (symlink thư mục + `data.yaml`). Verify: GPU util `96%` (không phải progress bar không hiện do tmux buffer, đã xác nhận qua `nvidia-smi` + `ps aux` — 1 process chính + 4 dataloader worker CPU~20% mỗi cái), `metrics.csv` log step tăng đều (49→449 qua các lần check), không traceback. Epoch 0 chưa hoàn tất tại thời điểm ghi log này (ETA ước tính ban đầu ~1h/epoch solo, nhưng do chia sẻ GPU với 2 job sau nên chậm hơn nhiều — xem ghi chú throughput dưới).
  - **YOLO26x/D-Fire relabeled — script mới `tasks/smoke_fire_detection/train_dfire_yolo26x.py`:** KHÔNG sửa `train.py` (hard-gate riêng cho 13c/Pyro-SDIS ở dòng ~489-496). Tra git history lấy hyperparameter gốc E0: `git show ceee019:artifacts/smoke_fire_detection/runs/dfire_yolo26n_baseline_full_vram/args.yaml` (model `yolo26n.pt`, batch `0.95` autobatch, imgsz `640`, patience `20`, epochs `100`, seed `20260707`, `deterministic: true`, optimizer `auto`); `git show d82bfcf:tasks/smoke_fire_detection/dataset.py` lấy lại thuật toán `cmd_dfire` đã bị trim (val 10% carve từ train, stratify theo label-type `smoke_only/fire_only/smoke_and_fire/empty`, test giữ nguyên không reshuffle) — viết lại độc lập trong script mới (`build_split`), verify determinism bằng `md5sum` 2 lần chạy giống nhau. Model `yolo26x.pt` train từ COCO pretrain gốc (tải qua Ultralytics, cache tại `~/.cache/ultralytics_weights/yolo26x.pt` — dọn khỏi repo root ngay sau khi tải để tránh rác `git status`). Batch đổi từ `0.95` (autobatch) sang **cố định `16`** — lý do: probe thực nghiệm cho thấy Ultralytics autobatch chỉ tính `total_memory - (reserved+allocated của chính process)`, không thấy được process khác (RF-DETR) đang chiếm GPU trên GB10 unified memory → rủi ro OOM chéo process khi chạy song song; probe batch=32@640 đo được `max_reserved=33.6GiB`, suy ra batch=16 ≈ 16-17GiB (verify khớp log thật `16.1G` khi train). Checkpoint/resume copy nguyên pattern `train.py` (atomic `last_resume.pt` qua callback `on_model_save`, xác nhận `save_model()` của Ultralytics chạy mỗi epoch bất kể `save_period` — đọc source `engine/trainer.py` dòng 560-717 — nên bỏ `save_period=1` để tránh sinh thêm `epochN.pt` lãng phí đĩa mà vẫn resume đúng mỗi epoch).
  - **Kill-resume test cho `train_dfire_yolo26x.py` (subset 64 train/16 val, scratchpad):** chạy 6 epoch mục tiêu, SIGKILL bằng PID trực tiếp (tránh lỗi tự sát wrapper — dùng `pgrep -f run_train.py` lấy PID cụ thể rồi `kill -9`) sau khi `last_resume.pt` xuất hiện (epoch 3 0-based = 4 epoch xong tại thời điểm kill thật, do độ trễ giữa các lệnh bash tuần tự). Resume lại: log Ultralytics `"Resuming training ... from epoch 5 to 6 total epochs"` khớp đúng `checkpoint_resume_state` tự viết (`epoch_zero_based=3` → next epoch label 5) — chạy tiếp 2 epoch còn lại, `TRAIN_FINISHED`, không restart từ 0. **PASS.**
  - **Đo headroom thật trước launch:** `nvidia-smi`, `free -h`, `torch.cuda.mem_get_info()` — lưu ý `mem_get_info()` trên GB10 phản ánh áp lực cả buff/cache (không đáng tin bằng cột `available` của `free -h`). Tại thời điểm launch YOLO26x/D-Fire: RF-DETR alone dùng `~16.2GiB` GPU, `free -h available=73Gi`/121Gi. Quyết định: chạy SONG SONG (không serial) theo đúng yêu cầu user (ưu tiên "đang chạy" hơn "đã xong"); batch=16 đưa tổng 2 job lên ~33GB GPU, an toàn so ngưỡng ~70% (85GiB) tổng 121.69GiB.
  - **YOLO26x/D-Fire — LAUNCH THẬT:** tmux `train_dfire_yolo26x`. Split thật: `train=15500 val=1721 test=4306` (khớp tổng gốc `17221` train + `4306` test). `Starting training for 100 epochs`, epoch 1 loss giảm hợp lý (box_loss 2.37→1.79 trong 59 iter đầu), GPU 16.1G khớp probe, không traceback (một số JPEG lỗi nhẹ được Ultralytics tự "corrupt JPEG restored and saved" — không phải lỗi script).
  - **User yêu cầu bổ sung (chat, không phải qua research_plan trước đó):** thêm RF-DETR-L trên D-Fire relabeled làm đối trọng so sánh cùng-domain với YOLO26x/D-Fire (lý do user nêu: "cần cái gì để so sánh với YOLO26x"). Viết `tasks/smoke_fire_detection/train_dfire_rfdetr.py` — reuse `train_dfire_yolo26x.build_split()` để dùng CHUNG split (giữ data cố định, chỉ đổi model family). Shim Roboflow-YOLO layout mới `datasets/smoke_fire_detection/dfire-relabeled-rfdetr/` — symlink PER-FILE (khác symlink thư mục của shim Pyro-SDIS, vì D-Fire train/val chung 1 thư mục gốc `train/`), verify bằng `rfdetr.datasets.yolo.is_valid_yolo_dataset()` → `True` trước khi launch. Protocol: `resolution=640` (khớp imgsz D-Fire, chia hết block-32 RF-DETR-L), `epochs=20/patience=5/batch4×accum4=16/seed=20260707` (giữ recipe chuẩn family RF-DETR như 13f, không kéo epochs=100 của YOLO26x). Verify: `nc=2 names={0:smoke,1:fire}`, số ảnh symlink khớp split (`15500/1721/4306`). KHÔNG chạy kill-resume test riêng cho run này (tái dùng nguyên code path đã proven ở 13f, chỉ đổi resolution/dataset/output-dir) — chỉ verify khởi động sạch: sanity-check validation pass (mAP 0.0 kỳ vọng, chưa train thật), không traceback.
  - **RF-DETR/D-Fire — LAUNCH THẬT:** tmux `train_dfire_rfdetr`, output `artifacts/smoke_fire_detection/runs/rfdetr_large_dfire_relabeled_gb10`.
  - **Throughput thực tế khi 3 job chạy chung 1 GPU (quan trọng, trả lời câu hỏi user):** RF-DETR/Pyro-SDIS chậm hẳn so ước tính solo — step tăng ~16.6 step/phút lúc có 2 job khác chạy cùng (so ước tính solo trước đó ~1h/epoch); ngoại suy full epoch (7384 step) ở tốc độ này ra **~7+ giờ/epoch** khi chia sẻ GPU 3 chiều qua time-slicing (KHÔNG phải parallel compute thật — 1 GPU vật lý duy nhất trên GB10). Khớp/vượt tham chiếu user nêu (Kaggle L4 solo YOLO26x/Pyro-SDIS: 142 phút/epoch) — GPU dùng chung ở đây chậm hơn hẳn 1 GPU riêng. Chấp nhận được vì mục tiêu qua đêm là "vẫn đang chạy" (resumable), không phải "xong trước sáng".
  - **Detachable xác nhận lại:** `KillUserProcesses=false` (verify D-Bus session trước) → tmux sống qua logout, user có thể tắt máy/thoát SSH ngay, không cần chờ thêm checkpoint hay thông báo nào — cơ chế resume đã kill-test PASS độc lập với việc checkpoint thật của run dài đã tới hay chưa.
  - **Commands chính:** `tmux kill-session -t detach_test`; `.venv/bin/pip show rfdetr`; `find -newermt`; `git log --all --diff-filter=A --name-only`; `git show <hash>:<path>` (args.yaml, dataset.py cmd_dfire, train_dfire_yolo.py cũ); `nproc`; `.venv/bin/python -c "..."` (probe memory, split determinism, resume test, shim verify); `mv yolo26x.pt ~/.cache/ultralytics_weights/`; `tmux new-session`/`send-keys` ×3; `nvidia-smi`; `free -h`; `torch.cuda.mem_get_info()`; `TZ=Asia/Ho_Chi_Minh date`.
  - **Files trực tiếp:** `tasks/smoke_fire_detection/train_dfire_yolo26x.py` (MỚI); `tasks/smoke_fire_detection/train_dfire_rfdetr.py` (MỚI); `tasks/smoke_fire_detection/docs/research_plan.md` (mục 8 item 1: thêm 2 bullet ghi run YOLO26x/D-Fire + RF-DETR/D-Fire đã launch, caveat 2-biến); `agent_context.md` (thêm state 3 tmux job); `CHANGELOG.md`.
  - **Files gián tiếp:** `datasets/smoke_fire_detection/pyro-sdis-yolo-rfdetr/` (shim, script tự tạo, launch thật lần đầu — trước đó chỉ có ở probe scratchpad); `datasets/smoke_fire_detection/dfire-relabeled-rfdetr/` (shim MỚI, script tự tạo, per-file symlink); `artifacts/smoke_fire_detection/dfire_relabeled_split/` (train.txt/val.txt/test.txt/dataset.yaml, script tự sinh); `artifacts/smoke_fire_detection/runs/rfdetr_large_pyro_sdis_gb10/` (checkpoint_best_ema.pth, metrics.csv — training thật); `artifacts/smoke_fire_detection/runs/dfire_yolo26x_relabeled/` (weights/, labels.jpg, args.yaml — training thật); `artifacts/smoke_fire_detection/runs/rfdetr_large_dfire_relabeled_gb10/` (training thật); `~/.cache/ultralytics_weights/yolo26x.pt` (tải mới, ngoài repo); `datasets/smoke_fire_detection/D-Fire/train/labels.cache` + `val.cache`-tương-đương (Ultralytics tự sinh); scratchpad ngoài repo (probe D-Fire memory/kill-resume, đã tự dọn `rm -rf` sau khi verify xong).
  - **Không làm:** không sửa `train.py`; không đè run cũ `dfire_yolo26n_baseline_full_vram`; không dừng/poll thêm sau khi verify khởi động sạch cho cả 3 job (theo đúng yêu cầu "launch xong thì dừng", user tự xử lý sáng mai).

- **2026-07-14 20:00:51 +0700**
  - **Task:** user hỏi throughput thật khi 3 job chạy chung GPU (đang cân nhắc chuyển sang Kaggle/Colab), yêu cầu khuyến nghị giữ 1-2 job phù hợp compute/VRAM server. Đo thật bằng `nvidia-smi pmon` (SM% từng process, không đoán) thay vì chỉ dựa GPU util tổng.
  - **Kết quả đo (3 job chạy chung):** SM share ~25-29% (RF-DETR/Pyro-SDIS, pid `1090347`), ~45-51% (YOLO26x/D-Fire, pid `1100144`), ~14-22% (RF-DETR/D-Fire, pid `1107334`) — chia KHÔNG đều (không phải round-robin công bằng), YOLO26x chiếm ưu thế nhiều nhất. Epoch time đo thật: RF-DETR/Pyro-SDIS step 349→549 trong 20.87 phút (`12:36:17`→`12:57:09`) = `9.58 step/phút` → ngoại suy full epoch (7384 step) ≈ **12.8h/epoch** (chậm hơn nhiều so ước tính subset-probe session trước `~1h/epoch` — số cũ không đáng tin bằng đo thật full-data này). YOLO26x/D-Fire đo thật gần xong epoch 1 ở `~1.9s/it` → **~30 phút/epoch**.
  - **Khuyến nghị đưa ra:** giữ RF-DETR/Pyro-SDIS (track chính 13f, đang đói GPU nhất) là ưu tiên; 2 job D-Fire là companion phụ, có thể chuyển Kaggle/Colab (RF-DETR/D-Fire nhẹ ~5.7GB VRAM @640, vừa T4/P100 free tier; YOLO26x user đã có kinh nghiệm Kaggle L4 142 phút/epoch).
  - **Quyết định user (qua AskUserQuestion):** CHỈ giữ RF-DETR/Pyro-SDIS local. Kill cả 2 job D-Fire (tiến trình mới, tiến độ không đáng kể — YOLO26x mới 68% epoch 1, RF-DETR/D-Fire mới sanity-check) — user tự chạy 2 job này trên Kaggle/Colab.
  - **Thực thi:** `tmux kill-session -t train_dfire_yolo26x`; `tmux kill-session -t train_dfire_rfdetr`; verify `pgrep -f "train_dfire_yolo26x.py\|train_dfire_rfdetr.py"` → không còn process. Verify RF-DETR/Pyro-SDIS solo: SM util tăng `25-29%→89%` (gần full GPU, khớp dự đoán ~3x), `free -h` used giảm `74Gi→47Gi` (available `73Gi`), tmux còn lại chỉ `train_rfdetr_pyrosdis`.
  - **Commands:** `nvidia-smi pmon -c 2/3`; `tmux kill-session`; `pgrep -f`; `nvidia-smi --query-compute-apps`; `free -h`; `TZ=Asia/Ho_Chi_Minh date`.
  - **Files trực tiếp:** `CHANGELOG.md`; `agent_context.md` (cập nhật state: chỉ còn 1 job local, note 2 script `train_dfire_yolo26x.py`/`train_dfire_rfdetr.py` vẫn giữ trong repo để user tự chạy trên Kaggle/Colab, không xóa).
  - **Files gián tiếp:** artifact run dở dang `artifacts/smoke_fire_detection/runs/dfire_yolo26x_relabeled/` (weights epoch 1 chưa hoàn tất, dừng giữa chừng — không xóa, không phải control lịch sử nên không cần giữ nguyên nghiêm ngặt, user có thể dọn hoặc dùng làm điểm khởi đầu nếu chạy lại local sau); `artifacts/smoke_fire_detection/runs/rfdetr_large_dfire_relabeled_gb10/` (chỉ có log sanity-check, chưa checkpoint thật); shim `datasets/smoke_fire_detection/dfire-relabeled-rfdetr/` và split `artifacts/smoke_fire_detection/dfire_relabeled_split/` giữ nguyên (tái dùng được nếu chạy lại, kể cả trên Kaggle/Colab sau khi copy dataset/script sang).
  - **Không làm:** không xóa 2 script mới (`train_dfire_yolo26x.py`, `train_dfire_rfdetr.py`) — vẫn cần cho user tự chạy trên Kaggle/Colab; không đụng RF-DETR/Pyro-SDIS đang chạy.

- **2026-07-14 19:02:28 +0700**
  - **Task:** user chốt qua AskUserQuestion: RF-DETR (không RT-DETR) + Pyro-SDIS thuần; variant theo điều kiện "2XL nếu được, không thì L" → phân tích PML 1.0 loại XL/2XL → chốt **RF-DETR-L**; version "develop@SHA nếu không thêm complexity" → phân tích: pin SHA = 1 dòng install, không block sửa/fork sau này → chốt **develop@`e877b758c309ca749c739c477cb96f1668af0e9f`** (chứa fix resume #1193; #1199 chỉ ở tầng introspection — PR #1205 xác nhận scope); recipe **batch4×accum4=16**. Sau đó verify thực nghiệm đầy đủ trước khi viết code.
  - **Sửa claim của chính session này:** "RF-DETR không train được 1280 (phải chia 56)" là SAI với lineup hiện tại — N/S/M/L dùng patch16×2windows → block 32 → 1280 hợp lệ (đọc `config.py` source; block 56 chỉ đúng Base/Large-deprecated patch14×4).
  - **Cài đặt:** `.venv/bin/pip install "rfdetr[train] @ git+https://github.com/roboflow/rf-detr.git@e877b75..."` — thành công trên aarch64; kéo pytorch_lightning 2.6.5, transformers 5.13.1, albumentations 2.0.8, faster-coco-eval 1.7.2, supervision 0.29.1, roboflow 1.3.13...; **numpy hạ 2.5.1→2.4.6** (constraint albumentations), ultralytics/torch không đổi, import OK cả hai. Verify fix #1193 hiện diện trong bản cài (`training/module_model.py:158`).
  - **Probe memory RF-DETR-L @1280 b4×acc4** (subset 400 train/120 val symlink, scratchpad): CUDA max_allocated `10.97GiB` / max_reserved `15.59GiB`, VmHWM main `5.9GiB`, footprint hệ thống ≈`18GiB` < budget 25GB. 2 epoch + 2 val = 158.8s → ước tính full data ~1h/epoch (train ~50min + val 4099 ảnh ~12min) → **~20h/20 epochs**. Layout thử đầu fail (`FileNotFoundError`) → RF-DETR đòi Roboflow layout `{split}/{images,labels}` + `data.yaml` root, KHÔNG đọc ultralytics layout `images/{split}` → dựng shim symlink.
  - **Kill-resume test (yêu cầu bắt buộc trước run dài):** baseline 4 epoch sạch (loss 15.76→9.80→8.33→8.04, mAP50-95 0.099→0.180→0.262→0.296, seed 20260707); run lặp lại bị SIGKILL toàn bộ process python giữa epoch 4 (sau checkpoint_2); resume `checkpoint_2.ckpt` → Lightning "Restored all states", tiếp đúng epoch 3 (KHÔNG restart từ 0 — không dính #688/#1044), step nối 99, loss 7.69/mAP 0.256 (EMA 0.282) — continuity đạt, không bit-exact (RNG augmentation không nằm trong checkpoint). Không crash kiểu #1028. **PASS.**
  - **Detachable:** `KillUserProcesses=false` verify qua `busctl get-property` (authoritative, không cần linger/sudo); tmux fake job `detach_test` (sleep 900) đã tạo lúc 19:02 chờ user logout test thực nghiệm — agent không tự logout được (session agent chết theo).
  - **Sai sót vận hành đã sửa trong session:** (1) watcher đầu tiên gán biến trong list bị background-hóa → check path rỗng → kill không xảy ra (train chạy hết — tận dụng làm baseline); (2) `kill -9 $(pgrep -f script)` tự sát wrapper bash (match cmdline) trước khi signal tới python — sửa bằng lọc `/proc/<pid>/exe`.
  - **Files trực tiếp:** `tasks/smoke_fire_detection/train_rfdetr.py` (MỚI — guard protocol khóa, shim tự tạo, verify resume checkpoint); `tasks/smoke_fire_detection/docs/research_plan.md` (mục 13 gap note → quyết định; thêm 13f đầy đủ protocol + số verify); `agent_context.md` (thêm server GB10 + 13f); `CHANGELOG.md`.
  - **Files gián tiếp:** `.venv` thêm ~35 package (rfdetr[train] stack, numpy downgrade); scratchpad (ngoài repo): `pyro_subset/` symlink, `rfdetr_mem_probe.py`, `rfdetr_resume_test.py`, `rfdetr_probe_run/` (1.5GB ckpt), `rfdetr_resume_baseline/` + `rfdetr_resume_test_run/` (~2.6GB mỗi cái — sẽ dọn sau khi user xác nhận), log files; `~/.roboflow/models/` cache pretrain weights rf-detr-large; tmux session `detach_test`. Chưa tạo `pyro-sdis-yolo-rfdetr/` shim trong repo (train_rfdetr.py sẽ tự tạo khi chạy thật — sẽ ghi nhận khi xảy ra).

- **2026-07-14 20:04:57 +0700**
  - **Task:** exclude artifact file ≥10 MB khỏi Git.
  - **Commands:** `uname -a`; `find artifacts -type f -size +10M -printf '%s %p\n' | sort -n`; `git ls-files --error-unmatch <path>`; `sed -i`; `git rm --cached artifacts/smoke_fire_detection/figlib_index.jsonl`; `git check-ignore -v <path>`; `git diff --check`.
  - **Files trực tiếp:** `.gitignore` (thêm exact rule cho `figlib_index.jsonl` 16.3 MB; `checkpoint_best_ema.pth` 141.5 MB); `CHANGELOG.md`.
  - **Files gián tiếp:** Git index bỏ track `artifacts/smoke_fire_detection/figlib_index.jsonl`; file local giữ nguyên; không có file/folder tự sinh.
